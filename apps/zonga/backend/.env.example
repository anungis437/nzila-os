# Zonga Django Backend — Environment Variables
# Copy to .env and fill in values

# Django
DJANGO_SECRET_KEY=change-me-to-a-random-string
DJANGO_DEBUG=True

# Database (shared with Drizzle — nzila_automation)
PGHOST=localhost
PGPORT=5433
PGDATABASE=nzila_automation
PGUSER=nzila
PGPASSWORD=nzila_dev

# Redis
REDIS_URL=redis://localhost:6379/2

# Authentication (Entra External ID)
AUTH_WEBHOOK_SECRET=whsec_your_webhook_secret
AUTH_JWKS_URL=https://login.microsoftonline.com/common/discovery/v2.0/keys

# Celery
CELERY_BROKER_URL=redis://localhost:6379/3

# CORS (Next.js frontend)
CORS_ALLOWED_ORIGINS=http://localhost:3006
