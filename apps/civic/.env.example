# CIVIC — .env.example
#
# Stub placeholder. No runtime code exists yet, so there are no environment
# variables to configure. This file exists to satisfy the repo's app-floor
# uniformity check (every app under apps/* must have README.md,
# .env.example, and package.json).
