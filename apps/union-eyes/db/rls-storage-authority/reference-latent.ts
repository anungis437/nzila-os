/**
 * db/rls-storage-authority/reference-latent.ts
 *
 * PR #752 round 9: extracted from the single-file
 * db/rls-storage-authority-manifest.ts (which exceeded the repository's
 * 8000-line hard cap at 8009 lines — see
 * tooling/contract-tests/file-size-enforcement.test.ts) into semantic
 * domain modules. This module owns: Everything else — reference/config data and remaining latent/unclassified tables.
 *
 * Domain boundaries were assigned mechanically by table-name keyword
 * matching against the entry's `table` field (see the one-time migration
 * script referenced in the round-9 commit message) — every entry's DATA
 * (classification, reason, privileges, authority) is unchanged from the
 * pre-split manifest; only its physical file location changed. Do not
 * hand-edit an entry's domain placement without also checking
 * db/__tests__/rls-storage-authority-registry-composition.test.ts, which
 * proves the composed registry is unaffected by which module an entry
 * lives in.
 *
 * Add NEW entries to whichever domain module most closely matches the
 * table's real subject matter; when genuinely ambiguous, prefer
 * reference-latent.ts (the catch-all) over forcing a bad fit elsewhere.
 */
import type { StorageAuthorityEntry } from './types';

export const referenceLatentEntries: StorageAuthorityEntry[] = [
  {
    table: "ab_tests",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): lib/ab-testing/ab-test-engine.ts (the TS side) has zero real callers anywhere under app/, actions/, lib/, services/. Django (ai_core app) has a live, router-registered AbTestsViewSet (queryset=AbTests.objects.all(), IsAuthenticated-only, no organization scoping) — contained via the existing DenyAllPermission in backend/ai_core/views.py (round 35). There is no legitimate consumer on either side. Root of the ab_test_variants/ab_test_assignments/ab_test_events cascade, all closed identically this round.",
    supportingCapability: ["backend/ai_core/views.py","backend/ai_core/tests_round50_ab_testing_containment.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "accessibility_issues",
    classification: "LATENT_UNREACHABLE",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/domains/infrastructure/accessibility.ts), parent-owned through accessibility_audits, but its sole non-test reference lib/accessibility/accessibility-service.ts has zero callers anywhere under app/, actions/, lib/, services/, scripts/ (verified by name-search — same dead file as accessibility_audits).",
    supportingCapability: ["lib/accessibility/accessibility-service.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "accessibility_test_suites",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'accessibilityTestSuites' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "accessibility_user_testing",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'accessibilityUserTesting' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "alert_escalations",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'alertEscalations' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "alert_rules",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 46 (SYSTEM_AND_MIXED_EXECUTION_AUTHORITY cohort, MIXED authority per doctrine's TENANT_RLS_REQUIRED-with-system-access shape, precedent: 'campaigns'): organization_id NOT NULL. TENANT PATH: app/api/alerts/realtime/route.ts (withApi, auth.minRole='member', organizationId trusted-derived from the framework's auth context) -> getRecentRealtimeAlerts(organizationId, limit) — SELECT only, innerJoin against alertExecutions, filtered by organizationId AND category='realtime-observability'. SYSTEM PATH: app/api/cron/observability-alerts/route.ts (withApi, auth.cron=true, a real registered cron root) -> runRealtimeObservabilitySweep() -> raw-SQL enumerates ALL organizations with breach/failure signals (GROUP BY organization_id across ml_predictions/ingestion_batches/platform_payments — legitimate system-wide enumeration, not a tenant-scoped call), then per-organization calls getOrCreateRealtimeRule()/emitSignal() which INSERT (and occasionally SELECT-then-skip-INSERT) scoped to that specific signal.organizationId — INSERT, SELECT. The 8 app/api/admin/alerts/* routes remain confirmed misleading generator artifacts bound to organizationMembers, not alert_rules. SECURITY DEFECT FOUND AND FIXED this round: runRealtimeObservabilitySweep() and its helpers executed entirely through the plain tenant `db` import despite being SYSTEM_SCHEDULE-invoked — a principal mismatch (doctrine section 22). Fixed by wrapping the sweep body in withSystemContext(), which (via the db Proxy's AsyncLocalStorage routing in db/db.ts) transparently routes every nested db.* call in getOrCreateRealtimeRule/emitSignal onto the union_eyes_system connection without needing to touch those helpers directly. services/observability/__tests__/realtime-alerting-service.test.ts updated to mock withSystemContext (6/6 tests passing). alert_executions (read via the tenant innerJoin, written via emitSignal) and notificationTracking/inAppNotifications (written via emitSignal) are NOT classified this round — out of scope per the round's SYSTEM_AND_MIXED_EXECUTION_AUTHORITY focus, left for a future PARENT_OWNED/notifications-focused round.",
    supportingCapability: ["app/api/alerts/realtime/route.ts","app/api/cron/observability-alerts/route.ts","services/observability/realtime-alerting-service.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: ["SELECT","INSERT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "NONE",
  },
  {
    table: "allocation_rules",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CORRECTED (round 50, state-machine root and fan-out cascade authority): the round-40 finding that allocation-engine.ts \u2018has zero production importers anywhere outside manifest/tests\u2019 was STALE/INCORRECT — app/api/finance/allocation/route.ts genuinely imports and calls getAllocationRules/createAllocationRule, and app/api/finance/allocation/run/route.ts genuinely calls runAllocation (which resolves this table's rows via ruleId). Direct NOT NULL organization_id column; both GET and POST require withMinRole + requireEntitlement(organizationId, 'allocation_engine', userId) and pass the trusted context.organizationId straight through (never client-suppliable). Root of the allocation_rule_versions/allocation_runs/allocation_basis_snapshots/allocation_run_lines cascade. SECURITY DEFECT FOUND AND FIXED (see allocation_rule_versions entry): runAllocation() previously resolved a client-supplied ruleId with no tenant check at all, letting one organization execute an allocation using another organization's private rule (cross-tenant IDOR) — fixed via a new verifyAllocationRuleOwnership guard.",
    supportingCapability: ["app/api/finance/allocation/route.ts","app/api/finance/allocation/run/route.ts","services/platform-economics/allocation-engine.ts","services/platform-economics/__tests__/allocation-engine.test.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "anti_scab_violations",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/domains/health-safety/cnesst-schema.ts). Reachable via live HTTP routes app/api/cnesst/anti-scab/route.ts, app/api/cnesst/anti-scab/[id]/route.ts.",
    supportingCapability: ["app/api/cnesst/anti-scab/route.ts","app/api/cnesst/anti-scab/[id]/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "api_access_tokens",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'apiAccessTokens' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "automation_rules",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 57: organization_id (aliased orgId in Drizzle) column present (nullable at the DB level, but every reachable query filters on it). Genuinely reachable: app/api/rewards/cron/route.ts (CRON_SECRET-authenticated system job) calls processScheduledAwards(orgId)/processAnniversaryAwards(orgId) in lib/services/rewards/automation-service.ts, which SELECTs `eq(automationRules.orgId, orgId) AND eq(automationRules.isActive, true)`. SECURITY DEFECT FOUND AND FIXED THIS ROUND (sibling cleanup): core.AutomationRulesViewSet was IsAuthenticated-only with no scoping at all — contained via DenyAllPermission (the cron path remains the sole legitimate consumer).",
    supportingCapability: ["app/api/rewards/cron/route.ts","lib/services/rewards/automation-service.ts","backend/core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "SYSTEM_SCHEDULE",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "break_policies",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 41 (DIRECT_ORG_RUNTIME_BATCH): direct NOT NULL organization_id column. Both app/api/breaks/policies/route.ts (collection) and app/api/breaks/policies/[id]/route.ts (item) are crud-factory routes (lib/api/crud-factory.ts) with orgScoped:true, readRole:'member', writeRole:'steward' — the factory forces eq(organizationId, <authenticated caller's own org>) on every SELECT/UPDATE/DELETE and sets organizationId from the trusted context (never the client body) on INSERT; verified against crud-factory.ts's GET/POST/PATCH/DELETE handlers directly. No Django model exists for this table.",
    supportingCapability: ["app/api/breaks/policies/[id]/route.ts","app/api/breaks/policies/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "calendar_events",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. app/api/calendar/events/route.ts + [id]/route.ts are crudRoutes()-backed (orgScoped:true, server-derived organizationId via withApi()). calendar_id is a parent FK to calendars, consistent with the same organization boundary.",
    supportingCapability: ["app/api/calendar/events/[id]/route.ts","app/api/calendar/events/route.ts","app/api/calendars/[id]/events/route.ts","app/api/events/[id]/occurrences/route.ts","app/api/events/[id]/route.ts","app/api/members/appointments/route.ts","lib/calendar-reminder-scheduler.ts","lib/external-calendar-sync/google-calendar-service.ts","lib/external-calendar-sync/microsoft-calendar-service.ts","lib/recurring-events-service.ts","services/financial-service/drizzle/relations.ts","services/financial-service/drizzle/schema.ts","services/financial-service/src/db/schema.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "calendar_sharing",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 6, Phase-3A priority tranche + reachability-scan correction): calendarSharing has a real, non-duplicate union-eyes-native pgTable('calendar_sharing', ...) declaration (db/schema/domains/scheduling/calendar.ts, db/schema/calendar-schema.ts), but zero non-test references to it exist anywhere in app/, actions/, lib/, or services/ within apps/union-eyes proper. The original scan's supportingCapability list for this table only surfaced services/financial-service's own same-named, separately-databased table (its own drizzle.config.ts/DATABASE_URL) and MISSED the real union-eyes declaration entirely — this entry corrects that gap. No known union-eyes application code path queries this table today.",
    supportingCapability: ["db/schema/domains/scheduling/calendar.ts","db/schema/calendar-schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "calendars",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, CALENDAR_AND_SCHEDULING family): db/schema/domains/scheduling/calendar.ts's calendars has organizationId NOT NULL (FK organizations, cascade) AS THE TENANT BOUNDARY, plus ownerId (text, NOT NULL, the creating user) and isPersonal (default true) as a WITHIN-TENANT visibility sub-scope — not a second tenant boundary. SECURITY DEFECTS FOUND AND FIXED (same-org peer exposure of personal calendars, an IDOR-class defect): app/api/calendars/route.ts's GET (collection list) and app/api/calendars/[id]/route.ts's GET/PATCH/DELETE previously only applied org-scoping via crud-factory's generic orgScoped option — crud-factory has no primitive for a conditional 'visible if shared OR mine' predicate, so EVERY org member could list, read, edit, and delete every OTHER member's personal (isPersonal=true) calendar. Fixed with hand-written GET handlers (app/api/calendars/route.ts, app/api/calendars/[id]/route.ts) that filter to organizationId=callerOrg AND (isPersonal=false OR ownerId=callerUserId), item GET 404s instead of leaking existence, and a lockedAuthCheck on PATCH/DELETE that 403s a steward attempting to mutate another member's personal calendar on the locked, guaranteed-fresh row (TOCTOU-safe, same pattern as crud-factory's other lockedAuthCheck consumers). SEPARATE DEFECT FIXED: ownerId had no dedicated auto-set path in crud-factory (only a column literally named createdBy is auto-forced) — POST was fully client-controllable for ownerId, letting a steward attribute a fabricated personal calendar to an arbitrary victim userId. Fixed via a beforeCreate hook that force-sets ownerId to the caller's own id. Regression tests: app/api/calendars/__tests__/route.test.ts, app/api/calendars/[id]/__tests__/route.test.ts (11 tests). SYSTEM READ PATH (SYSTEM_SCHEDULE, no defect found): app/api/cron/calendar-sync/route.ts is a genuine cron route (isCronRoute() auto-detected from the /api/cron/ path segment, verified via verifyCronAuth() cron-secret check in lib/api-auth-guard.ts's withApiAuth, not user JWT auth) that SELECTs calendars (never INSERT/UPDATE/DELETE — confirmed via exhaustive grep for `.update(calendars)`/`.insert(calendars)`/`.delete(calendars)` finding zero matches anywhere in the app) to validate a per-connection localCalendarId belongs to that SAME connection's own organizationId before importing external events — every query is explicitly WHERE-scoped in application code (calendars carries no deployed RLS policy; not one of the 24 baseline-0108.ts tables), so this is a correctly-scoped SYSTEM_SCHEDULE reader on plain TENANT_RUNTIME db, not a round-46-class principal mismatch (unlike that precedent, there is no missing scoping predicate here). app/api/calendar-sync/connections/[id]/sync/route.ts (TENANT_USER, withOrganizationAuth) similarly validates localCalendarId against the caller's own organizationId before triggering a sync — no cross-tenant IDOR on this table. RESIDUAL OBSERVATION (not a calendars-table defect, out of this round's 11-table scope): that same sync-trigger route does not additionally check isPersonal/ownerId before writing synced events, so a same-org steward could in principle trigger an import into another member's personal calendar's event stream — this is a calendar_events authorization question, not a calendars storage-authority question, and calendar_events is not one of the round-53 cohort tables; flagged for a future round. DUAL-SCHEMA FINDING (same as round 52's user_uuid_mapping/arbitrator_profiles precedent, not re-litigated): services/financial-service independently re-declares calendars/meeting_rooms in its own schema.ts/drizzle files, but that service has its own package.json, drizzle.config.ts, and DATABASE_URL pointing at a distinct Supabase Postgres project (services/financial-service/.env.example) — a genuine SEPARATE_DATABASE_BOUNDARY for that service's own copy, not evidence about THIS app's calendars table. lib/services/calendar-service.ts's listCalendars/getCalendarById/createCalendar/etc. accept raw caller-supplied filters with no trusted-context enforcement of their own, but git-grep confirms ZERO real production callers anywhere outside their own file and tests — dead code, not a live authority path. Django backend/unions/views.py's CalendarsViewSet was IsAuthenticated-only with no organization filter at all and no legitimate TS/frontend consumer of the Django REST path exists — contained via DenyAllPermission (backend/unions/tests_round53_calendar_scheduling_containment.py).",
    supportingCapability: ["app/[locale]/calendar/page.tsx","app/api/calendar-sync/connections/[id]/sync/route.ts","app/api/calendars/[id]/route.ts","app/api/calendars/route.ts","app/api/cron/calendar-sync/route.ts","lib/external-calendar-sync/google-calendar-service.ts","lib/external-calendar-sync/microsoft-calendar-service.ts","lib/services/calendar-service.ts","backend/unions/views.py","backend/unions/tests_round53_calendar_scheduling_containment.py"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "card_signing_events",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'cardSigningEvents' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "chargeback_statements",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CORRECTED round 57 (round-56's LATENT candidate was a false negative from checking only the literal filename 'allocation-engine'): app/api/finance/chargebacks/route.ts (withMinRole('officer') + requireEntitlement) calls getChargebacks({organizationId, ...}) re-exported through the services/platform-economics barrel (index.ts does `export * from './allocation-engine'`), which this round's file-name-only reachability check missed. organization_id NOT NULL, parent FKs to billingPeriods/allocationRuns. Genuinely reachable, tenant-scoped read.",
    supportingCapability: ["app/api/finance/chargebacks/route.ts","services/platform-economics/allocation-engine.ts","services/platform-economics/index.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "clause_comparisons",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): direct NOT NULL organization_id table with live app/api/clauses/compare/route.ts POST path. The route is withRoleAuth('member') gated, rejects any body.organizationId that does not match context.organizationId, then calls lib/services/clause-service.ts saveClauseComparison() with that context-matched organizationId and context userId. The service only inserts clauseComparisons in this path; other clause-service route imports do not read this table. services/financial-service references are a separate service schema, not Union Eyes runtime authority. ClauseComparisonsViewSet is separately router-registered but now unconditionally denied via DenyAllPermission because it has no legitimate Django consumer. Minimum proven runtime DML is INSERT only, no withSystemContext.",
    supportingCapability: ["app/api/clauses/compare/route.ts","lib/services/clause-service.ts","services/financial-service/drizzle/schema.ts","services/financial-service/src/db/schema.ts"],
    requiredRuntimePrivileges: ["INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "clause_comparisons_history",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 6, Phase-3A priority tranche + reachability-scan correction): clauseComparisonsHistory has a real, non-duplicate union-eyes-native pgTable('clause_comparisons_history', ...) declaration (db/schema/domains/agreements/shared-library.ts, db/schema/shared-clause-library-schema.ts), but zero non-test references to it exist anywhere in app/, actions/, lib/, or services/ within apps/union-eyes proper. The original scan's supportingCapability list for this table only surfaced services/financial-service's own same-named, separately-databased table (its own drizzle.config.ts/DATABASE_URL) and MISSED the real union-eyes declaration entirely — this entry corrects that gap. No known union-eyes application code path queries this table today.",
    supportingCapability: ["db/schema/domains/agreements/shared-library.ts","db/schema/shared-clause-library-schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "clc_api_config",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'clcApiConfig' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "clc_per_capita_benchmarks",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): sole TS reference is lib/services/external-data/clc-partnership-service.ts and that service has zero production importers; no raw-SQL reference exists. The generated ClcPerCapitaBenchmarksViewSet in backend/billing/views.py is router-registered but now uses unconditional SharedDenyAllPermission, so the only reachable surface grants no authority and no legitimate consumer exists.",
    supportingCapability: ["lib/services/external-data/clc-partnership-service.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "cms_blocks",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'cmsBlocks' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "cms_navigation_menus",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'cmsNavigationMenus' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "cms_pages",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. app/api/portal/documents/route.ts + upload/route.ts are crudRoutes()-backed (orgScoped:true, server-derived organizationId). template_id/created_by/updated_by are parent/audit-actor FKs, not a competing ownership axis.",
    supportingCapability: ["app/api/portal/documents/route.ts","app/api/portal/documents/upload/route.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "cnesst_filings",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/domains/health-safety/cnesst-schema.ts). Reachable via live HTTP routes app/api/cnesst/filings/route.ts, app/api/cnesst/filings/[id]/route.ts.",
    supportingCapability: ["app/api/cnesst/filings/route.ts","app/api/cnesst/filings/[id]/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "comparative_analyses",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 6, Phase-3A priority tranche + reachability-scan correction): comparativeAnalyses has a real, non-duplicate union-eyes-native pgTable('comparative_analyses', ...) declaration (db/schema/domains/analytics/analytics.ts, db/schema/analytics.ts), but zero non-test references to it exist anywhere in app/, actions/, lib/, or services/ within apps/union-eyes proper. The original scan's supportingCapability list for this table only surfaced services/financial-service's own same-named, separately-databased table (its own drizzle.config.ts/DATABASE_URL) and MISSED the real union-eyes declaration entirely — this entry corrects that gap. No known union-eyes application code path queries this table today.",
    supportingCapability: ["db/schema/domains/analytics/analytics.ts","db/schema/analytics.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "compliance_alerts",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CORRECTED round 42: the census placed this in the PARENT_OWNED lane (its orgColumn detector only recognizes the literal name 'organization_id'), but domains/compliance/employer-compliance.ts declares a DIRECT NOT NULL org_id column on this table (a naming variant, not a parent-owned relationship) — classified TENANT_RLS_REQUIRED, not PARENT_OWNED_RLS_REQUIRED. app/api/compliance/alerts/route.ts (withOrganizationAuth, organizationId server-derived) filters eq(complianceAlerts.orgId, organizationId) directly; the innerJoin to employers is for display (employer name) only, not authority. Read-only (SELECT); no insert/update/delete route exists. No Django model exists for this table.",
    supportingCapability: ["app/api/compliance/alerts/route.ts","lib/ai/employer-risk.ts","lib/ai/executive-insights.ts","lib/services/dashboard-kpi-service.ts","services/lmbp-immigration-service.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "contract_covered_orgs",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/domains/finance/contract-amendments.ts). Sole reference services/platform-economics/entitlement-guard.ts is one of the most widely-used modules in the app — reachable from 50+ live HTTP routes (app/api/grievances/**, app/api/cases/**, app/api/finance/**, app/api/ai/**, app/api/documents/**, app/api/billing/**, etc.) via lib/api/with-api.ts/lib/api/framework.ts.",
    supportingCapability: ["services/platform-economics/entitlement-guard.ts","lib/api/with-api.ts","lib/api/framework.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "correspondence",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Verified 2026-09-01, corrected 2026-09-02 (round 5, real paths — the prior citations omitted the [id] dynamic segment and did not exist): direct NOT NULL organization_id column, reachable via live HTTP routes app/api/correspondence/[id]/dispatch/route.ts (minRole steward), app/api/correspondence/[id]/sign/route.ts (minRole officer), app/api/correspondence/[id]/cancel/route.ts (minRole steward), app/api/correspondence/[id]/revision/route.ts (minRole officer) — all withApi(), all tenant org-scoped roles, plain `db` import, no withSystemContext.",
    supportingCapability: ["app/api/correspondence/[id]/dispatch/route.ts","app/api/correspondence/[id]/sign/route.ts","app/api/correspondence/[id]/cancel/route.ts","app/api/correspondence/[id]/revision/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "course_registrations",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, TRAINING_AND_REGISTRATION family): db/schema/domains/scheduling/training.ts's courseRegistrations has organizationId NOT NULL (own FK to organizations, independently declared — NOT derived from courseId/sessionId) plus memberId (text, NOT NULL, the registrant) as a within-tenant subject/owner scope, plus courseId FK -> trainingCourses.id and sessionId FK -> courseSessions.id. TWO LIVE ROUTES with DIFFERENT legitimate semantics: app/api/education/registrations/route.ts (writeRole:'member', self-registration — the frontend's CourseCatalog.tsx handleEnroll always sends the caller's own memberId) and app/api/education/completions/route.ts (writeRole:'steward', a privileged steward recording a completion ON BEHALF OF a member — memberId here is legitimately NOT the caller's own id, so it is intentionally left unforced). SECURITY DEFECTS FOUND AND FIXED, scoped to the self-registration route only: (1) crud-factory's ownerColumn:'memberId' restricts READS to the caller's own rows but has no auto-set path on CREATE (only a column literally named createdBy is force-set) — memberId was fully client-controllable on POST /api/education/registrations, letting any 'member' register an arbitrary victim memberId without consent. Fixed via a beforeCreate hook forcing memberId to the caller's own id. (2) Cross-org FK injection (round-53 doctrine section 27): because organizationId is independently declared (not derived from courseId/sessionId), nothing previously verified that a client-supplied courseId/sessionId belonged to the caller's own organization before insert — a member could reference another organization's course/session UUID (if known) in a registration landing in their own org. Fixed by validating both FKs against trainingCourses.organizationId/courseSessions.organizationId inside the same beforeCreate hook, throwing ApiError.badRequest on mismatch. Regression tests: app/api/education/registrations/__tests__/route.test.ts (4 tests). No PATCH/DELETE route exists for this table via the TS API today (only the two POST-capable collection routes above) — requiredRuntimePrivileges reflects SELECT+INSERT only, not full CRUD. Django backend/unions/views.py's CourseRegistrationsViewSet was IsAuthenticated-only with a member_id filterset field and NO organization filter at all — any authenticated user could enumerate/read any organization's registrations (test scores, attendance, certification status) by member_id; no legitimate TS/frontend consumer of the Django REST path exists — contained via DenyAllPermission (backend/unions/tests_round53_training_registration_containment.py). DUAL-SCHEMA FINDING (round-48 precedent, not re-litigated): services/financial-service re-declares course_registrations in its own schema — independently verified as a genuine SEPARATE_DATABASE_BOUNDARY via that service's own package.json/drizzle.config.ts/DATABASE_URL (same finding as calendars/meeting_rooms this round).",
    supportingCapability: ["app/api/education/completions/route.ts","app/api/education/registrations/route.ts","app/api/education/registrations/__tests__/route.test.ts","backend/unions/views.py","backend/unions/tests_round53_training_registration_containment.py"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "course_sessions",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Verified 2026-09-01, corrected 2026-09-02 (round 5 — the prior citation app/api/education/course-sessions/route.ts does not exist; real path is app/api/education/sessions/route.ts): direct NOT NULL organization_id column, reachable via crudRoutes({ table: courseSessions, readRole: 'member', writeRole: 'steward' }) at app/api/education/sessions/route.ts, and lib/services/education-service.ts (SELECT/INSERT/UPDATE), both using the plain `db` import, no withSystemContext.",
    supportingCapability: ["app/api/education/sessions/route.ts","lib/services/education-service.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "currency_exchange_rates",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): TS references are limited to lib/services/exchange-rate-service.ts and lib/services/multi-currency-treasury-service.ts, neither of which has a production importer; no raw-SQL reference exists. The generated CurrencyExchangeRatesViewSet in backend/billing/views.py is router-registered but now uses unconditional SharedDenyAllPermission, so the only reachable surface grants no authority and no legitimate consumer exists.",
    supportingCapability: ["lib/services/exchange-rate-service.ts","lib/services/multi-currency-treasury-service.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "customer_onboarding_milestones",
    classification: "SYSTEM_ONLY",
    reason: "CLOSED round 56 (CROSS_TENANT_PLATFORM_RESOURCE archetype): app/[locale]/dashboard/customer-success/page.tsx's loadOnboarding() runs a genuinely cross-organization raw SQL query (`FROM customer_onboarding_milestones m JOIN organizations o ...`, no organization filter, intentionally platform-wide) but is gated by requireUser()+isSystemAdmin() (redirect on failure) and executed inside withSystemContext() — a legitimate platform_lead/system_admin oversight capability, not a tenant-facing surface. No other reachable path exists.",
    supportingCapability: ["app/[locale]/dashboard/customer-success/page.tsx"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "PLATFORM_ADMIN",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "data_anonymization_log",
    classification: "LATENT_UNREACHABLE",
    reason: "Manually verified 2026-09-02: sole non-test reference lib/gdpr/consent-manager.ts has zero callers anywhere under app/, actions/, lib/, services/, scripts/ (verified by name-search) — consistent with the entire lib/gdpr/** directory being excluded from the TypeScript project (apps/union-eyes/tsconfig.json exclude list).",
    supportingCapability: ["lib/gdpr/consent-manager.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "data_processing_records",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'dataProcessingRecords' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "data_quality_warnings",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 57: organization_id NOT NULL, parent FK to ingestionBatches. Genuinely reachable via app/api/admin/ingest/batches/route.ts and [id]/route.ts (admin-only), which call lib/ingestion/migration-metrics.ts's functions that SELECT `eq(dataQualityWarnings.batchId, batchId)`.",
    supportingCapability: ["app/api/admin/ingest/batches/route.ts","lib/ingestion/migration-metrics.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "data_residency_configs",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'dataResidencyConfigs' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "data_retention_policies",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'dataRetentionPolicies' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "defensibility_packs",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Verified from live API usage: app/api/claims/[id]/evidence/route.ts resolves the claim via requireApiAuth({ orgScoped: true }) then queries defensibilityPacks by case_id only after confirming the claim belongs to the caller's organization (SELECT); lib/workflow-engine.ts inserts a pack when a case reaches a defensibility-triggering transition (INSERT). No UPDATE or DELETE against this table exists in production code (git grep confirmed) — download-count/verification-status columns are written only at generation time, never mutated afterward. Two identical-shape declarations exist (db/schema/defensibility-packs-schema.ts, imported directly by evidence/route.ts and workflow-engine.ts, vs db/schema/domains/infrastructure/defensibility.ts, reached via the '@/db/schema' barrel through domains/infrastructure/index.ts) — column names/types/nullability/defaults are IDENTICAL_OR_PROVEN_COMPATIBLE; the only difference is that the domain-consolidated copy dropped the Drizzle-level .references() FK annotations (case_id→claims.claim_id, pack_id→defensibility_packs.pack_id), which affects migration-generation fidelity but not the physical table or RLS policy target, since both declarations emit pgTable('defensibility_packs', ...) against the same physical table. Authority: the table carries its OWN NOT NULL organization_id column, populated at pack-generation time from the parent claim's org — this is a real, non-invented column (not fabricated to simplify RLS), so a direct TENANT_RLS_REQUIRED policy on organization_id is preferred over a PARENT_OWNED_RLS_REQUIRED join through claims; it is simpler and does not change the effective authorization boundary, which the app already double-enforces by checking claims.organization_id before the pack lookup. requiredRuntimePrivilege is SELECT_INSERT, not FULL_DML.",
    supportingCapability: ["app/api/claims/[id]/evidence/route.ts","lib/workflow-engine.ts","db/schema/defensibility-packs-schema.ts","db/schema/domains/infrastructure/defensibility.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "dispatch_requests",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): direct NOT NULL org_id column (db/schema/domains/dispatch/dispatch.ts). Reachable via app/api/dispatch/{requests,assign,queue}/route.ts, all withOrganizationAuth + hasMinRole gated, passing the trusted context.organizationId straight through. STATE MACHINE: status open -> partially_filled|filled (assignWorkersToDispatch) -> [cancelled/expired also modeled in the enum but no route transitions to them yet]. Root of the dispatch_assignments cascade (scoped via requestId, no org column of its own) and dispatch_rules (its own direct org_id column, closed separately). SECURITY DEFECT FOUND AND FIXED: assignWorkersToDispatch() (lib/services/dispatch-engine.ts) previously took no orgId and performed no check that a client-supplied requestId belonged to the caller's organization, letting one organization inject assignment rows into another organization's dispatch request (cross-tenant IDOR) — fixed by requiring orgId and verifying request ownership before any insert, mirroring the ownership check already correctly used in the same file's rankCandidates(). 3 tests updated + 1 new regression test in lib/services/__tests__/dispatch-engine.test.ts (all 28 tests across both dispatch-engine test files pass).",
    supportingCapability: ["app/api/dispatch/requests/route.ts","app/api/dispatch/assign/route.ts","app/api/dispatch/queue/route.ts","lib/services/dispatch-engine.ts","lib/services/__tests__/dispatch-engine.test.ts","lib/__tests__/dispatch-engine.test.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "dispatch_rules",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): direct NOT NULL org_id column (db/schema/domains/dispatch/dispatch.ts). Read via loadDispatchRules(orgId) inside rankCandidates(), itself called with the trusted org-scoped requestId ownership check already in place. No dedicated write route found (rules are configuration, not yet exposed for tenant self-service) — no defect, just a smaller current surface than dispatch_requests.",
    supportingCapability: ["lib/services/dispatch-engine.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "dsr_requests",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'dsrRequests' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "dunning_cases",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): services/platform-economics/dunning-service.ts is re-exported through services/platform-economics/index.ts (export * from './dunning-service') but exhaustive grep across app/, actions/, lib/, services/ (including app/api/cron/**) confirms ZERO real callers of any exported function outside the service file itself and the barrel re-export. No Django exposure. Root of the dunning_policies/dunning_steps cascade, closed identically this round.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "duplicate_groups",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 41 (DIRECT_ORG_RUNTIME_BATCH): direct NOT NULL organization_id column (ingestion-schema.ts). app/api/admin/duplicates/route.ts (minRole admin) filters eq(duplicateGroups.organizationId, organizationId) on GET/POST, with the POST update path verified via a preceding and(eq(id, body.group_id), eq(organizationId, organizationId)) existence check (verify-then-mutate on the internal UUID pk, not a collision-prone external key). The INSERT path (lib/ingestion/fuzzy-dedup.ts, invoked from app/api/admin/ingest/route.ts) takes organizationId as an explicit param sourced from that route's trusted withApi context, which additionally rejects any uploaded record whose organization_id mismatches the caller's own. Some reads execute inside withSystemContext but every query still manually filters by the caller's own trusted organizationId (same accepted pattern as ingestion_batches/collective_agreements in this manifest). No Django model exists for this table.",
    supportingCapability: ["app/api/admin/duplicates/route.ts","app/api/admin/ingest/route.ts","lib/ingestion/fuzzy-dedup.ts","lib/ingestion/migration-metrics.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "employer_contacts",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 41 (DIRECT_ORG_RUNTIME_BATCH), with a fix applied first: direct NOT NULL organization_id column (domains/communications/employer-communications.ts). app/api/employers/communications/contacts/route.ts (withOrganizationAuth, organizationId server-derived) filters eq(employerContacts.organizationId, organizationId) on GET and sets organizationId from the trusted context on INSERT. DEFECT FOUND AND FIXED this round: the client-supplied employerId (a plain uuid column with no DB-level FK) was accepted on POST with no verification it belonged to the caller's organization, allowing a contact row to be attached to another organization's employer record; the route now verifies and(eq(employers.id, data.employerId), eq(employers.organizationId, organizationId)) before insert and rejects otherwise. No Django model exists for this table.",
    supportingCapability: ["app/api/employers/communications/contacts/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "employer_execution_artifacts",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (APPEND_ONLY_EVIDENCE archetype): organization_id NOT NULL. Reachable via app/api/employer-execution/{payroll-runs,remittance-runs,replay}/** routes (withApi()-backed, server-derived organizationId) and lib/workers/employer-execution/process-evidence-seal.ts (system-invoked worker writing sealed evidence artifacts). payroll_run_id/remittance_run_id are parent FKs within the same organization boundary.",
    supportingCapability: ["app/[locale]/dashboard/employer-execution/payroll-runs/[id]/page.tsx","app/[locale]/dashboard/employer-execution/remittance-runs/[id]/page.tsx","app/api/employer-execution/payroll-runs/[id]/route.ts","app/api/employer-execution/remittance-runs/[id]/route.ts","app/api/employer-execution/remittance-runs/route.ts","app/api/employer-execution/replay/[runId]/route.ts","lib/workers/employer-execution/process-evidence-seal.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: ["SELECT", "INSERT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "HIGH",
  },
  {
    table: "employer_execution_compliance_events",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (APPEND_ONLY_EVIDENCE archetype): organization_id NOT NULL. Reachable via app/api/employer-execution/{compliance,payroll-runs,replay}/** routes (withApi()-backed, server-derived organizationId) and lib/workers/employer-execution/process-compliance-watchdog.ts (system-invoked worker). payroll_run_id/remittance_run_id are parent FKs within the same organization boundary.",
    supportingCapability: ["app/[locale]/dashboard/employer-execution/compliance/page.tsx","app/[locale]/dashboard/employer-execution/payroll-runs/[id]/page.tsx","app/[locale]/dashboard/employer-execution/remittance-runs/[id]/page.tsx","app/api/employer-execution/compliance/route.ts","app/api/employer-execution/payroll-runs/[id]/route.ts","app/api/employer-execution/payroll-runs/route.ts","app/api/employer-execution/replay/[runId]/route.ts","lib/workers/employer-execution/process-compliance-watchdog.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: ["SELECT", "INSERT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "HIGH",
  },
  {
    table: "employer_execution_replays",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (WORKER_OR_CRON_EXECUTION archetype): organization_id NOT NULL. Reachable via app/api/employer-execution/replay/[runId]/route.ts (withApi()-backed, server-derived organizationId) and lib/workers/employer-execution/process-replay-run.ts (system-invoked worker). source_payroll_run_id/replay_payroll_run_id are parent FKs within the same organization boundary.",
    supportingCapability: ["app/[locale]/dashboard/employer-execution/payroll-runs/[id]/page.tsx","app/api/employer-execution/replay/[runId]/route.ts","lib/workers/employer-execution/process-replay-run.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: ["SELECT", "INSERT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "HIGH",
  },
  {
    table: "employer_responses",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'employerResponses' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "employer_timesheet_entries",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. Reachable via app/api/employer-execution/{payroll-runs,replay,timesheets}/** routes (withApi()-backed, server-derived organizationId). batch_id/member_employment_id/employer_id/worksite_id/bargaining_unit_id/job_classification_id are parent FKs consistent with the same tenant boundary as employer_timesheet_batches.",
    supportingCapability: ["app/[locale]/dashboard/employer-execution/timesheets/page.tsx","app/api/employer-execution/payroll-runs/route.ts","app/api/employer-execution/replay/[runId]/route.ts","app/api/employer-execution/timesheets/route.ts","lib/workers/employer-execution/process-timesheet-validation.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "employers",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/union-structure-schema.ts). Reachable via multiple live HTTP routes: app/api/employers/route.ts, app/api/employers/[id]/route.ts, app/api/employers/communications/route.ts, app/api/employers/communications/contacts/route.ts, app/api/ai/employers/[id]/risk/route.ts, app/api/compliance/alerts/route.ts, app/api/compliance/reports/route.ts, app/api/pilot/onboarding/route.ts.",
    supportingCapability: ["app/api/employers/route.ts","app/api/employers/[id]/route.ts","app/api/employers/communications/route.ts","app/api/employers/communications/contacts/route.ts","app/api/ai/employers/[id]/risk/route.ts","app/api/compliance/alerts/route.ts","app/api/compliance/reports/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "employment_history",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'employmentHistory' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "event_attendees",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: lib/calendar-reminder-scheduler.ts and lib/recurring-events-service.ts (the two TS references) have ZERO real callers in app/, actions/ — dead TS (the financial-service references are that service's own duplicate schema declaration, not union-eyes production code). SECURITY DEFECT FOUND AND FIXED THIS ROUND: unions.EventAttendeesViewSet was IsAuthenticated-only with no organization filter — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["lib/calendar-reminder-scheduler.ts","lib/recurring-events-service.ts","backend/unions/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "event_check_ins",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'eventCheckIns' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "event_registrations",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'eventRegistrations' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "event_reminders",
    classification: "LATENT_UNREACHABLE",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id + user_id columns (db/schema/domains/scheduling/calendar.ts), but its sole non-test union-eyes reference is lib/calendar-reminder-scheduler.ts, whose exports (scheduleEventReminders, cancelEventReminders, rescheduleEventReminders, etc.) have zero callers anywhere under app/, actions/, lib/, services/, scripts/ (verified by name-search). The only other reference is services/financial-service/** — a separately deployed service (own package.json/drizzle.config.ts/server entrypoint), not part of union-eyes' own request handling.",
    supportingCapability: ["lib/calendar-reminder-scheduler.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "exit_interview_events",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (STATE_MACHINE_RESOURCE archetype): organization_id NOT NULL. Reachable via app/api/exit-interviews/[id]/{governance,index,publish}/route.ts, withApi()-backed with server-derived organizationId. interview_id is a parent FK to exitInterviews within the same organization boundary; governance/publish routes gate lifecycle transitions rather than allowing generic PATCH.",
    supportingCapability: ["app/api/exit-interviews/[id]/governance/route.ts","app/api/exit-interviews/[id]/index/route.ts","app/api/exit-interviews/[id]/publish/route.ts","app/api/exit-interviews/[id]/review/route.ts","app/api/exit-interviews/[id]/route.ts","app/api/exit-interviews/[id]/submit/route.ts","app/api/exit-interviews/[id]/summarize/route.ts","app/api/exit-interviews/analytics/route.ts","app/api/exit-interviews/governance-timeline/route.ts","app/api/exit-interviews/route.ts","lib/knowledge-transfer/governance/consent-controls.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "exit_interview_sessions",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'exitInterviewSessions' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "exit_interviews",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 41 (DIRECT_ORG_RUNTIME_BATCH): direct NOT NULL organization_id column (domains/infrastructure/exit-interviews.ts). All 12 app/api/exit-interviews/** routes are withApi-authenticated (minRole member/steward/officer) and consistently filter eq(exitInterviews.organizationId, organizationId) (or and() with the record id) with organizationId sourced from the trusted withApi context, verified directly in the collection route, the [id] item route, and the analytics/governance-timeline/documents/publish/review/submit/summarize/index sub-routes. The lib/knowledge-transfer/** library references are downstream consumers of data already loaded through these org-scoped routes, not an independent write path. No Django model exists for this table.",
    supportingCapability: ["app/[locale]/dashboard/knowledge-transfer/[id]/page.tsx","app/api/exit-interviews/[id]/documents/route.ts","app/api/exit-interviews/[id]/governance/route.ts","app/api/exit-interviews/[id]/index/route.ts","app/api/exit-interviews/[id]/publish/route.ts","app/api/exit-interviews/[id]/review/route.ts","app/api/exit-interviews/[id]/route.ts","app/api/exit-interviews/[id]/submit/route.ts","app/api/exit-interviews/[id]/summarize/route.ts","app/api/exit-interviews/analytics/route.ts","app/api/exit-interviews/expertise-map/route.ts","app/api/exit-interviews/governance-timeline/route.ts","app/api/exit-interviews/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "federations",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/domains/federation/federation-schema.ts — \"Link to organizations table (type: 'federation')\"). Reachable via multiple live HTTP routes: app/api/federations/route.ts, app/api/federations/[id]/route.ts, app/api/federations/[id]/affiliates/route.ts, app/api/federations/[id]/dashboard/route.ts, app/api/federations/[id]/meetings/route.ts, app/api/federations/[id]/remittances/route.ts, app/api/onboarding/discover-federation/route.ts.",
    supportingCapability: ["app/api/federations/route.ts","app/api/federations/[id]/route.ts","app/api/federations/[id]/affiliates/route.ts","app/api/federations/[id]/dashboard/route.ts","app/api/federations/[id]/meetings/route.ts","app/api/federations/[id]/remittances/route.ts","app/api/onboarding/discover-federation/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
    {
    table: "fee_adjustments",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CORRECTED round 57 (same barrel-reachability miss as chargeback_statements/transaction_fee_rules): transaction-fee-engine.ts's reverseTransactionFee() — called from app/api/payments/webhooks/stripe/route.ts (a live, system-principal webhook) — inserts into feeAdjustments as part of a fee reversal. organization_id NOT NULL, parent FK to transactionFeeEvents.",
    supportingCapability: ["app/api/payments/webhooks/stripe/route.ts","services/platform-economics/transaction-fee-engine.ts"],
    requiredRuntimePrivileges: ["INSERT"],
    requiredSystemPrivileges: ["INSERT"],
    invocationAuthority: "WEBHOOK",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "field_notes",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'fieldNotes' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "field_organizer_activities",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'fieldOrganizerActivities' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "gdpr_data_requests",
    classification: "USER_RLS_REQUIRED",
    reason: "CLOSED round 57: organization_id AND user_id both NOT NULL. lib/gdpr/consent-manager.ts's GdprRequestManager.getUserRequests(userId, organizationId) (called from app/api/gdpr/data-export/route.ts) is self-scoped — a requester sees only their own privacy requests, not an org-wide admin listing (per doctrine: a tenant admin should not automatically read every privacy request). SECURITY DEFECT FOUND AND FIXED THIS ROUND (sibling cleanup): compliance.GdprDataRequestsViewSet was IsAuthenticated-only with no scoping at all — contained via DenyAllPermission.",
    supportingCapability: ["app/api/gdpr/data-export/route.ts","lib/gdpr/consent-manager.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "user_sessions",
    classification: "LATENT_UNREACHABLE",
    reason: "RECLASSIFIED 2026-09-02 (round 5, from USER_RLS_REQUIRED): re-verified this table's sole cited evidence, lib/workers/cleanup-worker.ts's scheduled session-cleanup DELETE, has ZERO real (non-test) importers anywhere in app/, actions/, lib/, or services/ — not even from lib/job-queue.ts, whose own doc comment confirms all of lib/workers/*.ts (report-worker, cleanup-worker, email-worker, notification-worker, sms-worker) was orphaned by the 2026-02-19 migration of background-job execution to Django Celery ('Background jobs are now executed by Django Celery workers ... All business logic now lives in the Python monolith'). No other non-test reference to the 'userSessions' Drizzle export exists in the union-eyes TS app. This does NOT change the table's actual DB-level protection: migration 0058 already enables RLS with per-user policies (sessions_select_own/insert_own/update_own/delete_own) independent of this manifest. Per this file's own LATENT_UNREACHABLE policy, if any future TS code starts querying this table again this entry must be revisited before shipping.",
    supportingCapability: ["db/schema/user-management-schema.ts","db/migrations/0058_world_class_rls_policies.sql"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "holidays",
    classification: "MIXED_GLOBAL_TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DERIVED_TENANT_DATA archetype): organization_id is nullable — a single column distinguishes shared/global statutory holidays (NULL) from tenant-specific override holidays (non-NULL), matching the MIXED_GLOBAL_TENANT_RLS_REQUIRED shape exactly. Reachable via app/api/labour-standards/[jurisdiction]/** routes (withApi()-backed) and db/queries/deadline-queries.ts. Reads need global-OR-own-org visibility; writes must not let a tenant author/mutate a global row.",
    supportingCapability: ["app/api/labour-standards/[jurisdiction]/compliance-check/route.ts","app/api/labour-standards/[jurisdiction]/route.ts","lib/ai/insights-generator.ts","lib/ai/role-templates.ts","lib/canadian-labour-standards/index.ts","lib/canadian-labour-standards/statutory-holidays.ts","lib/ml/models/workload-forecast-model.ts","lib/quebec/labour-law-engine.ts","services/financial-service/drizzle/0000_lucky_mole_man.sql","services/financial-service/drizzle/meta/0000_snapshot.json","services/financial-service/drizzle/schema.ts","services/financial-service/src/db/schema.ts","services/transfer-pricing-service.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: ["SELECT", "INSERT", "UPDATE"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "HIGH",
  },
  {
    table: "icra_assessments",
    classification: "SYSTEM_ONLY",
    reason: "ICRA_PSEUDONYMOUS_NO_LOGIN=APPROVED, ASSESSMENT_ID_AS_SOLE_BEARER_AUTHORITY=REJECTED (PR #752): the pseudonymous product model is intentional, but assessmentId alone must never authorize DB access. lib/icra/assessment-capability.ts adds a hashed, purpose-bound bearer capability; every route touching an existing assessment now verifies it before entering withSystemContext. icra_assessments.organization_id references icra_organizations (ICRA's own pseudonymous org-context table), NOT a Union Eyes tenant organization — it must not be classified TENANT_RLS_REQUIRED merely because an organization_id column exists. All application access goes through withSystemContext() (union_eyes_system), gated by: capability verification for the pseudonymous questionnaire/results flow (start/answer/submit/profile/results/report/checkout), auth()+single-use claimToken for the authenticated claim workflow, and a constant-time CRON_SECRET check for the internal report-review mutation. Verified 2026-09-01 (round 7, ICRA capability architecture): no route selects/mutates this table via the ordinary tenant runtime path.",
    supportingCapability: ["app/api/icra/start/route.ts","app/api/icra/submit/route.ts","app/api/icra/[assessmentId]/answer/route.ts","app/api/icra/[assessmentId]/submit/route.ts","app/api/icra/[assessmentId]/profile/route.ts","app/api/icra/[assessmentId]/claim/route.ts","app/api/icra/results/[id]/route.ts","app/api/icra/report/[assessmentId]/route.ts","app/api/icra/report/[assessmentId]/review/route.ts","app/api/icra/checkout/route.ts","app/api/icra/email-results/route.ts","lib/icra/assessment-capability.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "SYSTEM_SCHEDULE",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "icra_assessment_answers",
    classification: "SYSTEM_ONLY",
    reason: "ICRA_PSEUDONYMOUS_NO_LOGIN=APPROVED, ASSESSMENT_ID_AS_SOLE_BEARER_AUTHORITY=REJECTED (PR #752): the pseudonymous product model is intentional, but assessmentId alone must never authorize DB access. lib/icra/assessment-capability.ts adds a hashed, purpose-bound bearer capability; every route touching an existing assessment now verifies it before entering withSystemContext. Verified 2026-09-01 (round 7, ICRA capability architecture): only reached via withSystemContext() in the capability-gated answer/submit routes (per-question upsert, then read back to compute the profile). No direct organization_id column; scoped only through the parent icra_assessments row and its capability.",
    supportingCapability: ["app/api/icra/submit/route.ts","app/api/icra/[assessmentId]/answer/route.ts","app/api/icra/[assessmentId]/submit/route.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "SYSTEM_SCHEDULE",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "icra_operational_indicators",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 7, ICRA capability architecture): zero non-test references to 'icraOperationalIndicators' anywhere in app/, actions/, lib/, or services/. No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "icra_benchmark_groups",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 7, ICRA capability architecture): zero non-test references to 'icraBenchmarkGroups' anywhere in app/, actions/, lib/, or services/. No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "international_addresses",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: lib/address/address-service.ts's AddressService/PostalCodeValidator classes have ZERO real instantiations in app/, actions/ — dead TS. Personal/organization address data (organization_id NOT NULL, user_id nullable) — not global reference data despite the table name. SECURITY DEFECT FOUND AND FIXED THIS ROUND: auth_core.InternationalAddressesViewSet was IsAuthenticated-only with no organization filter — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["lib/address/address-service.ts","backend/auth_core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "journal_entries",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'journalEntries' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "knowledge_base",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. Reachable via app/api/{ai/summarize,exit-interviews/[id]/publish,governance/bylaws}/route.ts, all withApi()/withRoleAuth()-backed with server-derived organizationId (never a client body/query value). created_by is an audit-actor FK to profiles.",
    supportingCapability: ["app/api/ai/summarize/route.ts","app/api/exit-interviews/[id]/publish/route.ts","app/api/governance/bylaws/route.ts","app/api/knowledge-base/[id]/route.ts","app/api/knowledge-base/route.ts","lib/ai/chatbot-service.ts","lib/ai/template-engine.ts","lib/knowledge-transfer/indexing/semantic-indexer.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "kpi_configurations",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Verified 2026-09-01, corrected 2026-09-02 (round 5 — the prior citation app/api/kpi/configurations/route.ts does not exist; no app/api/kpi/* directory exists at all): direct NOT NULL organization_id column; real, verified non-test references are app/api/targets/route.ts (withApi minRole officer), app/api/analytics/kpis/route.ts (crudRoutes readRole member/writeRole steward), and actions/analytics-actions.ts — all tenant-scoped, plain `db` import, no withSystemContext.",
    supportingCapability: ["app/api/targets/route.ts","app/api/analytics/kpis/route.ts","actions/analytics-actions.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "legal_holds",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): sole TS reference is lib/services/policy-engine.ts, but its legal-hold helpers (checkLegalHold/canDelete) have zero production call sites; the only live policy-engine route calls evaluate(), which does not read legalHolds. No raw-SQL reference exists. The generated LegalHoldsViewSet in backend/compliance/views.py is router-registered but now uses unconditional DenyAllPermission, so the only reachable surface grants no authority and no legitimate consumer exists.",
    supportingCapability: ["lib/services/policy-engine.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "meeting_rooms",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, CALENDAR_AND_SCHEDULING family): db/schema/domains/scheduling/calendar.ts's meetingRooms has organizationId NOT NULL (FK organizations, cascade) with no user/parent column of any kind — direct org-owned resource, not a parent-owned or shared-with-user-override case (unlike calendars). BOTH live routes (app/api/meeting-rooms/route.ts collection, app/api/meeting-rooms/[id]/bookings/route.ts item — the folder name is misleading, it is a standard crud-factory item route on meetingRooms itself, not a separate bookings sub-resource) use crudRoutes with orgScoped:true and no ownerColumn, which is the correct minimal shape for a table with no per-row user dimension. CHILD-TABLE INJECTION CHECK (round-53 doctrine section 21): room_bookings.roomId FKs to meetingRooms.id and could in principle let an org-scoped booking reference another organization's room — but git-grep confirms roomBookings has ZERO real production callers anywhere in this app (no route/action/service references it) — dead code, no live cross-tenant injection path exists today. SECURITY DEFECTS FOUND AND FIXED (Django, not TS): backend/unions/views.py's MeetingRoomsViewSet and RoomBookingsViewSet were both IsAuthenticated-only with no organization filter at all (any authenticated user of any organization could list/read/update/delete any organization's meeting rooms and room bookings) — no legitimate TS/frontend consumer of either Django REST path exists (git-grep confirmed). Both contained via DenyAllPermission (backend/unions/tests_round53_calendar_scheduling_containment.py). DUAL-SCHEMA FINDING (round-48 precedent, not re-litigated): services/financial-service re-declares meetingRooms in its own schema — independently verified as a genuine SEPARATE_DATABASE_BOUNDARY via that service's own package.json/drizzle.config.ts/DATABASE_URL (same finding as calendars in this same round).",
    supportingCapability: ["app/api/meeting-rooms/[id]/bookings/route.ts","app/api/meeting-rooms/route.ts","backend/unions/views.py","backend/unions/tests_round53_calendar_scheduling_containment.py"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "mfa_configurations",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'mfaConfigurations' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "mobile_app_config",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'mobileAppConfig' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "mobile_devices",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): direct organization_id column (nullable — pseudonymous/pre-org device registration is a legitimate design, matching this app's other bearer/claim-token products). Reachable via app/api/mobile/sync/route.ts (auth required, minRole member); GET filters userId AND organizationId from the trusted context; POST stamps both on insert. SECURITY DEFECT FOUND AND FIXED: deviceId has a database-level global unique constraint, but the POST handler's onConflictDoUpdate(target: deviceId) previously had no check that a pre-existing row for that deviceId belonged to the caller — any authenticated user who knew (or guessed) another user's deviceId could silently overwrite that row's deviceToken, hijacking their push notification channel while userId/organizationId on the row stayed unchanged. Fixed by adding an ownership check (assertDeviceNotOwnedByAnotherUser) before the upsert, returning 409 Conflict for a mismatched owner; 3 new regression tests in app/api/mobile/sync/__tests__/route.test.ts (6/6 tests pass across both mobile route test files). MobileDevicesViewSet (Django, notifications app) was separately router-registered (IsAuthenticated-only, unscoped) and is now unconditionally denied via the existing DenyAllPermission already used for MobileAnalyticsViewSet (round 48) in the same file, because it has no legitimate Django consumer.",
    supportingCapability: ["app/api/mobile/sync/route.ts","app/api/mobile/sync/__tests__/route.test.ts","lib/mobile/mobile-engine.ts","backend/notifications/views.py","backend/notifications/tests_round50_mobile_devices_containment.py"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "negotiations",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/bargaining-negotiations-schema.ts). Reachable via live HTTP routes app/api/negotiations/route.ts, app/api/bargaining/negotiations/route.ts, app/api/bargaining/negotiations/[id]/route.ts.",
    supportingCapability: ["app/api/negotiations/route.ts","app/api/bargaining/negotiations/route.ts","app/api/bargaining/negotiations/[id]/route.ts","lib/services/negotiations-service.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "nlrb_clrb_filings",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'nlrbClrbFilings' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "organizer_impacts",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'organizerImpacts' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "organizer_tasks",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 41 (DIRECT_ORG_RUNTIME_BATCH), with a fix applied first: direct NOT NULL organization_id column (domains/communications/organizer-workflows.ts). app/api/organizer/impact/route.ts is a crud-factory collection route (orgScoped:true, readRole:'member', writeRole:'steward') — SELECT/INSERT only, no item route exists so UPDATE/DELETE are not reachable. DEFECT FOUND AND FIXED this round: OrganizerTasksViewSet (backend/unions/views.py) was router-registered with queryset=Model.objects.all() + IsAuthenticated-only (no organization filter at all, not even a client-controlled one) — zero real frontend consumer found — contained via DenyAllPermission.",
    supportingCapability: ["app/api/organizer/impact/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "organizing_contacts",
    classification: "LATENT_UNREACHABLE",
    reason: "CORRECTED 2026-09-01 (round 7): the round-6 entry was itself a false classification — its verification grep only matched single-line `pgTable('organizing_contacts'` declarations and missed the real, multi-line `export const organizingContacts = pgTable(\n  'organizing_contacts',\n  {...})` declarations at db/schema/domains/infrastructure/organizing.ts and db/schema/organizing-tools-schema.ts (both carry their own organization_id-indexed columns). A repo-wide, multi-line-aware search (scripts/rls-storage-reachability-audit.ts) confirms these ARE real union-eyes-native declarations, but zero non-test references to 'organizingContacts' exist anywhere in app/, actions/, lib/, or services/ (excluding financial-service, which separately declares a same-named table in its own database). Correct disposition is therefore LATENT_UNREACHABLE, not SEPARATE_DATABASE_BOUNDARY — the union-eyes table exists and is part of this schema's remit, it is simply unused.",
    supportingCapability: ["db/schema/domains/infrastructure/organizing.ts","db/schema/organizing-tools-schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "outreach_enrollments",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'outreachEnrollments' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "outreach_sequences",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'outreachSequences' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "outreach_steps_log",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'outreachStepsLog' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pack_download_log",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'packDownloadLog' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pay_equity_exercises",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 41 (DIRECT_ORG_RUNTIME_BATCH): direct NOT NULL organization_id column (domains/health-safety/cnesst-schema.ts). Both app/api/cnesst/pay-equity/route.ts (collection) and app/api/cnesst/pay-equity/[id]/route.ts (item) are crud-factory routes (orgScoped:true, readRole:'steward', writeRole:'officer') — the factory forces the caller's own organizationId on every SELECT/INSERT/UPDATE/DELETE; verified directly against crud-factory.ts. No Django model exists for this table.",
    supportingCapability: ["app/api/cnesst/pay-equity/[id]/route.ts","app/api/cnesst/pay-equity/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "pci_dss_cardholder_data_flow",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'pciDssCardholderDataFlow' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pci_dss_encryption_keys",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): sole TS reference is lib/services/pci-compliance-service.ts, which has zero production importers; no raw-SQL reference exists. The generated PciDssEncryptionKeysViewSet in backend/compliance/views.py is router-registered but now uses unconditional DenyAllPermission, so the only reachable surface grants no authority and no legitimate consumer exists. This does not create PCI runtime authority; if the PCI service is wired later, re-open before shipping.",
    supportingCapability: ["lib/services/pci-compliance-service.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pci_dss_quarterly_scans",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): sole TS reference is lib/services/pci-compliance-service.ts, which has zero production importers; no raw-SQL reference exists. The generated PciDssQuarterlyScansViewSet in backend/compliance/views.py is router-registered but now uses unconditional DenyAllPermission, so the only reachable surface grants no authority and no legitimate consumer exists. This does not create PCI runtime authority; if the PCI service is wired later, re-open before shipping.",
    supportingCapability: ["lib/services/pci-compliance-service.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pci_dss_requirements",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): sole TS reference is lib/services/pci-compliance-service.ts, which has zero production importers; no raw-SQL reference exists. The generated PciDssRequirementsViewSet in backend/compliance/views.py is router-registered but now uses unconditional DenyAllPermission, so the only reachable surface grants no authority and no legitimate consumer exists. This does not create PCI runtime authority; if the PCI service is wired later, re-open before shipping.",
    supportingCapability: ["lib/services/pci-compliance-service.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pci_dss_saq_assessments",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): sole TS reference is lib/services/pci-compliance-service.ts, which has zero production importers; no raw-SQL reference exists. The generated PciDssSaqAssessmentsViewSet in backend/compliance/views.py is router-registered but now uses unconditional DenyAllPermission, so the only reachable surface grants no authority and no legitimate consumer exists. This does not create PCI runtime authority; if the PCI service is wired later, re-open before shipping.",
    supportingCapability: ["lib/services/pci-compliance-service.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pilot_checklist_items",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, PILOT_CONTROL_PLANE_STORAGE family; PILOT CONTROL-PLANE PRODUCT DOCTRINE UNCHANGED — storage authority only, no pilot business/readiness doctrine touched): db/schema/domains/pilot/pilot-onboarding.ts's pilotChecklistItems has organizationId NOT NULL, no user column — direct org-owned. TWO live writers, both properly scoped: app/api/pilot/onboarding/route.ts (GET/PATCH, withOrganizationAuth + hasMinRole('officer'), organizationId always server-derived from auth context, itemId validated against a fixed whitelist, PATCH write wrapped in withRLSContext i.e. TENANT_RUNTIME with app.current_user_id set) and app/api/pilot/bootstrap/cupe/route.ts (POST, minRole:'admin', a one-click demo-org bootstrap script whose target orgId is resolved server-side from a fixed fixtures/cupe/pilot-org/cupe-pilot-setup.json slug lookup, never client-supplied — no cross-tenant IDOR). No Django ViewSet exists for this table (no containment needed).",
    supportingCapability: ["app/api/pilot/bootstrap/cupe/route.ts","app/api/pilot/onboarding/route.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "pilot_demo_seeds",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 41 (DIRECT_ORG_RUNTIME_BATCH): direct NOT NULL organization_id column (domains/pilot/pilot-demo-seeds.ts). Both app/api/pilot/demo-data/route.ts (POST/DELETE, withOrganizationAuth) and app/api/pilot/onboarding/route.ts (GET, withOrganizationAuth) derive organizationId server-side (getOrganizationIdForUser) and consistently filter eq(pilotDemoSeeds.organizationId, organizationId) — this table generates/removes demo data INTO the caller's OWN organization only; distinct from the pilot_checklist_items 'bootstrap/cupe' route, which provisions a fixed non-caller pilot org and was excluded from this batch. No Django model exists for this table.",
    supportingCapability: ["app/api/pilot/demo-data/route.ts","app/api/pilot/onboarding/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "pilot_enrollments",
    classification: "MIXED_GLOBAL_TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, PILOT_CONTROL_PLANE_STORAGE family; PILOT CONTROL-PLANE PRODUCT DOCTRINE UNCHANGED): db/schema/domains/pilot/pilot-enrollments.ts's pilotEnrollments has organizationId NOT NULL with a UNIQUE index (one enrollment row per org) — org-level platform-governance data (adoption/engagement/health-score metrics), not per-member. GENUINELY MIXED SCOPE, not simple direct-org (round-53 doctrine section 55 — an org column alone is insufficient): app/api/pilot/current/route.ts is a TENANT_USER path (withOrganizationAuth + hasMinRole('officer')) reading ONLY the caller's own org's enrollment row (eq(pilotEnrollments.organizationId, organizationId), executed inside withSystemContext i.e. SYSTEM_RUNTIME despite the tenant-scoped caller — an intentional platform-governed-data read pattern, not a principal-mismatch defect since the WHERE predicate already constrains the row). app/api/pilot/overview/route.ts is a genuine cross-org PLATFORM_ADMIN path (WHERE status='active', no organizationId filter, joining ALL enrolled orgs) explicitly gated by hasMinRole('officer') AND (caller's own org === the platform's own DEFAULT_ORGANIZATION_ID OR caller's userId is in the PLATFORM_ADMIN_USER_IDS allowlist) — matches the round-48 customer_nps_surveys SYSTEM_ONLY precedent's shape but this table ALSO has a genuine same-org tenant read path, hence MIXED rather than pure SYSTEM_ONLY. Genuinely SELECT-only from the TS app today: git-grep across app/, actions/, lib/, services/ finds zero INSERT/UPDATE/DELETE against pilotEnrollments anywhere — enrollment rows are created out-of-band (pilot ops/migration), consistent with 'the pilot team legitimately manages tenant enrollments' (round-53 doctrine section 31/34). No Django ViewSet exists for this table.",
    supportingCapability: ["app/api/pilot/current/route.ts","app/api/pilot/overview/route.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
    {
    table: "pilot_events",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 57: organization_id AND user_id both NOT NULL. Genuinely reachable via numerous real routes (app/api/grievances/**, app/api/cases/**, app/api/pilot/**, app/api/cron/sla-watchdog/route.ts) through lib/services/pilot-tracking.ts/pilot-metrics.ts — an org-scoped pilot-programme usage/analytics event stream (org admins may view aggregate pilot metrics for their own org), not a self-only private record.",
    supportingCapability: ["app/api/pilot/events/route.ts","app/api/cron/sla-watchdog/route.ts","lib/services/pilot-tracking.ts","lib/services/pilot-metrics.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "pilot_feedback",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (TENANT_PLUS_SUBJECT archetype): organization_id AND user_id both NOT NULL. SECURITY DEFECT FOUND AND FIXED this round: app/api/pilot/feedback/route.ts previously destructured `userId`/`organizationId` directly from the client-supplied POST body (arbitrary cross-user/cross-org impersonation) and read `organizationId` from an unauthenticated-caller-controlled `?organizationId=` query param on GET (cross-org IDOR reading another organization's feedback summary). Fixed to derive both from the authenticated withRoleAuth() context (BaseAuthContext.userId/organizationId, which the wrapper already resolves via getOrganizationIdForUser() but the route was not consuming) — see app/api/pilot/__tests__/pilot-feedback-idor-fix.route.test.ts for the regression coverage (4 tests).",
    supportingCapability: ["app/api/pilot/feedback/route.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "pilot_milestones",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, PILOT_CONTROL_PLANE_STORAGE family; PILOT CONTROL-PLANE PRODUCT DOCTRINE UNCHANGED): db/schema/domains/pilot/pilot-milestones.ts's pilotMilestones has organizationId NOT NULL, no user column — direct org-owned. UNLIKE pilot_enrollments, every query against THIS table in both live routes carries an explicit organizationId predicate — app/api/pilot/overview/route.ts's cross-org fan-out happens at the pilotEnrollments level (an unscoped WHERE status='active' query, gated PLATFORM_ADMIN — see that entry), then loops per already-authorized enrollment.organizationId to fetch that SPECIFIC org's milestones (eq(pilotMilestones.organizationId, orgId), verified by direct read of the route — never a single unscoped query against this table itself). app/api/pilot/current/route.ts (TENANT_USER, withOrganizationAuth + hasMinRole('officer')) reads only the caller's own org's milestones the same way. Both execute inside withSystemContext (SYSTEM_RUNTIME). invocationAuthority recorded as MIXED (TENANT_USER via /current, PLATFORM_ADMIN via /overview) even though the table's own authority boundary (organizationId) is always enforced at the SQL predicate level in every query. Genuinely SELECT-only from the TS app today: git-grep confirms zero INSERT/UPDATE/DELETE against pilotMilestones anywhere — milestone rows are created out-of-band (pilot ops/migration). No Django ViewSet exists for this table.",
    supportingCapability: ["app/api/pilot/current/route.ts","app/api/pilot/overview/route.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "policy_rules",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 41 (DIRECT_ORG_RUNTIME_BATCH), resolving the round-40 exception: direct NOT NULL organization_id column (policy-engine-schema.ts). DEFECT FOUND AND FIXED this round — exactly the gap round 40 flagged: PolicyEngine.evaluate() read active policyRules by ruleType/category/status/enforced with NO organizationId predicate at all, and app/api/governance/policies/evaluate/route.ts's handler did not even destructure organizationId from its trusted withApi context — any admin from any organization could evaluate against, and see the matched rule ids of, every other organization's active policy rules. Fixed: evaluate() now takes organizationId as its first parameter and filters eq(policyRules.organizationId, organizationId); the route now requires and passes the trusted organizationId. Sole caller (this route) and its test suite (lib/services/__tests__/policy-engine.test.ts) updated; no INSERT/UPDATE/DELETE route exists for this table. The generated PolicyRulesViewSet in backend/compliance/views.py remains unconditionally denied via DenyAllPermission.",
    supportingCapability: ["app/api/governance/policies/evaluate/route.ts","lib/services/policy-engine.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "poll_votes",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. app/api/communications/polls/[pollId]/vote/route.ts is crudRoutes()-backed (orgScoped:true, writeRole:steward, server-derived organizationId). poll_id is a parent FK to polls. KNOWN RESIDUAL FINDING (not blocking, documented for follow-up): the generic CRUD factory does not cross-validate that the client-supplied poll_id belongs to the caller's own organization before inserting a vote row, so a steward could in principle record a vote against another organization's poll_id (a poll-results data-integrity issue, not a cross-tenant confidentiality breach — the vote row itself remains correctly organization_id-scoped to the caller). Recommended follow-up: add a beforeCreate hook validating poll_id's organization_id matches the caller.",
    supportingCapability: ["app/api/communications/polls/[pollId]/vote/route.ts","lib/engagement-scoring.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "polls",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. app/api/communications/polls/route.ts + [pollId]/route.ts are crudRoutes()-backed (orgScoped:true, server-derived organizationId). created_by is an audit-actor FK to profiles.",
    supportingCapability: ["app/api/communications/polls/[pollId]/route.ts","app/api/communications/polls/route.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "preventive_withdrawals",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/domains/health-safety/cnesst-schema.ts). Reachable via live HTTP routes app/api/cnesst/preventive-withdrawals/route.ts, app/api/cnesst/preventive-withdrawals/[id]/route.ts.",
    supportingCapability: ["app/api/cnesst/preventive-withdrawals/route.ts","app/api/cnesst/preventive-withdrawals/[id]/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "program_enrollments",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 6, Phase-3A priority tranche + reachability-scan correction): programEnrollments has a real, non-duplicate union-eyes-native pgTable('program_enrollments', ...) declaration (db/schema/domains/scheduling/training.ts, db/schema/education-training-schema.ts), but zero non-test references to it exist anywhere in app/, actions/, lib/, or services/ within apps/union-eyes proper. The original scan's supportingCapability list for this table only surfaced services/financial-service's own same-named, separately-databased table (its own drizzle.config.ts/DATABASE_URL) and MISSED the real union-eyes declaration entirely — this entry corrects that gap. No known union-eyes application code path queries this table today.",
    supportingCapability: ["db/schema/domains/scheduling/training.ts","db/schema/education-training-schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "public_content",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: the sole TS reference, lib/examples/static-generation-patterns.ts, is an explicitly-documented Next.js pattern EXAMPLE file (`@NOTE: This is an example file demonstrating Next.js patterns`) with zero real importers — dead/demo code, not production. SECURITY DEFECT FOUND AND FIXED THIS ROUND: content.PublicContentViewSet was IsAuthenticated-only with no organization filter despite organization_id being nullable (mixed tenant/global row shapes) — contained via DenyAllPermission pending a real tenant/global-aware get_queryset(). No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["lib/examples/static-generation-patterns.ts","backend/content/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "public_events",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'publicEvents' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "push_devices",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 46 (SYSTEM_AND_MIXED_EXECUTION_AUTHORITY cohort — corrects the Round 45 MIXED_ROOT ejection): organization_id AND profile_id (user) both NOT NULL. Round 45 ejected this table citing lib/workers/notification-worker.ts (a BullMQ `new Worker(...)` instance) as a real system read path. Round 46 traced that claim to its root per doctrine and found it does NOT hold: `notificationWorker` and its sole dependency `services/fcm-service.ts` (FCMService, sendToDevice/sendToDevices/sendToProfile/etc.) have ZERO importers anywhere in the codebase outside their own module and their own test files — no bootstrap script, deployment process, or queue-producer wiring instantiates this worker. This is the same dead-code-behind-a-`workers/`-path pattern found this round for employer_execution_* and foreign_workers — a worker-named file/class existing is not proof of a real system caller (doctrine section 5). The ONLY real reachable path is TENANT: app/api/mobile/devices/route.ts (crudRoutes, orgScoped:true, real organization_id column). SECURITY DEFECT FOUND AND FIXED this round (independent of the MIXED/system question): org scoping alone let any org member (readRole:'member') list every other member's registered device via GET, and the crud-factory's create-time invariant enforcement only auto-forces organizationId/createdBy, not arbitrary ownerColumn values — so POST could register a device under an arbitrary profileId in the request body. Fixed: added `ownerColumn: 'profileId'` (restricts GET to the caller's own devices) and a `beforeCreate` hook forcing profileId to the authenticated caller regardless of body content, extracted into the named/tested `buildPushDeviceCreateValues()` helper. New test: app/api/mobile/devices/__tests__/route.test.ts (3/3 passing). Django `PushDevicesViewSet` contained via DenyAllPermission (was IsAuthenticated-only, no scoping at all). Dead code (lib/workers/notification-worker.ts, services/fcm-service.ts) left in place, not deleted — out of scope for this classification round; flagged for a future cleanup round.",
    supportingCapability: ["app/api/mobile/devices/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "recognition_award_types",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority — same archetype as the round-40 recognition_programs closure): direct NOT NULL org_id column. Live read path is app/[locale]/dashboard/admin/rewards/analytics/page.tsx (requireUser() + hasMinRole('admin'), orgId from the authenticated user, query runs inside withRLSContext, filtered ra.org_id = orgId via the recognitionAwards join). Live write/read path is app/api/rewards/cron/route.ts (CRON_SECRET timing-safe gated system job) via lib/services/rewards/automation-service.ts. SECURITY DEFECT FOUND AND FIXED: processAnniversaryAwards() looked up the 'Work Anniversary' award type by name+kind alone with NO org_id filter, so a cron run for one organization could pick up ANOTHER organization's matching award-type row (cross-tenant data confusion in a system-invoked job, using its programId/credit rules/approval settings to issue awards to the wrong org's members) — fixed by adding an org_id condition to the lookup; 1 new regression test in lib/services/rewards/__tests__/automation-service.test.ts (15/15 tests pass). RecognitionAwardTypesViewSet (Django, unions app) was separately router-registered (IsAuthenticated-only, unscoped) and is now unconditionally denied via the existing DenyAllPermission already used for RecognitionProgramsViewSet in the same file, because it has no legitimate Django consumer.",
    supportingCapability: ["app/[locale]/dashboard/admin/rewards/analytics/page.tsx","app/api/rewards/cron/route.ts","lib/services/rewards/automation-service.ts","lib/services/rewards/award-service.ts","lib/services/rewards/program-service.ts","lib/services/rewards/__tests__/automation-service.test.ts","backend/unions/views.py","backend/unions/tests_round50_recognition_awards_containment.py"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "NONE",
  },
  {
    table: "recognition_awards",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority — same archetype as the round-40 recognition_programs closure): direct NOT NULL org_id column. Live read path is app/[locale]/dashboard/admin/rewards/analytics/page.tsx (requireUser() + hasMinRole('admin'), orgId from the authenticated user, filtered ra.org_id = orgId inside withRLSContext). Live write path is lib/services/rewards/award-service.ts's createAwardRequest/issueAward (orgId passed explicitly by every caller, including the now-fixed app/api/rewards/cron/route.ts system job — see recognition_award_types entry for the cross-tenant defect found and fixed in that job this round). STATE MACHINE: award status pending -> issued (issueAward), with requiresApproval gating auto-issue. RecognitionAwardsViewSet (Django, unions app) was separately router-registered (IsAuthenticated-only, unscoped) and is now unconditionally denied via the existing DenyAllPermission already used for RecognitionProgramsViewSet in the same file, because it has no legitimate Django consumer.",
    supportingCapability: ["app/[locale]/dashboard/admin/rewards/analytics/page.tsx","app/api/rewards/cron/route.ts","lib/services/rewards/automation-service.ts","lib/services/rewards/award-service.ts","lib/services/rewards/export-service.ts","lib/services/rewards/notification-service.ts","backend/unions/views.py","backend/unions/tests_round50_recognition_awards_containment.py"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: ["SELECT","INSERT","UPDATE"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "NONE",
  },
  {
    table: "recognition_programs",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): direct NOT NULL org_id table. Live path is app/[locale]/dashboard/admin/rewards/analytics/page.tsx, which requires requireUser(), hasMinRole('admin'), derives orgId from the authenticated user, and runs the analytics SQL inside withRLSContext; recognitionPrograms is only joined for budget display on rows already filtered by rbe.org_id = orgId. lib/services/rewards/program-service.ts has SELECT/INSERT/UPDATE helpers but no production caller today, so it does not expand the live DML set. RecognitionProgramsViewSet is separately router-registered but now unconditionally denied via DenyAllPermission because it has no legitimate Django consumer. Minimum proven runtime DML is SELECT only, no withSystemContext.",
    supportingCapability: ["app/[locale]/dashboard/admin/rewards/analytics/page.tsx","lib/services/rewards/program-service.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "report_delivery_history",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'reportDeliveryHistory' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "report_executions",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 6, Phase-3A priority tranche + reachability-scan correction): reportExecutions has a real, non-duplicate union-eyes-native pgTable('report_executions', ...) declaration (db/schema/domains/analytics/reports.ts, db/schema/reports-schema.ts), but zero non-test references to it exist anywhere in app/, actions/, lib/, or services/ within apps/union-eyes proper. The original scan's supportingCapability list for this table only surfaced services/financial-service's own same-named, separately-databased table (its own drizzle.config.ts/DATABASE_URL) and MISSED the real union-eyes declaration entirely — this entry corrects that gap. No known union-eyes application code path queries this table today.",
    supportingCapability: ["db/schema/domains/analytics/reports.ts","db/schema/reports-schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "report_shares",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 6, Phase-3A priority tranche + reachability-scan correction): reportShares has a real, non-duplicate union-eyes-native pgTable('report_shares', ...) declaration (db/schema/domains/analytics/reports.ts, db/schema/reports-schema.ts), but zero non-test references to it exist anywhere in app/, actions/, lib/, or services/ within apps/union-eyes proper. The original scan's supportingCapability list for this table only surfaced services/financial-service's own same-named, separately-databased table (its own drizzle.config.ts/DATABASE_URL) and MISSED the real union-eyes declaration entirely — this entry corrects that gap. No known union-eyes application code path queries this table today.",
    supportingCapability: ["db/schema/domains/analytics/reports.ts","db/schema/reports-schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "reports",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, REPORTING family): the original 113-reference count is almost entirely a false-positive-heavy generic-word scan match (the literal English word \"reports\" appearing in unrelated page paths like dashboard/admin/dues/reports/page.tsx, doc files, and completely unrelated financial/health-safety/export routes) — re-derived reachability from scratch via a precise `import { reports } from '@/db/schema'` grep rather than trusting the raw hint list (round-53 doctrine section 35: do not assume from a report's file-path naming). db/schema/domains/analytics/reports.ts's reports has organizationId NOT NULL (FK organizations), createdBy (NOT NULL, matches crud-factory's special-cased auto-set column name — no spoofing gap here, unlike calendars.ownerId/course_registrations.memberId), and isPublic (boolean) — confirmed NEVER read/enforced anywhere in the app (git-grep for `isPublic` across every reports route finds zero references): it is a vestigial schema column with no live authorization behavior today, so no per-creator/private-report restriction actually exists to violate; every org member with 'member'+ role can read all their org's report DEFINITIONS (config/name/type/category), 'steward'+ can write — this IS the observed real behavior, not an invented stricter one. EXACTLY 6 real importers of the reports Drizzle export, ALL org-scoped crudRoutes with no owner column: app/api/reports/route.ts and [id]/route.ts (the canonical collection/item pair) plus FOUR mislabeled scaffolding duplicates — [id]/execute/route.ts, [id]/run/route.ts, [id]/share/route.ts, and builder/route.ts — which despite their URL paths implying report-execution/sharing logic are byte-for-byte identical crudRoutes configs on the SAME reports table (the same recurring scaffolding-tool table/path mismatch seen elsewhere this round in meeting-rooms/[id]/bookings and the education family). DERIVED-DATA LINEAGE (round-53 doctrine section 36, round-48 precedent reused): reports.config (JSONB describing data sources/fields/filters) is a report DEFINITION, not a stored output/artifact — the genuine ad-hoc report-EXECUTION engine (app/api/reports/execute/route.ts, ReportExecutor, dynamically builds and runs SQL from a client-supplied config) is officer-gated and organizationId-scoped from auth context, but operates on an UNSAVED ad-hoc config parameter and never reads a saved `reports` row at all — confirmed no live code path ever loads a saved report's `config` and feeds it into ReportExecutor (report_executions, the would-be output-storage table, was independently closed LATENT_UNREACHABLE in round 6 with zero non-test TS references, corroborating that no execution-of-a-saved-report flow exists today). Auditing ReportExecutor's own SQL-generation injection-safety is a distinct, deeper security surface outside this round's table-storage-authority scope (round-53 doctrine section 62: do not expand into full product redesign) — flagged for a dedicated future review, not a defect against the reports table's own row-level authority. Django backend/analytics/views.py's ReportsViewSet was IsAuthenticated-only with no organization filter at all and no legitimate TS/frontend consumer of the Django REST path exists — contained via DenyAllPermission (backend/analytics/tests_round53_reports_containment.py).",
    supportingCapability: ["app/api/reports/route.ts","app/api/reports/[id]/route.ts","app/api/reports/[id]/execute/route.ts","app/api/reports/[id]/run/route.ts","app/api/reports/[id]/share/route.ts","app/api/reports/builder/route.ts","app/api/reports/execute/route.ts","backend/analytics/views.py","backend/analytics/tests_round53_reports_containment.py"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "retention_policies",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (2026-09-04, round 40 SIMPLE_TENANT:org:NORMAL cohort): sole TS reference is lib/services/policy-engine.ts, but its retention helpers (getRetentionPolicy/canDelete) have zero production call sites; the only live policy-engine route calls evaluate(), which does not read retentionPolicies. No raw-SQL reference exists. The generated RetentionPoliciesViewSet in backend/compliance/views.py is router-registered but now uses unconditional DenyAllPermission, so the only reachable surface grants no authority and no legitimate consumer exists.",
    supportingCapability: ["lib/services/policy-engine.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "reward_redemptions",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 57: org_id AND user_id both NOT NULL, parent FK to recognitionPrograms. Genuinely reachable via app/[locale]/dashboard/admin/rewards/analytics/page.tsx (requireUser + withRLSContext, admin analytics) and lib/services/rewards/redemption-service.ts (member-initiated redemption, self+org scoped). Org-wide read is legitimate for the admin analytics surface; write paths remain self-scoped.",
    supportingCapability: ["app/[locale]/dashboard/admin/rewards/analytics/page.tsx","lib/services/rewards/redemption-service.ts","backend/unions/views.py"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "right_of_refusal_events",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/domains/health-safety/cnesst-schema.ts). Reachable via live HTTP routes app/api/cnesst/right-of-refusal/route.ts, app/api/cnesst/right-of-refusal/[id]/route.ts.",
    supportingCapability: ["app/api/cnesst/right-of-refusal/route.ts","app/api/cnesst/right-of-refusal/[id]/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "room_bookings",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 6, Phase-3A priority tranche + reachability-scan correction): roomBookings has a real, non-duplicate union-eyes-native pgTable('room_bookings', ...) declaration (db/schema/domains/scheduling/calendar.ts, db/schema/calendar-schema.ts), but zero non-test references to it exist anywhere in app/, actions/, lib/, or services/ within apps/union-eyes proper. The original scan's supportingCapability list for this table only surfaced services/financial-service's own same-named, separately-databased table (its own drizzle.config.ts/DATABASE_URL) and MISSED the real union-eyes declaration entirely — this entry corrects that gap. No known union-eyes application code path queries this table today.",
    supportingCapability: ["db/schema/domains/scheduling/calendar.ts","db/schema/calendar-schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "satisfaction_surveys",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 57: organization_id NOT NULL, parent FK to claims. Genuinely reachable via numerous app/api/satisfaction/** routes (lib/services/satisfaction-service.ts).",
    supportingCapability: ["app/api/satisfaction/route.ts","app/api/satisfaction/[id]/route.ts","lib/services/satisfaction-service.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "scheduled_reports",
    classification: "LATENT_UNREACHABLE",
    reason: "Verified 2026-09-01 (round 6, Phase-3A priority tranche + reachability-scan correction): scheduledReports has a real, non-duplicate union-eyes-native pgTable('scheduled_reports', ...) declaration (db/schema/domains/analytics/reports.ts, db/schema/reports-schema.ts), but zero non-test references to it exist anywhere in app/, actions/, lib/, or services/ within apps/union-eyes proper. The original scan's supportingCapability list for this table only surfaced services/financial-service's own same-named, separately-databased table (its own drizzle.config.ts/DATABASE_URL) and MISSED the real union-eyes declaration entirely — this entry corrects that gap. No known union-eyes application code path queries this table today.",
    supportingCapability: ["db/schema/domains/analytics/reports.ts","db/schema/reports-schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "scim_configurations",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'scimConfigurations' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "scim_events_log",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'scimEventsLog' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "security_events",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 49, immutable security and audit evidence authority cohort): direct organization_id column, read via app/[locale]/dashboard/security/page.tsx (privileged gate: requireUser() + hasMinRole('security_manager') — a genuine security-operations role per lib/api-auth-guard.ts's ROLE_HIERARCHY, not an ordinary org admin/member — redirects non-privileged users away; reads run under withSystemContext scoped to the caller's OWN organizationId, never cross-org). SEVERE PRIVILEGED-READER-AUTHORITY DEFECT FOUND AND FIXED: app/api/security/events/route.ts (a generic crudRoutes({orgScoped:true}) collection route) had readRole:'member' and writeRole:'steward' — far more permissive than the dashboard's own security_manager gate. Any ordinary org 'member' could call this API directly to read every security event for their organization (bypassing the intended privileged-reader restriction entirely), and any 'steward' could POST arbitrary self-authored security event records (attacker-controlled severity/type/description — a fabricated-evidence risk). Fixed: both readRole and writeRole tightened to 'security_manager', matching the dashboard's own gate. DJANGO SIDE: core/views.py's SecurityEventsViewSet (IsAuthenticated-only, unscoped, filterable by organization_id/user_id — letting any authenticated user of any org read/write/delete any org's security events) provided a second, worse bypass; contained via DenyAllPermission (core/isolation.py's DenyAllPermission, already used elsewhere in that app).",
    supportingCapability: ["app/[locale]/dashboard/security/page.tsx","app/api/security/events/route.ts","app/api/__tests__/crud-routes-coverage.test.ts","backend/core/views.py"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "security_posture_checks",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 49, immutable security and audit evidence authority cohort): direct organization_id column, read-only via the SAME privileged app/[locale]/dashboard/security/page.tsx gate as security_events (requireUser() + hasMinRole('security_manager'), withSystemContext, scoped to the caller's own organizationId). No in-app writer exists anywhere (neither TS nor Django) — posture-check rows must originate from an external monitoring/scanning process writing directly to the database, outside this application's request-handling surface; this is a read-only consumption point, not a defect. No Django exposure exists for this table (verified: zero references anywhere in backend/ outside migrations).",
    supportingCapability: ["app/[locale]/dashboard/security/page.tsx"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "segment_exports",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'segmentExports' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "shopify_config",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 46 (SYSTEM_AND_MIXED_EXECUTION_AUTHORITY cohort, SYSTEM_WORKER:org:NORMAL cohort lane, credential-sensitive): orgId is the PRIMARY KEY (one config row per org) storing SECRET REFERENCES, not raw secrets — storefrontTokenSecretRef/adminTokenSecretRef/webhookSecretRef are meant to be resolved via a separate secrets manager (db/schema/recognition-rewards-schema.ts / db/schema/domains/infrastructure/rewards.ts, two identical declarations, not investigated further this round). Traced every reference: lib/services/rewards/shopify-service.ts's fetchCuratedCollections(orgId) is the ONLY code that queries this table (org-scoped correctly via eq(shopifyConfig.orgId, orgId)), and it only ever reads the non-sensitive allowedCollections column — but fetchCuratedCollections itself has ZERO callers anywhere outside its own test file (lib/services/rewards/__tests__/shopify-service.test.ts), confirmed dead code. lib/services/rewards/webhook-service.ts's getWebhookSecret() has a commented-out shopifyConfig query (documented future intent, never implemented) and instead returns a single global process.env.SHOPIFY_WEBHOOK_SECRET — also dead code (zero external callers). app/[locale]/dashboard/admin/rewards/shopify/page.tsx imports testShopifyConnection(), which uses a global SHOPIFY_ADMIN_TOKEN env var and never touches shopifyConfig at all. No insert/update path exists anywhere. CREDENTIAL FINDING: Django's ShopifyConfigViewSet was IsAuthenticated-only with no scoping and a default DRF serializer — would have exposed every organization's *_secret_ref columns to any authenticated user via a second API surface, a credential-adjacent leak regardless of whether these are raw secrets or references. DEFECT FOUND AND FIXED THIS ROUND: contained via the existing Round40DenyAllPermission — no real TS or Django consumer of this table was found anywhere. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/rewards/shopify-service.ts","lib/services/rewards/webhook-service.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "social_engagement",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'socialEngagement' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "social_feeds",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'socialFeeds' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "social_posts",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. app/api/social-media/posts/route.ts uses withRoleAuth() and destructures organizationId from the resolved BaseAuthContext (server-derived), rejecting the request when absent. account_id/campaign_id/created_by are parent/audit-actor FKs within the same organization boundary.",
    supportingCapability: ["app/api/social-media/analytics/route.ts","app/api/social-media/campaigns/route.ts","app/api/social-media/posts/route.ts","lib/social-media/social-media-service.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "sso_providers",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, SSO_CREDENTIAL_CONFIGURATION family — treated as the highest-risk table in this round's cohort): db/schema/sso-scim-schema.ts's ssoProviders has organizationId NOT NULL (FK organizations), createdBy (matches crud-factory's special-cased column name), and highly sensitive fields (samlCertificate, oidcClientSecret) per-org SAML/OIDC identity-provider configuration. ONLY ONE live route anywhere: app/api/enterprise/sso/providers/route.ts (GET minRole:'member', POST minRole:'admin'; no PATCH/DELETE/rotate route exists at all — DML is SELECT+INSERT only). GET was already correctly redacting secrets (samlCertificate/oidcClientSecret mapped to true/null booleans, never raw values) and org-scoped from context (never client-supplied). SECURITY DEFECT FOUND AND FIXED: POST's create response previously returned the newly-inserted row UNREDACTED (`return created`), echoing samlCertificate/oidcClientSecret in the clear over the wire — inconsistent with GET's own redaction and an unnecessary secondary exposure surface (logs/proxies/browser devtools) even though the submitting admin already possesses the value. Fixed to apply the identical redaction as GET. organizationId/createdBy were already correctly forced from auth context on create (never trusting a client-supplied organizationId/createdBy in the body) — verified via regression test. Regression tests: app/api/enterprise/sso/providers/__tests__/route.test.ts (5 tests). NO PRE-AUTH DISCOVERY PATH EXISTS (round-53 doctrine section 42): git-grep confirms this is the ONLY code path touching ssoProviders anywhere in the app — no email/domain-based pre-login provider-resolution route was found, meaning the real SSO authentication/login pipeline (if any) does not currently consult this table at all; it is admin-configured reference data without a live enforcement consumer beyond this CRUD surface. RESIDUAL BLOCKER, NOT closure-preventing (round-53 doctrine section 57 — CREDENTIAL_STORAGE_MODEL_UNRESOLVED): oidcClientSecret is stored PLAINTEXT despite the schema's '// Encrypted' comment — no encrypt/decrypt call exists anywhere in the codebase for this field (git-grep confirmed). Building real encryption-at-rest (KMS integration, key rotation policy) is a substantial infrastructure investment outside this round's bounded-defect-fix mandate; documented honestly as an unresolved credential-storage-hygiene gap for a dedicated future round, not glossed over. RESIDUAL OBSERVATION (theoretical, not currently exploitable): samlEntityId/oidcIssuer have no unique constraint at the schema level, so cross-org domain/issuer collision protection does not exist today — but since no live code path resolves a provider BY entity-id/issuer (per the pre-auth-discovery finding above), this cannot currently enable cross-org SSO hijacking; flagged for the same future round that resolves the pre-auth discovery/authentication-consumer question. Django backend/auth_core/views.py's SsoProvidersViewSet was ALREADY DenyAllPermission (contained in a prior round, no action needed this round — reverified unchanged).",
    supportingCapability: ["app/api/enterprise/sso/providers/route.ts","app/api/enterprise/sso/providers/__tests__/route.test.ts","backend/auth_core/views.py"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "sso_sessions",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'ssoSessions' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "steward_assignments",
    classification: "TENANT_RLS_REQUIRED",
    reason: "organization_id present and NOT NULL in the canonical declaration (db/schema/union-structure-schema.ts): steward-to-coverage-area assignment with tenure/training/certification tracking and an optional grievance link. Round 4 (2026-09-01): resolved the CONFLICTING_SCHEMA finding — db/schema/domains/communications/organizer-workflows.ts previously declared a second, conflicting 'steward_assignments' pgTable for a narrower steward-to-individual-member concept. Confirmed via git grep for the module path, its stewardAssignmentTypeEnum, and every real caller of the 'stewardAssignments' symbol that the stale declaration had ZERO production consumers of its own — every real caller (app/api/stewards/[id]/route.ts, app/api/stewards/route.ts, db/queries/union-structure-queries.ts, lib/cognition/ue-adapter.ts, lib/services/steward-assignment.ts, db/schema/domains/member/stewards.ts) already resolved to the canonical declaration, either directly or via db/schema/index.ts's explicit override. Deleted the stale table/enum/relations/types rather than re-exporting them. Tenant-isolation fix from round 3 (lib/services/steward-assignment.ts's assignSteward() persisting organization_id on insert) confirmed still present.",
    supportingCapability: ["app/api/stewards/[id]/route.ts","app/api/stewards/route.ts","db/queries/union-structure-queries.ts","lib/cognition/ue-adapter.ts","lib/services/steward-assignment.ts","db/schema/union-structure-schema.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "stewards",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): org_id AND user_id both NOT NULL (db/schema/domains/member/stewards.ts) — the census scanner missed the org column because it is named `org_id`, not `organization_id`. org_id is the tenant isolation boundary (a steward roster is legitimately visible to org staff, unlike a private per-user resource); user_id identifies which member holds the steward role. app/[locale]/dashboard/stewards/page.tsx uses requireUser()+getOrganizationIdForUser() (BR-6 canonical resolver); lib/cognition/ue-adapter.ts queries with `eq(stewards.orgId, organizationId)`. Of the 78 raw text matches the census census scanner found for the word \"stewards\", the overwhelming majority are marketing copy/prose or an unrelated local variable named `stewards` — only db/schema/domains/member/stewards.ts, app/[locale]/dashboard/stewards/page.tsx, app/api/cognition/{kpis,workload}/route.ts, and lib/cognition/ue-adapter.ts actually reference the Drizzle table.",
    supportingCapability: ["app/(marketing)/components/animated-reviews.tsx","app/[locale]/(marketing)/pricing/page.tsx","app/[locale]/(marketing)/proof/page.tsx","app/[locale]/dashboard/admin/page.tsx","app/[locale]/dashboard/analytics/layout.tsx","app/[locale]/dashboard/claims/page.tsx","app/[locale]/dashboard/cognition/page.tsx","app/[locale]/dashboard/governance/layout.tsx","app/[locale]/dashboard/leadership/page.tsx","app/[locale]/dashboard/stewards/page.tsx","app/[locale]/dashboard/stewards/ratings/page.tsx","app/api/admin/members/stats/route.ts","app/api/ai/pension/members/[id]/projection/route.ts","app/api/ai/pension/plans/[id]/funding/route.ts","app/api/breaks/compliance/route.ts","app/api/cognition/kpis/route.ts","app/api/cognition/workload/route.ts","app/api/ml/query/route.ts","lib/action-enforcer.ts","lib/ai/steward-copilot.ts","lib/ai/template-engine.ts","lib/ai/transparency.ts","lib/auth/roles.ts","lib/canadian-labour-standards/break-defaults.ts","lib/cognition/ue-adapter.ts","lib/hooks/use-members-console-data.ts","lib/hubspot/syncIcraPurchase.ts","lib/icra-pdf/reportNarrativeEngine.ts","lib/icra/adaptation/adaptivePassageLibrary.ts","lib/icra/adaptation/facilitatorAdaptationGuide.ts","lib/icra/contradictions/contradictionSignalPairs.ts","lib/icra/ges-level5/probes.ts","lib/icra/insight-engine.ts","lib/icra/modalities-v2/registry.ts","lib/institutional-legitimacy.ts","lib/intelligence/sector/sectorContinuityProfiles.ts","lib/intelligence/stewardship/dependencyRecurrenceModel.ts","lib/knowledge-transfer/governance/consent-controls.ts","lib/marketing/organizer-impact.ts","lib/oci/benchmark/sectorBaselines.ts","lib/oci/benchmark/stewardshipBurdenPatterns.ts","lib/oci/benchmark/types.ts","lib/oci/conversations/continuityConversationPrompts.ts","lib/oci/facilitation/executiveWorkshopFlows.ts","lib/oci/facilitation/facilitationGuide.ts","lib/oci/facilitation/institutionalDiscoveryFramework.ts","lib/oci/frameworks/governance-entropy-scale.ts","lib/quebec/break-defaults.ts","lib/representation/protocol-types.ts","lib/runtime/onboarding/successorStewardshipRuntime.ts","lib/services/ai/clause-extraction-service.ts","lib/services/dashboard-kpi-service.ts","lib/services/steward-assignment.ts","lib/workbook/engines/continuityRedistributionPlanner.ts","lib/workbook/engines/precedentContinuityMapper.ts","lib/workbook/engines/stewardshipRedistributionEngine.ts","services/platform-economics/entitlement-guard.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE"],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "strategic_goals",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 41 (DIRECT_ORG_RUNTIME_BATCH): direct NOT NULL organization_id column (domains/governance/strategic-goals.ts). app/api/executive/dashboard/route.ts (withApi, minRole vice_president) filters eq(strategicGoals.organizationId, organizationId) directly, and lib/services/dashboard-kpi-service.ts's getExecutiveMetrics() (called from the same route) independently filters eq(strategicGoals.organizationId, organizationId) as well — both with organizationId sourced from the trusted withApi context. Read-only (SELECT); no route inserts/updates/deletes this table. No Django model exists for this table.",
    supportingCapability: ["app/api/executive/dashboard/route.ts","lib/services/dashboard-kpi-service.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "support_tickets",
    classification: "SYSTEM_ONLY",
    reason: "CORRECTED round 44 (round 40's CONTAINED_NO_AUTHORITY closure is invalidated — a real consumer exists that round 40's scan missed): app/[locale]/dashboard/support/page.tsx, .../customer-success/page.tsx, and .../sector-analytics/page.tsx all execute real, unscoped raw SQL against support_tickets (e.g. 'FROM support_tickets', 'LEFT JOIN support_tickets t ON t.organization_id = o.id') inside withSystemContext(), deliberately aggregating/listing every organization's tickets side-by-side for cross-org executive dashboards — this is bounded privileged/system execution, not ordinary tenant authority, even though the table itself carries a NOT NULL organization_id. SECURITY DEFECT FOUND AND FIXED THIS ROUND: all three pages previously gated this cross-organization view with hasMinRole('support_agent' / 'customer_success_director' / 'platform_lead') — an ordinary PER-ORGANIZATION role check (lib/auth/rbac-server.ts's getUserRole resolves the role within the caller's own resolved org), meaning any tenant's own admin could grant one of their own members that role name and view every other organization's ticket subjects, submitters, satisfaction ratings, and grievance/settlement amounts. Fixed: all three pages now gate on isSystemAdmin() (lib/api-auth-guard.ts, backed by the org-independent users.isSystemAdmin column), the same mechanism used for genuine platform-staff-only surfaces elsewhere in this app. Read-only (SELECT aggregates/listings only); no insert/update/delete path exists anywhere. The generated SupportTicketsViewSet in backend/core/views.py remains correctly denied via DenyAllPermission — ordinary Django API access grants nothing, independent of this TS-side platform-admin path.",
    supportingCapability: ["app/[locale]/dashboard/support/page.tsx","app/[locale]/dashboard/customer-success/page.tsx","app/[locale]/dashboard/sector-analytics/page.tsx","lib/api-auth-guard.ts","lib/db/with-rls-context.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "PLATFORM_ADMIN",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "survey_answers",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. app/api/communications/surveys/[surveyId]/results/route.ts is withApi()-backed with server-derived organizationId. response_id/question_id are parent FKs to surveyResponses/surveyQuestions within the same organization boundary as surveys.",
    supportingCapability: ["app/api/communications/surveys/[surveyId]/results/route.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "survey_questions",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. app/api/communications/surveys/[surveyId]/results/route.ts is withApi()-backed with server-derived organizationId. survey_id is a parent FK to surveys within the same organization boundary.",
    supportingCapability: ["app/api/communications/surveys/[surveyId]/results/route.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "survey_responses",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. app/api/communications/surveys/[surveyId]/responses/route.ts is crudRoutes()-backed (orgScoped:true, server-derived organizationId). user_id is intentionally nullable to support anonymous survey responses (product design, not an authority gap) — survey_id is a parent FK to surveys within the same organization boundary.",
    supportingCapability: ["app/api/communications/surveys/[surveyId]/responses/route.ts","app/api/communications/surveys/[surveyId]/results/route.ts","lib/engagement-scoring.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "surveys",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 56 (DIRECT_TENANT_RESOURCE archetype): organization_id NOT NULL. app/api/communications/surveys/route.ts + [surveyId]/route.ts are crudRoutes()-backed (orgScoped:true, server-derived organizationId). created_by is an audit-actor FK to profiles.",
    supportingCapability: ["app/[locale]/surveys/[surveyId]/page.tsx","app/api/communications/surveys/[surveyId]/route.ts","app/api/communications/surveys/route.ts","app/api/satisfaction/route.ts","lib/ai/insights-generator.ts","lib/marketing/organizer-impact.ts","lib/oci/facilitation/types.ts","lib/services/satisfaction-service.ts","lib/workbook-pdf/loadWorkbookModules.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "task_comments",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'taskComments' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "training_courses",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, TRAINING_AND_REGISTRATION family): db/schema/domains/scheduling/training.ts's trainingCourses has organizationId NOT NULL (FK organizations) with no user/parent column of any kind — direct org-owned resource. NOT a child of training_programs: there is no programId FK on trainingCourses at all (trainingPrograms.requiredCourses/electiveCourses are loose JSONB arrays, not FKs) — the two tables are independent siblings, contrary to the round-53 spec's assumed parent/child graph. SCAFFOLDING DUPLICATE-ROUTE FINDING (not a defect, documented for clarity): app/api/education/certifications/generate/route.ts, .../certifications/renewal-reminders/route.ts, .../completions/certificates/route.ts, and .../notification-preferences/route.ts are ALL auto-generated duplicate crudRoutes wrappers mistakenly wired to `table: trainingCourses` despite their URL paths suggesting unrelated resources (a recurring scaffolding-tool mismatch also seen this round in meeting-rooms/[id]/bookings and education/programs/[id]/enrollments / education/sessions/[id]/attendance, all of which are ALSO mislabeled duplicate trainingCourses item routes) — every one of these duplicates has the IDENTICAL safe config (orgScoped:true, readRole:'member', writeRole:'steward', no ownerColumn) as the canonical app/api/education/courses/route.ts, so none introduce a distinct authority surface or defect; they are redundant routing, not a security gap. lib/services/education-service.ts's getCourseById/listCourses/createCourse/updateCourse/deleteCourse etc. accept raw caller-supplied filters with no trusted-context enforcement of their own, but git-grep confirms ZERO real production callers anywhere outside this file and its own tests — dead code. Django backend/unions/views.py's TrainingCoursesViewSet was IsAuthenticated-only with no organization filter at all and no legitimate TS/frontend consumer of the Django REST path exists — contained via DenyAllPermission (backend/unions/tests_round53_training_registration_containment.py). DUAL-SCHEMA FINDING (round-48 precedent, not re-litigated): services/financial-service re-declares training_courses in its own schema — independently verified as a genuine SEPARATE_DATABASE_BOUNDARY (same finding as calendars/meeting_rooms/course_registrations this round).",
    supportingCapability: ["app/api/education/courses/route.ts","app/api/education/certifications/generate/route.ts","app/api/education/certifications/renewal-reminders/route.ts","app/api/education/completions/certificates/route.ts","app/api/education/notification-preferences/route.ts","app/api/education/programs/[id]/enrollments/route.ts","app/api/education/sessions/[id]/attendance/route.ts","backend/unions/views.py","backend/unions/tests_round53_training_registration_containment.py"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "training_programs",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 53, FINAL_SIMPLE_TENANT_EXCEPTION_CLOSURE, TRAINING_AND_REGISTRATION family): db/schema/domains/scheduling/training.ts's trainingPrograms has organizationId NOT NULL (FK organizations) with no user/parent column — direct org-owned resource, independent of trainingCourses (see that entry — requiredCourses/electiveCourses are loose JSONB, not an FK graph). Only one live route: app/api/education/programs/route.ts (crudRoutes, orgScoped:true, readRole:'member', writeRole:'officer', no item route exists — no PATCH/DELETE capability via the TS API today, requiredRuntimePrivileges reflects SELECT+INSERT only). programEnrollments (a separate table, not in this round's cohort) is the real child of trainingPrograms via its own programId FK; app/api/education/programs/[id]/enrollments/route.ts is, despite its path, a mislabeled duplicate trainingCourses item route (see that entry) and touches neither trainingPrograms nor programEnrollments. Django backend/unions/views.py's TrainingProgramsViewSet was IsAuthenticated-only with no organization filter at all and no legitimate TS/frontend consumer of the Django REST path exists — contained via DenyAllPermission (backend/unions/tests_round53_training_registration_containment.py). DUAL-SCHEMA FINDING (round-48 precedent, not re-litigated): services/financial-service re-declares training_programs in its own schema — independently verified as a genuine SEPARATE_DATABASE_BOUNDARY (same finding as the rest of this family).",
    supportingCapability: ["app/api/education/programs/route.ts","backend/unions/views.py","backend/unions/tests_round53_training_registration_containment.py"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
    {
    table: "transaction_fee_events",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 57: organization_id NOT NULL. Genuinely reachable via app/api/payments/webhooks/stripe/route.ts (direct SELECT on sourceTransactionId) and transaction-fee-engine.ts's evaluateFee()/captureTransactionFee()/reverseTransactionFee(), all invoked from that same live Stripe webhook under a system principal (withSystemContext).",
    supportingCapability: ["app/api/payments/webhooks/stripe/route.ts","services/platform-economics/transaction-fee-engine.ts","services/platform-economics/reconciliation-service.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: ["SELECT","INSERT","UPDATE"],
    invocationAuthority: "WEBHOOK",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
    {
    table: "transaction_fee_rules",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CORRECTED round 57 (same barrel-reachability miss as chargeback_statements/fee_adjustments): transaction-fee-engine.ts's findApplicableRule() (SELECT-only) is called from evaluateFee(), which IS invoked by the live app/api/payments/webhooks/stripe/route.ts webhook under a system principal — genuinely reachable, read-only in production today. createFeeRule() (the sole writer) has no real caller; write authority remains latent pending a real management route.",
    supportingCapability: ["app/api/payments/webhooks/stripe/route.ts","services/platform-economics/transaction-fee-engine.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "WEBHOOK",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "trend_analyses",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 48, derived analytics and AI telemetry authority cohort): direct NOT NULL organization_id table (db/schema/analytics.ts, canonical). DUAL-SCHEMA FINDING: services/financial-service also declares 'trend_analyses' — same same-database conclusion as analytics_metrics (see that entry's dual-schema finding in db/rls-storage-authority/analytics-ai.ts; financial-service shares this app's DATABASE_URL and lib/logger at runtime). Real route app/api/analytics/claims/trends/route.ts uses the standard crudRoutes({orgScoped:true}) factory pattern. No defect found.",
    supportingCapability: ["actions/analytics-actions.ts","app/api/analytics/claims/trends/route.ts","lib/ai/insights-generator.ts","services/financial-service/drizzle/relations.ts","services/financial-service/drizzle/schema.ts","services/financial-service/src/db/schema.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "ue_policy_bindings",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'uePolicyBindings' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "user_signatures",
    classification: "USER_RLS_REQUIRED",
    reason: "CLOSED round 57: organization_id AND user_id both NOT NULL. app/api/correspondence/signatures/route.ts's getUserSignatures(userId, organizationId)/saveUserSignature() are self-scoped (a member's own e-signature image/metadata for correspondence) within the tenant boundary — not ordinary org-wide tenant visibility.",
    supportingCapability: ["app/api/correspondence/signatures/route.ts","lib/services/correspondence-service.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "voting_sessions",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Verified 2026-09-01, corrected 2026-09-02 (round 5 — the prior citation app/api/governance/voting-sessions/route.ts does not exist; real path is app/api/governance/elections/sessions/route.ts): direct NOT NULL organization_id column, reachable via app/api/governance/elections/sessions/route.ts (withApi, minRole officer for GET, admin for POST) and its [id]/{vote,results} sub-routes; confirmed UPDATE+DELETE (session lifecycle transitions/cancellation) in the [id] item route. Minimum runtime privilege set from confirmed DML verbs, not defaulted to FULL_DML.",
    supportingCapability: ["app/api/governance/elections/sessions/route.ts","app/api/governance/elections/sessions/[id]/vote/route.ts","app/api/governance/elections/sessions/[id]/results/route.ts","app/api/governance/elections/sessions/[id]/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "wcb_employer_assessments",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/domains/health-safety/provincial-wcb-schema.ts). Reachable via live HTTP routes app/api/wcb/assessments/route.ts, app/api/wcb/assessments/[id]/route.ts.",
    supportingCapability: ["app/api/wcb/assessments/route.ts","app/api/wcb/assessments/[id]/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "website_settings",
    classification: "LATENT_UNREACHABLE",
    reason: "Zero references to the 'websiteSettings' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files, and migrations via git grep across app/, actions/, lib/, services/ (scan: 2026-09-01, corrected pass with test-file exclusion). No known application code path queries this table.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "worksites",
    classification: "TENANT_RLS_REQUIRED",
    reason: "Manually verified 2026-09-02: direct NOT NULL organization_id column (db/schema/union-structure-schema.ts). Reachable via live HTTP routes app/api/worksites/route.ts, app/api/worksites/[id]/route.ts, app/api/dashboard/export-csv/route.ts, app/api/admin/pilot-status/route.ts, app/api/admin/seed-cupe-pilot/route.ts, app/api/pilot/bootstrap/cupe/route.ts.",
    supportingCapability: ["app/api/worksites/route.ts","app/api/worksites/[id]/route.ts","app/api/dashboard/export-csv/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "HIGH",
  },
  {
    table: "ab_test_assignments",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CORRECTED (round 50, state-machine root and fan-out cascade authority): the prior LATENT_UNREACHABLE finding only checked the TS side (correctly: zero real callers). It missed that Django (ai_core app) has a live, router-registered AbTestAssignmentsViewSet (queryset=AbTestAssignments.objects.all(), IsAuthenticated-only, no organization scoping) — LATENT_UNREACHABLE is factually wrong once a router-registered ModelViewSet exists, per the django-reachability invariant test. Contained via the existing DenyAllPermission in backend/ai_core/views.py (round 35); there is no legitimate consumer on either side.",
    supportingCapability: ["backend/ai_core/views.py","backend/ai_core/tests_round50_ab_testing_containment.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "ab_test_events",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CORRECTED (round 50, state-machine root and fan-out cascade authority): the prior LATENT_UNREACHABLE finding only checked the TS side (correctly: zero real callers). It missed that Django (ai_core app) has a live, router-registered AbTestEventsViewSet (queryset=AbTestEvents.objects.all(), IsAuthenticated-only, no organization scoping) — LATENT_UNREACHABLE is factually wrong once a router-registered ModelViewSet exists, per the django-reachability invariant test. Contained via the existing DenyAllPermission in backend/ai_core/views.py (round 35); there is no legitimate consumer on either side.",
    supportingCapability: ["backend/ai_core/views.py","backend/ai_core/tests_round50_ab_testing_containment.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "ab_test_variants",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): same lib/ab-testing/ab-test-engine.ts as the now-closed ab_tests root, zero real callers. Django (ai_core app) has a live, router-registered AbTestVariantsViewSet (queryset=AbTestVariants.objects.all(), IsAuthenticated-only, no organization scoping) — contained via the existing DenyAllPermission in backend/ai_core/views.py (round 35). There is no legitimate consumer on either side.",
    supportingCapability: ["backend/ai_core/views.py","backend/ai_core/tests_round50_ab_testing_containment.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "access_justification_requests",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (round 49, immutable security and audit evidence authority cohort): see employer_access_attempts / firewall_compliance_audit's entries (same service, same round-49 finding) for the full dual-app Django exposure analysis. services/employer-non-interference-service.ts (TS side) has zero callers anywhere; services/api/employer_non_interference_service_views.py's submit_justification/justification_requests actions correctly derive organization_id from request.user.organization_id but have zero frontend consumers, and compliance/views.py's AccessJustificationRequestsViewSet (IsAuthenticated-only, unscoped) was contained via DenyAllPermission too. No legitimate consumer found on either side.",
    supportingCapability: ["services/employer-non-interference-service.ts","services/api/employer_non_interference_service_views.py","backend/compliance/views.py","backend/compliance/tests_round49_evidence_containment.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "address_change_history",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence): no organization_id column; FK addressId -> internationalAddresses (organization_id NOT NULL), append-only shape (createdAt only, no updatedAt). lib/address/address-service.ts's AddressService (the sole writer, via createAddress()'s 'Save to history' insert) has ZERO real callers anywhere in app/, actions/, lib/, services/ (git-grep confirmed) — same dead-TS finding as address_validation_cache (round 51, same file). Django AddressChangeHistoryViewSet (backend/auth_core/views.py) was IsAuthenticated-only with queryset=Model.objects.all() (full CRUD, no org filter) — contained via DenyAllPermission this round (same pattern as the sibling AddressValidationCacheViewSet in the same file). No legitimate consumer found on either side.",
    supportingCapability: ["lib/address/address-service.ts","backend/auth_core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "address_validation_cache",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) lib/address/address-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/auth_core/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/auth_core/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/address/address-service.ts","backend/auth_core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "alert_actions",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'alertActions' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "alert_conditions",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'alertConditions' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "alert_executions",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence): no organization_id column; NOT NULL alertRuleId FK -> alertRules, CLOSED round 46 TENANT_RLS_REQUIRED (MIXED tenant+system authority). RESOLVED_PARENT_READY, re-verifying the exact shape alert_rules' own entry already documented: SELECT is tenant-reachable via getRecentRealtimeAlerts(organizationId) (app/api/alerts/realtime/route.ts) which innerJoins alertExecutions to alertRules and filters on alertRules.organizationId = the trusted context org — a fail-closed parent-verified predicate, no direct executionId route exists so no IDOR surface. INSERT is system-reachable via emitSignal() (app/api/cron/observability-alerts/route.ts's runRealtimeObservabilitySweep(), wrapped in withSystemContext per the round-46 fix) and is always scoped to a rule.id resolved via getOrCreateRealtimeRule(signal), itself scoped to signal.organizationId — no client-suppliable parent-injection surface. No UPDATE/DELETE path exists anywhere (write-once execution record). No Django ViewSet exists for this table. No raw-SQL references found.",
    supportingCapability: ["services/observability/realtime-alerting-service.ts","app/api/alerts/realtime/route.ts","app/api/cron/observability-alerts/route.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: ["INSERT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "NONE",
  },
  {
    table: "alert_recipients",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 42: the census's TS references were all false positives — 'alertRecipients' as matched in actions/analytics-actions.ts, components/analytics/kpi-builder-dialog.tsx, and components/targets/targets-console.tsx is a JSONB field/prop name on unrelated KPI/target records, not an import of the actual alertRecipients Drizzle table export (git-grep-confirmed zero real imports of the table anywhere in app/, actions/, lib/, services/, components/). The services/financial-service/** references are likewise a jsonb column named 'alert_recipients' on a different table, not a dual schema declaration. DEFECT FOUND AND FIXED THIS ROUND: AlertRecipientsViewSet (backend/core/views.py) was router-registered with queryset=Model.objects.all() + IsAuthenticated-only and a client-controlled, unenforced alert_rule_id filterset field — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["backend/core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "allocation_runs",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): direct NOT NULL organization_id column, stamped from the trusted auth-context organizationId inside runAllocation()'s transaction (services/platform-economics/allocation-engine.ts), never client input. Reachable via app/api/finance/allocation/run/route.ts (withMinRole('admin') + requireEntitlement gated). STATE MACHINE: status simulated (isSimulation=true, no side effects) | posted (isSimulation=false, creates platform_cost_ledger_entries + chargeback_statements + marks source ledger entries allocated, all inside one DB transaction). Root of the allocation_basis_snapshots/allocation_run_lines cascade (both scoped via runId, no org column of their own). SECURITY DEFECT FOUND AND FIXED: the ruleId this run applies was previously resolvable across tenants — see allocation_rules/allocation_rule_versions entries for the fix. NOTE: getAllocationRun(runId) in the same file has no org filter and is genuinely unreachable today (zero callers outside the engine file itself) — flagged so it is not wired into a route without adding the same org check.",
    supportingCapability: ["app/api/finance/allocation/run/route.ts","services/platform-economics/allocation-engine.ts","services/platform-economics/__tests__/allocation-engine.test.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "allocation_basis_snapshots",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority — direct child of the now-closed allocation_runs root): no organization_id column of its own; authority derives entirely through runId -> allocation_runs, which is stamped with a trusted organizationId at creation. Written only inside the same transaction as its parent allocation_runs row in runAllocation() (services/platform-economics/allocation-engine.ts); never independently writable.",
    supportingCapability: ["services/platform-economics/allocation-engine.ts"],
    requiredRuntimePrivileges: ["INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "allocation_rule_versions",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority — direct child of the now-closed allocation_rules root): no organization_id column of its own; authority derives through ruleId -> allocation_rules. SECURITY DEFECT FOUND AND FIXED: getActiveRuleVersion(ruleId) (services/platform-economics/allocation-engine.ts) resolved a version by ruleId alone with no tenant scoping; runAllocation() passed a client-supplied ruleId straight through, letting one organization apply another organization's private allocation-rule configuration to its own cost data (cross-tenant IDOR). Fixed by adding verifyAllocationRuleOwnership(ruleId, organizationId), called in runAllocation() before the rule version is ever resolved/applied; existing + 2 new regression tests in services/platform-economics/__tests__/allocation-engine.test.ts all pass (14/14).",
    supportingCapability: ["services/platform-economics/allocation-engine.ts","services/platform-economics/__tests__/allocation-engine.test.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "allocation_run_lines",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority — direct child of the now-closed allocation_runs root): no organization_id column of its own; authority derives entirely through runId -> allocation_runs. Written only inside the same transaction as its parent allocation_runs row in runAllocation() (services/platform-economics/allocation-engine.ts); never independently writable.",
    supportingCapability: ["services/platform-economics/allocation-engine.ts"],
    requiredRuntimePrivileges: ["INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "arms_length_verification",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/founder-conflict-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/founder-conflict-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "automation_execution_log",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'automationExecutionLog' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "automation_schedules",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'automationSchedules' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "autopay_settings",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: lib/utils/autopay-utils.ts's exported functions (getAutoPaySettings, upsertAutoPaySettings, etc.) have zero real callers in app/, actions/ (confirmed via direct function-name search) — dead TS. SECURITY DEFECT FOUND AND FIXED THIS ROUND: billing.AutopaySettingsViewSet was router-registered with queryset=Model.objects.all() + IsAuthenticated-only and no organization/user filter (no organization_id column exists at all — user_id only) — contained via SharedDenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/utils/autopay-utils.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "award_history",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence): no organization_id column; NOT NULL templateId FK -> awardTemplates, ALSO CLOSED this round as CONTAINED_NO_AUTHORITY (dead code, see that entry — BLOCKER_ROOT_MICRO_COHORT). lib/services/rewards/template-service.ts's recordTemplateUsage()/getTemplateHistory() (the only code touching this table) have ZERO real callers anywhere in app/, actions/, lib/, services/ (git-grep confirmed — the whole module has no importer outside its own test file). Django AwardHistoryViewSet (backend/unions/views.py) was IsAuthenticated-only with queryset=Model.objects.all() (full CRUD, filterable by template_id/recipient_id, no org filter) — contained via DenyAllPermission this round. No legitimate consumer found on either side.",
    supportingCapability: ["lib/services/rewards/template-service.ts","backend/unions/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "band_councils",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/indigenous-data-service.ts, lib/services/indigenous-data-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/indigenous-data-service.ts","lib/services/indigenous-data-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "bank_of_canada_rates",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: services/transfer-pricing-service.ts's TransferPricingService class (the sole reader/writer of this and 6 sibling transfer-pricing tables) has ZERO real instantiations anywhere in app/, actions/, lib/, services/ outside its own file (confirmed via `new TransferPricingService(` search) — dead TS, distinct from the unrelated, genuinely-reachable lib/services/transfer-pricing-service.ts (convertUSDToCAD/checkT106Requirement/validateBillingRequest, used by app/api/billing/validate/route.ts). SECURITY DEFECT FOUND AND FIXED THIS ROUND: billing.BankOfCanadaRatesViewSet was IsAuthenticated-only with no organization filter (no organization_id column exists at all) — contained via SharedDenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/transfer-pricing-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "bank_transactions",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'bankTransactions' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "benchmark_categories",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'benchmarkCategories' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "benchmark_data",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'benchmarkData' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "benefit_comparisons",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 44 (RESOLVED_PARENT_ROOT candidate; parents collective_agreements + cba_clauses are both closed TENANT_RLS_REQUIRED, but the child itself has no real consumer): lib/services/clause-service.ts imports benefitComparisons for TYPE DECLARATIONS ONLY (NewBenefitComparison/BenefitComparison) — the table is never actually queried anywhere in app/, actions/, lib/, services/ (git-grep-confirmed round 44). DEFECT FOUND AND FIXED THIS ROUND: BenefitComparisonsViewSet (backend/bargaining/views.py) was router-registered with queryset=Model.objects.all() + IsAuthenticated-only and NO organization filter — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/clause-service.ts","backend/bargaining/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "blind_trust_registry",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: services/founder-conflict-service.ts's FounderConflictService class (the sole reader/writer of this table and its 2 siblings) has ZERO real instantiations anywhere in app/, actions/ (confirmed via `new FounderConflictService(` search) — dead TS. SECURITY DEFECT FOUND AND FIXED THIS ROUND: compliance.BlindTrustRegistryViewSet was IsAuthenticated-only with no organization/user filter (no organization_id column exists at all — user_id only) — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/founder-conflict-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "break_glass_activations",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (round 49, immutable security and audit evidence authority cohort): services/break-glass-service.ts's BreakGlassService (exported from lib/services/index.ts, but with zero actual importers anywhere) and services/force-majeure-integration.ts (also zero callers) are both fully dead TS code. DJANGO SIDE IS REAL AND REACHABLE: services/api/break_glass_service_views.py's BreakGlassServiceViewSet is router-registered at 'break-glass-service' and correctly derives organization_id from request.user.organization_id on both reads and writes, but has ZERO frontend consumers anywhere. No legitimate consumer found on either side for a capability this sensitive (emergency/disaster-recovery break-glass access). CONTAINED: BreakGlassServiceViewSet's permission_classes changed to a new local DenyAllPermission (collaterally also protects its break_glass_system/disaster-recovery/emergency-declaration actions, out of this round's direct scope); the separate generated compliance/views.py BreakGlassActivationsViewSet (IsAuthenticated-only, unscoped) was contained too.",
    supportingCapability: ["services/break-glass-service.ts","services/force-majeure-integration.ts","services/api/break_glass_service_views.py","backend/compliance/views.py","backend/compliance/tests_round49_evidence_containment.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "break_glass_system",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/break-glass-service.ts, services/force-majeure-integration.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/break-glass-service.ts","services/force-majeure-integration.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "certification_alerts",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: services/certification-management-service.ts's CertificationManagementService class (the sole reader/writer of this table and its 2 siblings) has ZERO real instantiations anywhere in app/, actions/ (confirmed via `new CertificationManagementService(` search) — dead TS. The `continuing_education` string match in components/education/admin/ApprenticeshipManager.tsx is a UI category label, not a table reference — that component's real fetch calls target /api/education/programs, an unrelated table. SECURITY DEFECT FOUND AND FIXED THIS ROUND: compliance.CertificationAlertsViewSet was IsAuthenticated-only with no organization/user filter (no organization_id column exists at all — user_id only) — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/certification-management-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "certification_compliance_reports",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/certification-management-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/certification-management-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "certification_types",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/certification-management-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/certification-management-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "clause_embeddings",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 44 (RESOLVED_PARENT_ROOT:HIGH-confidence, parent cba_clauses already closed TENANT_RLS_REQUIRED): no organizationId column; authority derived through clause_id -> cba_clauses. app/api/grievances/[id]/suggest-clauses/route.ts (withOrganizationAuth, minRole steward) calls lib/services/clause-intelligence.ts's findRelevantClauses(orgId, ...), which reads clauseEmbeddings for vector similarity scoring and hydrates matched clauses filtered by and(eq(cbaClause.id, hit.clauseId), eq(cbaClause.organizationId, orgId)). DEFECT FOUND AND FIXED THIS ROUND: the embedding scan previously pulled every organization's embedding vectors into memory before the org-filtered hydration step (db.select(...).from(clauseEmbeddings) with no join/predicate) — fixed to inner-join cba_clauses and filter by organizationId up front, so cross-org embedding rows are never read at all. Read-only (SELECT); no insert/update/delete path exists (embeddings are seeded outside application code). No Django model exists for this table.",
    supportingCapability: ["app/api/grievances/[id]/suggest-clauses/route.ts","lib/services/clause-intelligence.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "clause_library_tags",
    classification: "MULTI_PARTY_RLS_REQUIRED",
    reason: "CLOSED round 55 (final non-voting parent architecture exceptions): direct parent shared_clause_library CLOSED this round (MULTI_PARTY_RLS_REQUIRED, OWNER_PLUS_EXPLICIT_SHARING_AUTHORITY \u2014 see that entry for the full sharing-level model). Tags inherit the parent clause's authority exactly, not a simplified version: app/api/clause-library/[id]/tags/route.ts's GET is gated by canReadSharedClause against the parent clause row (tag visibility \u2264 parent clause visibility \u2014 a federation/congress/public/explicit-grant reader sees tags, an unauthorized org gets a 404, not a differently-shaped error, per the existing not-found-fails-closed convention). POST/DELETE (add/remove a tag) are owner-only via isSharedClauseOwner (tag mutation \u2264 parent clause mutation \u2014 broad read visibility never implies tag-write authority). CONCRETE DEFECT FOUND AND FIXED: DELETE-by-tagId previously deleted by tagId ALONE with no clauseId check at all \u2014 a caller who knew or guessed any tag's id (including one belonging to a DIFFERENT clause, possibly owned by a different organization) could detach it regardless of which clause the URL named; fixed to require and(eq(id,tagId), eq(clauseId,id)) so a tag must belong to the exact clause named in the URL. Also previously had ZERO clause-existence/ownership check on any of GET/POST/DELETE (matching the parent's pre-round-55 state) \u2014 fixed identically to the parent's route family. classification is MULTI_PARTY_RLS_REQUIRED (not PARENT_OWNED_RLS_REQUIRED) because it inherits the parent's genuinely multi-party read model, not a single-tenant boundary \u2014 PARENT_OWNED_RLS_REQUIRED's simpler 1:1-organization-boundary vocabulary would misrepresent this table's actual authority shape. Django ClauseLibraryTagsViewSet (backend/bargaining/views.py) DOES exist (the round-54 entry's 'no ViewSet exists for this table' claim was incorrect \u2014 corrected this round) and was IsAuthenticated-only with queryset=Model.objects.all(), no scoping of any kind \u2014 contained via DenyAllPermission this round (same cohort as SharedClauseLibraryViewSet).",
    supportingCapability: ["app/api/clause-library/[id]/tags/route.ts", "lib/clause-library/sharing-authority.ts", "backend/bargaining/views.py"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "clc_oauth_tokens",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) lib/services/external-data/clc-partnership-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/external-data/clc-partnership-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "clc_union_density",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) lib/services/external-data/clc-partnership-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/external-data/clc-partnership-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "conflict_disclosures",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: services/founder-conflict-service.ts's FounderConflictService class (the sole reader/writer of this table and its 2 siblings) has ZERO real instantiations anywhere in app/, actions/ (confirmed via `new FounderConflictService(` search) — dead TS. SECURITY DEFECT FOUND AND FIXED THIS ROUND: compliance.ConflictDisclosuresViewSet was IsAuthenticated-only with no organization/user filter (no organization_id column exists at all — user_id only) — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/founder-conflict-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "conflict_of_interest_policy",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/founder-conflict-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/founder-conflict-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "conflict_training",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'conflictTraining' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "continuing_education",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: services/certification-management-service.ts's CertificationManagementService class (the sole reader/writer of this table and its 2 siblings) has ZERO real instantiations anywhere in app/, actions/ (confirmed via `new CertificationManagementService(` search) — dead TS. The `continuing_education` string match in components/education/admin/ApprenticeshipManager.tsx is a UI category label, not a table reference — that component's real fetch calls target /api/education/programs, an unrelated table. SECURITY DEFECT FOUND AND FIXED THIS ROUND: compliance.ContinuingEducationViewSet was IsAuthenticated-only with no organization/user filter (no organization_id column exists at all — user_id only) — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/certification-management-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "contract_amendments",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'contractAmendments' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "commercial_contracts",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence, BLOCKER_ROOT_MICRO_COHORT — direct parent of contract_line_items, one-hop): organization_id NOT NULL (db/schema/domains/finance/contracts.ts). All 3 real reference paths verified: app/api/contracts/route.ts's GET/POST both use the trusted withApi organizationId context (GET filters by it directly; POST passes it straight to createContract(), never client-suppliable). app/api/pilot/apply/[id]/commercial-transition/route.ts requires system_admin (platform-tier) and derives organizationId from the already-verified pilot application record inside a withSystemContext transaction, not from client input. services/platform-economics/contract-service.ts's activateContract/terminateContract/checkContractEntitlement/recordEntitlementUsage/resetExpiredUsagePeriods have zero real callers anywhere (dead code, flagged not fixed since unreachable) and take contractId/organizationId as trusted caller-supplied parameters, not client input directly. SECURITY DEFECT FOUND AND FIXED THIS ROUND (see contract_line_items entry): getContractLineItems(contractId) had zero org verification, a cross-org IDOR — fixed. No Django ViewSet exists for this table.",
    supportingCapability: ["app/api/contracts/route.ts","app/api/pilot/apply/[id]/commercial-transition/route.ts","services/platform-economics/contract-service.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: ["INSERT","UPDATE"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "NONE",
  },
  {
    table: "contract_line_items",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence): no organization_id column; NOT NULL contractId FK -> commercialContracts, ALSO CLOSED this round as TENANT_RLS_REQUIRED (organization_id NOT NULL — see that entry, BLOCKER_ROOT_MICRO_COHORT). SECURITY DEFECT FOUND AND FIXED THIS ROUND (real, live cross-org IDOR): app/api/contracts/[id]/clauses/route.ts's GET (auth.minRole='member') passed the URL's contract id straight to services/platform-economics/contract-service.ts's getContractLineItems(contractId) with ZERO verification that the contract belonged to the caller's organizationId — any authenticated member of ANY organization could read another organization's contract pricing/feature-key/SLA/usage-limit data by guessing or enumerating contract ids. Fixed: getContractLineItems(contractId, organizationId) now verifies contract ownership (a scoped SELECT against commercialContracts) before returning line items, returning an empty array (fail-closed, no existence leak) when the contract does not belong to the caller's org; the route now passes the trusted organizationId from context. services/platform-economics/__tests__/contract-service.test.ts extended with a cross-org IDOR regression test (18/18 passing). INSERT is reachable only via createContract() (app/api/contracts/route.ts POST), which creates line items scoped to a contract just created with the trusted organizationId — no separate injection surface. No UPDATE/DELETE path exists anywhere. No Django ViewSet exists for this table.",
    supportingCapability: ["services/platform-economics/contract-service.ts","app/api/contracts/[id]/clauses/route.ts","app/api/contracts/route.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "contract_rate_cards",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'contractRateCards' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "contribution_rates",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "CLOSED round 57: no organization_id/user_id column — wage-benchmark reference data by industry/sector. Genuinely reachable via lib/graphql/resolvers.ts (app/api/graphql/route.ts is a live GraphQL endpoint) for reads, and lib/services/external-data/wage-enrichment-service.ts for system-principal writes (withSystemContext). SECURITY DEFECT FOUND AND FIXED THIS ROUND: analytics.ContributionRatesViewSet was IsAuthenticated-only with no scoping at all — contained via DenyAllPermission (GraphQL remains the sole legitimate consumer).",
    supportingCapability: ["lib/graphql/resolvers.ts","lib/services/external-data/wage-enrichment-service.ts","backend/analytics/views.py"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: ["SELECT","INSERT","UPDATE"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "correspondence_recipients",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 44 (RESOLVED_PARENT_ROOT:HIGH-confidence, parent correspondence already closed TENANT_RLS_REQUIRED): no organizationId column; authority derived through correspondence_id -> correspondence. app/api/correspondence/route.ts's POST (withApi, minRole steward) creates recipients only against a correspondence.id it just created in the same trusted-org insert (correspondenceId: created.id, never client-supplied); app/api/correspondence/[id]/route.ts's GET verifies item.organizationId === organizationId after loading via getCorrespondenceWithDetails(id) before returning recipients. No reassignment path exists (recipients are never re-parented to a different correspondence). No Django model exists for this table.",
    supportingCapability: ["app/api/correspondence/route.ts","app/api/correspondence/[id]/route.ts","lib/services/correspondence-service.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "cost_of_living_data",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, EXTERNAL_REFERENCE_AND_SYNC family): externally-sourced Statistics Canada CPI/inflation reference data (db keys on geographyCode+year, refDate) with no organizationId column — same meaning for every organization. Sole writer is lib/services/external-data/wage-enrichment-service.ts's syncCOLAData, invoked exclusively via the real CRON_SECRET-gated app/api/cron/external-data-sync/route.ts (git-grep confirmed zero other callers). DEFECT FOUND AND FIXED THIS ROUND: syncCOLAData previously ran on the plain tenant `db` import despite being SYSTEM_SCHEDULE-invoked; now wrapped in withSystemContext (same round-46 pattern already applied to syncUnionDensity). Existing rows are period-keyed (new year -> new/updated row for that period; syncCOLAData's own update-if-existing-by-geography+year logic already treats each period as its own record rather than overwriting history). Regression test added (lib/services/external-data/__tests__/wage-enrichment-service.test.ts) proving withSystemContext is invoked. Django: backend/analytics/views.py CostOfLivingDataViewSet was a separate unscoped IsAuthenticated-only surface with no legitimate consumer — contained via DenyAllPermission.",
    supportingCapability: ["lib/services/external-data/wage-enrichment-service.ts","app/api/cron/external-data-sync/route.ts","backend/analytics/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: ["SELECT","INSERT","UPDATE"],
    invocationAuthority: "SYSTEM_SCHEDULE",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "country_address_formats",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) lib/address/address-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/auth_core/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/auth_core/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/address/address-service.ts","backend/auth_core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "cpi_adjusted_pricing",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/joint-trust-fmv-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/joint-trust-fmv-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "cpi_data",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/joint-trust-fmv-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/joint-trust-fmv-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "cross_border_transactions",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) lib/services/currency-service.ts, services/currency-enforcement-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/currency-service.ts","services/currency-enforcement-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "currency_enforcement_policy",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: same dead TransferPricingService class as bank_of_canada_rates. SECURITY DEFECT FOUND AND FIXED THIS ROUND: billing.CurrencyEnforcementPolicyViewSet was IsAuthenticated-only with no organization filter — contained via SharedDenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/transfer-pricing-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "currency_enforcement_violations",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: same dead TransferPricingService class as bank_of_canada_rates. SECURITY DEFECT FOUND AND FIXED THIS ROUND: billing.CurrencyEnforcementViolationsViewSet was IsAuthenticated-only with no organization filter — contained via SharedDenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/transfer-pricing-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "data_classification_policy",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, PRIVACY_AND_JURISDICTION family): db/schema/domains/compliance/employer-interference.ts's dataClassificationPolicy has no organizationId column — a single platform-wide 'Employer Data Firewall Policy' doctrine (effectiveDate/expiryDate versioned, approvedBy is just the approving user, not a tenant). The 3 real routes (app/api/privacy/{breach,dsar,provincial}/route.ts) all wire the SAME crudRoutes({table: dataClassificationPolicy, orgScoped: true, readRole: 'member', writeRole: ...}) — orgScoped is a no-op since the table has no org column. DEFECT FOUND AND FIXED THIS ROUND: writeRole was 'admin' (an ORDINARY per-organization role) gating a mutation to platform-wide compliance doctrine — any single org's own admin could rewrite what employer/union data-firewall rules apply platform-wide, the exact same defect class as round 51's feature_flags fix. Fixed: writeRole changed to 'compliance_manager' (a genuine PLATFORM_ELEVATED_ROLES member, see lib/api-auth-guard.ts — never tenant-self-service-assignable). readRole stays 'member' (global readability does not require platform authority, only global writability does). Regression test added (app/api/privacy/__tests__/data-classification-policy-writerole.test.ts) proving all 3 routes pass writeRole:'compliance_manager', never 'admin'. Django: backend/compliance/views.py DataClassificationPolicyViewSet was a separate unscoped IsAuthenticated-only duplicate surface with no legitimate consumer of its own — contained via DenyAllPermission (the crudRoutes path above is the sole legitimate surface).",
    supportingCapability: ["app/api/privacy/breach/route.ts","app/api/privacy/dsar/route.ts","app/api/privacy/provincial/route.ts","app/api/privacy/__tests__/data-classification-policy-writerole.test.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "data_classification_registry",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/employer-non-interference-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/employer-non-interference-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "data_subject_access_requests",
    classification: "USER_RLS_REQUIRED",
    reason: "CLOSED round 56 (PRIVACY_SENSITIVE archetype): user_id NOT NULL. Reachable via app/api/{enterprise/dsr/requests,gdpr/consents,gdpr/data-erasure,gdpr/requests}/route.ts — GDPR/DSR (Data Subject Rights) request handling, self-service by design.",
    supportingCapability: ["app/api/enterprise/dsr/requests/route.ts","app/api/gdpr/consents/route.ts","app/api/gdpr/data-erasure/route.ts","app/api/gdpr/requests/route.ts","services/provincial-privacy-service.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE"],
    requiredSystemPrivileges: ["SELECT", "UPDATE"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "HIGH",
  },
  {
    table: "disaster_recovery_drills",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/break-glass-service.ts, services/force-majeure-integration.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/break-glass-service.ts","services/force-majeure-integration.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "dispatch_assignments",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority — direct child of the now-closed dispatch_requests root): no org_id column of its own; authority derives entirely through requestId -> dispatch_requests. SECURITY DEFECT FOUND AND FIXED here (see the dispatch_requests entry for the full write-up): assignWorkersToDispatch() previously wrote to this table for any requestId with no organization check at all. Fixed via an ownership check on the parent dispatch_requests row before any insert.",
    supportingCapability: ["app/api/dispatch/assign/route.ts","lib/services/dispatch-engine.ts","lib/services/__tests__/dispatch-engine.test.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "dsr_activity_log",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'dsrActivityLog' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "dunning_policies",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): same services/platform-economics/dunning-service.ts as the now-closed dunning_cases root; no real callers found (see dunning_cases entry). No Django exposure.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "dunning_steps",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): same services/platform-economics/dunning-service.ts as the now-closed dunning_cases root; no real callers found (see dunning_cases entry). No Django exposure.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "emergency_declarations",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/break-glass-service.ts, services/force-majeure-integration.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/break-glass-service.ts","services/force-majeure-integration.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "employer_access_attempts",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED (round 49, immutable security and audit evidence authority cohort): db/schema/employer-non-interference-schema.ts declares NO organization_id column. services/employer-non-interference-service.ts's EmployerNonInterferenceService (TS side) has zero callers anywhere — see firewall_compliance_audit's entry (same service, same round-49 finding) for the full dual-app Django exposure analysis. No legitimate consumer found on either side; both services/api/employer_non_interference_service_views.py's ViewSet and compliance/views.py's EmployerAccessAttemptsViewSet were contained via DenyAllPermission this round.",
    supportingCapability: ["services/employer-non-interference-service.ts","services/api/employer_non_interference_service_views.py","backend/compliance/views.py","backend/compliance/tests_round49_evidence_containment.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "employer_reports",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 42 (PARENT_OWNED:parent:HIGH): no direct organization_id column; authority derived through employer_id -> employers (closed round 41, TENANT_RLS_REQUIRED). app/api/compliance/reports/route.ts (withOrganizationAuth, organizationId server-derived) loads the caller's own employer ids first (eq(employers.organizationId, organizationId)) then filters employerReports via inArray(employerReports.employerId, employerIds) — the canonical verify-parent-then-filter-child pattern, no client-supplied employerId used for authority. Read-only (SELECT); no insert/update/delete route exists. No Django model exists for this table.",
    supportingCapability: ["app/api/compliance/reports/route.ts"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "exchange_rates",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) lib/services/currency-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/currency-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "feature_flags",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, TRUE_GLOBAL_REFERENCE/PLATFORM_SHARED_CONFIGURATION partition): db/schema/domains/infrastructure/features.ts has no organizationId column at all — one row per globally-unique flag `name`, read by dozens of AI routes via lib/services/feature-flags.ts's isFeatureEnabled/AI_FEATURES (SELECT-only) and by the read-only GET /api/feature-flags (any authenticated user, evaluates booleans only, no row data exposed). The ONLY real production WRITE path is lib/feature-flags.ts's toggleFeatureFlag (SELECT+UPDATE, no INSERT — flags are pre-seeded), invoked from PATCH /api/admin/feature-flags. DEFECT FOUND AND FIXED THIS ROUND: that PATCH route used withAdminAuth (hasMinRole('admin') — an ORDINARY per-organization admin role) to gate a mutation that flips a platform-wide kill switch affecting every organization; any single org's own admin could disable/enable a platform-wide feature for all tenants. The route's own org-mismatch guard (comparing a body.organizationId field to the caller's org) was dead code — toggleFlagSchema is a plain z.object({name,enabled}) with no .passthrough(), so zod strips any organizationId field from the parsed body before the check runs, meaning the guard could never actually fire. Fixed by switching PATCH to withSystemAdminAuth (isSystemAdmin() — genuine platform-wide authority, the established pattern already used elsewhere in this codebase), per round-51 doctrine section 11 (do not invent a new role when a real platform-admin check already exists). GET (read-only flag listing) remains withAdminAuth — global readability does not require platform-level authority, only global writability does. lib/services/feature-flags.ts's own write helpers (upsertFeatureFlag/enableFeature/disableFeature/setRolloutPercentage/addOrganizationToPilot/removeOrganizationFromPilot) and lib/services/feature-flags-service.ts's enableFeatureFlag/disableFeatureFlag all have zero real callers anywhere (git-grep confirmed) — dead code, not a live mutation path. Regression test added (app/api/__tests__/admin-feature-flags.route.test.ts) proving PATCH is gated by withSystemAdminAuth (not withAdminAuth) and that a system-admin-auth denial blocks the toggle before any DB write.",
    supportingCapability: ["app/api/admin/feature-flags/route.ts","app/api/feature-flags/route.ts","lib/feature-flags.ts","lib/services/feature-flags.ts","lib/services/feature-flags-service.ts","app/[locale]/dashboard/settings/_components/platform-settings-content.tsx"],
    requiredRuntimePrivileges: ["SELECT","UPDATE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "federation_executives",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'federationExecutives' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "federation_meetings",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'federationMeetings' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "federation_resources",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'federationResources' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "firewall_access_rules",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence): db/schema/employer-non-interference-schema.ts declares NO organization_id column — a role-based access-policy configuration table (userRole/dataTypeId/accessPermitted), not tenant-scoped by design. services/employer-non-interference-service.ts's EmployerNonInterferenceService.validateAccessAttempt() (the sole TS reader) has ZERO real callers anywhere (git-grep confirmed) — same dead-TS finding as the round-49 firewall_compliance_audit/certification_audit_log cohort in the same service. DJANGO: the dedicated EmployerNonInterferenceServiceViewSet.check_access action (services/api/employer_non_interference_service_views.py) was already CONTAINED round 49 (DenyAllPermission). A SEPARATE generated ModelViewSet (backend/compliance/views.py's FirewallAccessRulesViewSet) was STILL IsAuthenticated-only with queryset=Model.objects.all() — contained via DenyAllPermission this round. No legitimate consumer found on either side.",
    supportingCapability: ["services/employer-non-interference-service.ts","services/api/employer_non_interference_service_views.py","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "firewall_violations",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence): db/schema/employer-non-interference-schema.ts declares NO organization_id column — a security-incident log keyed by user_id/data_type_id, not tenant-scoped by design (systemDetected default true, incidentStatus lifecycle). services/employer-non-interference-service.ts's flagViolation()/getOpenViolations()/generateComplianceAudit() (the only code touching this table) have ZERO real callers anywhere (git-grep confirmed) — same dead-TS finding as the round-49 cohort in the same service. DJANGO: the dedicated EmployerNonInterferenceServiceViewSet.report_violation action was already CONTAINED round 49 (DenyAllPermission). A SEPARATE generated ModelViewSet (backend/compliance/views.py's FirewallViolationsViewSet) was STILL IsAuthenticated-only with queryset=Model.objects.all() (exposing every organization's security-violation user_id/email/IP/description data) — contained via DenyAllPermission this round. No legitimate consumer found on either side.",
    supportingCapability: ["services/employer-non-interference-service.ts","services/api/employer_non_interference_service_views.py","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "fmv_benchmarks",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/joint-trust-fmv-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/joint-trust-fmv-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "fmv_policy",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/joint-trust-fmv-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/joint-trust-fmv-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "fmv_violations",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/joint-trust-fmv-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/joint-trust-fmv-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "geofence_events",
    classification: "USER_RLS_REQUIRED",
    reason: "CLOSED round 56 (PRIVACY_SENSITIVE archetype): user_id NOT NULL. Reachable via app/api/location/{track,geofence,consent}/route.ts and app/api/emergency/{recovery,activate}/route.ts. app/api/location/track/route.ts is explicitly documented as \"strictly self-service: userId is always the [authenticated caller's] user.id\" via getCurrentUser() — never a client-supplied value.",
    supportingCapability: ["services/geofence-privacy-service.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NORMAL",
  },
  {
    table: "geofences",
    classification: "TENANT_RLS_REQUIRED",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, LOCATION_AND_GEOFENCE family): geofences has no organizationId column, but unionLocalId is the real tenant boundary (a 'union local' is this domain's organization — 'Strike Line - Plant A', 'Union Hall - Local 123'). DEFECT FOUND AND FIXED THIS ROUND: POST /api/location/geofence (services/geofence-privacy-service.ts's createGeofence) trusted a client-supplied body.unionLocalId with zero verification — any authenticated user of ANY organization could create a geofence claiming to belong to a different union local (classic request-body-tenant-id IDOR). Fixed: unionLocalId is now always resolved server-side via getOrganizationIdForUser(caller), never accepted from the request body. Also fixed a second defect in the same route: GET /api/location/geofence (checkGeofenceEntry) accepted an arbitrary userId query param and would log a geofence entry/exit event attributed to that id — any authenticated caller could fabricate GPS-derived attendance records for another member (same class of defect the round-49 fix already closed for /api/location/track). Fixed: userId is now always the authenticated caller's own id. Also added a cross-tenant read guard: checkGeofenceEntry now throws 'Geofence not found' if the target geofence's unionLocalId does not match the caller's own union local, so Org B cannot probe Org A's geofence definitions via entry-check. Regression tests added (app/api/location/__tests__/geofence-route.test.ts, services/__tests__/geofence-privacy-service.test.ts).",
    supportingCapability: ["app/api/location/geofence/route.ts","services/geofence-privacy-service.ts","app/api/location/__tests__/geofence-route.test.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "independent_appraisals",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/joint-trust-fmv-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/joint-trust-fmv-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "indigenous_data_access_log",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: services/indigenous-data-service.ts's IndigenousDataService class (the sole reader/writer) has ZERO real instantiations anywhere in app/, actions/ (confirmed via `new IndigenousDataService(` search) — dead TS. SECURITY DEFECT FOUND AND FIXED THIS ROUND: compliance.IndigenousDataAccessLogViewSet was IsAuthenticated-only with no organization/user filter (no organization_id column exists at all — user_id only) — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY. Sovereignty-sensitive: no legitimate consumer currently exists, so no ordinary tenant-admin authority was assumed.",
    supportingCapability: ["services/indigenous-data-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "indigenous_data_sharing_agreements",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/indigenous-data-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/indigenous-data-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "journal_entry_lines",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'journalEntryLines' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "key_holder_registry",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: services/break-glass-service.ts's BreakGlassService class (the sole reader/writer) has ZERO real instantiations anywhere in app/, actions/ (confirmed via `new BreakGlassService(` search) — dead TS. SECURITY DEFECT FOUND AND FIXED THIS ROUND: compliance.KeyHolderRegistryViewSet was IsAuthenticated-only with no organization/user filter (no organization_id column exists at all — user_id only) — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/break-glass-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "knowledge_base_articles",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) lib/services/support-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/core/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/core/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/support-service.ts","backend/core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "license_renewals",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/certification-management-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/certification-management-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "lmbp_compliance_alerts",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/lmbp-immigration-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/lmbp-immigration-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "lmbp_compliance_reports",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/lmbp-immigration-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/lmbp-immigration-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "lmbp_letters",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/lmbp-immigration-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/lmbp-immigration-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "location_deletion_log",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, LOCATION_AND_GEOFENCE family): write-only compliance evidence with no organization/user column and no legitimate TypeScript reader anywhere (git-grep confirmed zero routes read this table). Two writers exist: (1) deleteAllUserLocationData, invoked from DELETE /api/location/consent (self-service consent revocation, already correctly self-scoped to the caller's own id per the round-49/round-17 fix — no cross-user defect here); (2) deleteExpiredLocationData, documented as 'should run hourly via cron' but has ZERO real callers anywhere (no cron route wires it up) — a latent/unwired path per round-46 doctrine, not a live SYSTEM_SCHEDULE invocation. Since it stores only counts/date-ranges/deletion-type (no raw location data) and has no read surface, this is global append-only evidence rather than tenant-partitioned data.",
    supportingCapability: ["app/api/location/consent/route.ts","services/geofence-privacy-service.ts"],
    requiredRuntimePrivileges: ["INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "location_tracking",
    classification: "USER_RLS_REQUIRED",
    reason: "CLOSED round 56 (PRIVACY_SENSITIVE archetype): user_id NOT NULL. Reachable via app/api/location/{track,geofence,consent}/route.ts, same self-service userId derivation (getCurrentUser().id) verified for geofence_events — both tables are written by the same route family.",
    supportingCapability: ["lib/services/geofence-privacy-service.ts","lib/services/location-tracking-service.ts","services/geofence-privacy-service.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT"],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NORMAL",
  },
  {
    table: "location_tracking_config",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, LOCATION_AND_GEOFENCE family): a platform-wide singleton config row (db/schema/domains/compliance/geofence.ts's own comment: 'Location tracking configuration (global settings)'). Read/lazily-created only by the private getConfig() helper in services/geofence-privacy-service.ts, called internally from the self-service trackLocation/requestLocationConsent flows; values (backgroundTrackingAllowed=false, retention hours, etc.) are hardcoded defaults, never accepted from client input, and no route exposes an update endpoint for this config. No tenant/user column exists and none is needed — this genuinely means the same thing platform-wide.",
    supportingCapability: ["services/geofence-privacy-service.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "lrb_agreements",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) lib/services/external-data/lrb-unified-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/external-data/lrb-unified-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "lrb_employers",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'lrbEmployers' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "lrb_unions",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'lrbUnions' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "mentorships",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/lmbp-immigration-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/lmbp-immigration-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "movement_trends",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, DERIVED_MOVEMENT_INSIGHT family): db/schema/domains/marketing.ts's movementTrends has no organizationId column BY DESIGN — organizationsContributing is an aggregate integer count, not an FK, and rows represent cross-union statistics ('anonymized cross-union trends... No individual organization identifiable, Minimum 5 organizations per trend', per the page's own doctrine comment). Round-48 derived-data lineage doctrine applied: the source is inherently cross-organizational, not one tenant's private data re-labelled global, so tenant confidentiality does not carry through to the aggregate. Reads are gated by requireUser() + an explicit officer-level MOVEMENT_INSIGHTS_ROLES allow-list (admin/president/vice_president/secretary_treasurer/chief_steward/officer/national_officer/fed_executive/fed_staff/clc_executive/clc_staff/system_admin) in both app/[locale]/dashboard/movement-insights/page.tsx and its export/page.tsx sibling — an ordinary 'member' cannot view this. No application code anywhere INSERTs/UPDATEs/DELETEs this table (git-grep confirmed) — rows are populated entirely out-of-band (an external aggregation process, not part of this app's runtime); the separate data_aggregation_consent table (has its own organizationId, correctly tenant-scoped) gates whether a viewing org has opted in, shown as a UI prompt, not a hard read gate. Django: backend/content/views.py MovementTrendsViewSet allowed full CRUD to ANY authenticated user of ANY org with no officer-role gate at all and no legitimate consumer of its own — contained via DenyAllPermission.",
    supportingCapability: ["app/[locale]/dashboard/movement-insights/export/page.tsx","app/[locale]/dashboard/movement-insights/page.tsx","backend/content/views.py"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "negotiation_sessions",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 44 (RESOLVED_PARENT_ROOT candidate; parent negotiations is closed TENANT_RLS_REQUIRED, but the child itself has no real consumer): lib/services/negotiations-service.ts's listSessions has zero real callers anywhere in app/, actions/, lib/, services/ — in fact ALL 7 exports of this service module (listNegotiations, getNegotiationById, listProposals, getProposalById, listTentativeAgreements, listSessions, listTeamMembers) have zero real callers (git-grep-confirmed round 44); the app's real negotiations/proposals/tentative-agreements routes use crud-factory or a dedicated custom implementation (see tentative_agreements, round 42) instead of this service. DEFECT FOUND AND FIXED THIS ROUND: NegotiationSessionsViewSet (backend/bargaining/views.py) was router-registered with queryset=Model.objects.all() + IsAuthenticated-only and NO organization filter — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/negotiations-service.ts","backend/bargaining/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pack_verification_log",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'packVerificationLog' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "policy_evaluations",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 44 (resolves the round-42 exception): the INSERT path remains safely parent-verified — lib/services/policy-engine.ts's evaluate() (fixed round 41 to filter policyRules by organizationId) inserts policyEvaluations rows only for rule.id values already drawn from that org-scoped rule set. SECURITY DEFECT FOUND AND FIXED THIS ROUND: the sole SELECT path, app/api/governance/telemetry/route.ts, read an org-UNSCOPED aggregate COUNT of policyEvaluations (and governanceEvents) behind only basic authenticateUser() (no role/platform-admin gate) — any authenticated user of any organization could see a platform-wide denial count. Fixed: the DB-backed cross-organization aggregate now requires isSystemAdmin() (lib/api-auth-guard.ts); ordinary authenticated callers fall back to the safe in-process-only counters (matching the round-30 strike_fund_disbursements safe-zeroed-response precedent). This yields a genuinely MIXED authority model: TENANT_USER/TENANT_RUNTIME for the org-scoped insert, PLATFORM_ADMIN/SYSTEM_RUNTIME for the now-gated cross-org read. The generated PolicyEvaluationsViewSet (backend/compliance/views.py) has no legitimate consumer and is contained via DenyAllPermission.",
    supportingCapability: ["app/api/governance/telemetry/route.ts","lib/services/policy-engine.ts","lib/api-auth-guard.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: ["INSERT"],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "NONE",
  },
  {
    table: "policy_exceptions",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 44 (RESOLVED_PARENT_ROOT:HIGH-confidence, parent policy_rules already closed TENANT_RLS_REQUIRED round 41): no organizationId column; authority derived through rule_id -> policy_rules. lib/services/policy-engine.ts's private checkException(ruleIds, ...) is called only from evaluate() (app/api/governance/policies/evaluate/route.ts, minRole via withOrganizationAuth) with ruleIds already drawn exclusively from the org-filtered policyRules query — so the ruleId IN (...) predicate can never reference another organization's rule. Read-only (SELECT); no insert/update/delete path exists. DEFECT FOUND AND FIXED THIS ROUND: PolicyExceptionsViewSet (backend/compliance/views.py) was router-registered with queryset=Model.objects.all() + IsAuthenticated-only and NO organization filter — contained via DenyAllPermission (no legitimate Django consumer found).",
    supportingCapability: ["app/api/governance/policies/evaluate/route.ts","lib/services/policy-engine.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
    {
    table: "precedent_citations",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: zero real (non-financial-service-duplicate-schema) callers of precedentCitations found in app/, actions/ — dead TS. SECURITY DEFECT FOUND AND FIXED THIS ROUND: bargaining.PrecedentCitationsViewSet was IsAuthenticated-only with no organization filter — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY. Citation visibility (once a real consumer exists) must not exceed the cited precedent's own visibility — documented for the future implementer.",
    supportingCapability: ["backend/bargaining/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "precedent_tags",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 44 (RESOLVED_PARENT_ROOT candidate; parent arbitration_precedents is closed TENANT_RLS_REQUIRED, but the child itself has no real consumer): precedentTags has zero real TS references anywhere in app/, actions/, lib/, services/ — only services/financial-service's own separate dual-schema declares it (a different deployable Node service, matching the round-41/42/43 dual-schema precedent). DEFECT FOUND AND FIXED THIS ROUND: PrecedentTagsViewSet (backend/bargaining/views.py) was router-registered with queryset=Model.objects.all() + IsAuthenticated-only and NO organization filter — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead/separate-service-only TS, no legitimate consumer.",
    supportingCapability: ["backend/bargaining/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pricing_discount_rules",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): same services/platform-economics/pricing-template-service.ts as the now-closed pricing_templates root; written only inside the dead seedGtmTemplates seed function, which has zero real callers (see pricing_templates entry). No Django exposure.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "pricing_regional_deployments",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): same services/platform-economics/pricing-template-service.ts as the now-closed pricing_templates root; written only inside the dead seedGtmTemplates seed function, which has zero real callers (see pricing_templates entry). No Django exposure.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "privacy_breaches",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, PRIVACY_AND_JURISDICTION family): db/schema/provincial-privacy-schema.ts's privacyBreaches has no organizationId column (reportedBy is a user id, affectedProvince is province-wide, not tenant-scoped) — this is a platform-wide breach registry by design, not per-tenant state. However, its ONLY TypeScript writers/readers — services/provincial-privacy-service.ts's reportBreach, markBreachNotificationSent, getBreachesApproachingDeadline, and the private triggerUrgentBreachNotification — have ZERO production callers anywhere in app/, actions/, lib/, services/ outside their own test file (git-grep confirmed); the real routes that import this service file (app/api/emergency/{dashboard,pipeda}/route.ts) call entirely different, DB-free functions from a SEPARATE module (lib/services/provincial-privacy-service.ts's getPrivacyRules/assessBreachNotification/generateComplianceReport), never the class methods that touch this table. Dead TS code. Django: backend/compliance/views.py PrivacyBreachesViewSet was a separate unscoped IsAuthenticated-only surface with no legitimate consumer — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/provincial-privacy-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "procurement_bids",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/joint-trust-fmv-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/joint-trust-fmv-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "procurement_requests",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/joint-trust-fmv-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/billing/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via SharedDenyAllPermission in backend/billing/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/joint-trust-fmv-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "provincial_data_handling",
    classification: "USER_RLS_REQUIRED",
    reason: "CLOSED round 56 (PRIVACY_SENSITIVE archetype): user_id NOT NULL. Reachable via app/api/privacy/consent/route.ts and app/api/emergency/{dashboard,pipeda}/route.ts (services/provincial-privacy-service.ts), same reachability chain verified for provincial_consent.",
    supportingCapability: ["services/provincial-privacy-service.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE"],
    requiredSystemPrivileges: ["SELECT"],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NORMAL",
  },
  {
    table: "provincial_privacy_config",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, PRIVACY_AND_JURISDICTION family): db/schema/provincial-privacy-schema.ts's provincialPrivacyConfig has no organizationId/user column — one row per province (AB/BC/ON/QC/etc.), the same PIPEDA/provincial-law reference data for every organization. The only real reader is services/provincial-privacy-service.ts's getProvinceConfig (SELECT, with a hardcoded-defaults fallback when no row exists), called internally from recordConsent via the real, authenticated POST /api/privacy/consent route (requireApiAuth). No production code anywhere ever INSERTs/UPDATEs this table (git-grep confirmed zero writers) — rows are reference data maintained outside application runtime. Django: backend/compliance/views.py ProvincialPrivacyConfigViewSet was a separate unscoped IsAuthenticated-only read+write surface with no legitimate consumer of its own — contained via DenyAllPermission (the read-only getProvinceConfig path above is the sole legitimate surface).",
    supportingCapability: ["services/provincial-privacy-service.ts","app/api/privacy/consent/route.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "push_deliveries",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence): no organization_id column; NOT NULL FKs to pushNotifications and pushDevices, BOTH already CLOSED TENANT_RLS_REQUIRED (round 46, which also independently found services/fcm-service.ts's FCMService has zero importers anywhere). Re-verified this round: lib/engagement-scoring.ts (dead code, same finding as newsletter_engagement round 47) and services/fcm-service.ts (only imported by lib/workers/notification-worker.ts, itself confirmed to have zero bootstrap/instantiation callers anywhere — same dead-worker pattern as push_devices round 46) are BOTH fully unreachable — no real TS caller touches this table. Django PushDeliveriesViewSet (backend/notifications/views.py) was IsAuthenticated-only with queryset=Model.objects.all() — contained via DenyAllPermission this round. No legitimate consumer found on either side.",
    supportingCapability: ["lib/engagement-scoring.ts","services/fcm-service.ts","backend/notifications/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "recovery_time_objectives",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/break-glass-service.ts, services/force-majeure-integration.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/break-glass-service.ts","services/force-majeure-integration.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "recusal_tracking",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: services/founder-conflict-service.ts's FounderConflictService class (the sole reader/writer of this table and its 2 siblings) has ZERO real instantiations anywhere in app/, actions/ (confirmed via `new FounderConflictService(` search) — dead TS. SECURITY DEFECT FOUND AND FIXED THIS ROUND: compliance.RecusalTrackingViewSet was IsAuthenticated-only with no organization/user filter (no organization_id column exists at all — user_id only) — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/founder-conflict-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "segment_executions",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'segmentExecutions' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "shared_clause_library",
    classification: "MULTI_PARTY_RLS_REQUIRED",
    reason: "CLOSED round 55 (final non-voting parent architecture exceptions \u2014 OWNER_PLUS_EXPLICIT_SHARING_AUTHORITY): 6 real production routes derived (app/api/clause-library/route.ts GET+POST, [id]/route.ts GET+PATCH+DELETE, [id]/share/route.ts GET+POST, [id]/tags/route.ts GET+POST+DELETE, search/route.ts GET+POST, compare/route.ts POST). ALL SIX had ZERO organization/sharing authorization prior to this round \u2014 concrete defects found and fixed: (1) list/search returned every clause in the table regardless of sharingLevel or organization \u2014 fixed via lib/clause-library/sharing-authority.ts's buildClauseVisibilityCondition, a SQL-level OR-condition (owner OR sharingLevel='public' OR sharingLevel='private' AND caller \u2208 sharedWithOrgIds OR sharingLevel='federation' AND same federation ancestor OR sharingLevel='congress' AND both orgs CLC-affiliated with an active congress_memberships row), joined against organizations (hierarchyPath/clcAffiliated) + congressMemberships (active) so the predicate is exact per-row, not a fetch-then-filter approximation. (2) POST create trusted body.sourceOrganizationId directly \u2014 any steward of ANY org could create a clause 'owned' by an arbitrary organization \u2014 fixed to always stamp the authenticated organizationId. (3) GET/PATCH/DELETE [id] had no ownership/sharing check at all \u2014 any steward could read, edit, or delete any other organization's clause \u2014 fixed: GET gated by canReadSharedClause (owner or a sharing-level match), PATCH/DELETE owner-only regardless of sharingLevel (broad reader visibility never implies write \u2014 verified via a pre-mutation SELECT+ownership-check, not just a WHERE-clause miss). (4) [id]/share GET+POST (the sharingLevel/sharedWithOrgIds configuration itself) had no check at all, letting ANY steward view or CHANGE any clause's sharing settings \u2014 fixed to owner-only for both GET and POST (sharing configuration is owner-internal, not shared clause content \u2014 sharedWithOrgIds is also now redacted from the [id] GET response for non-owner readers). (5) compare/route.ts read full clause text/tags and bumped comparisonCount for every requested id with zero check \u2014 fixed to silently exclude unauthorized ids (not an existence oracle) and only increment comparisonCount on the authorized subset. Owner root: sourceOrganizationId (NOT NULL FK to organizations). Real sharing-level vocabulary (verified against components/clause-library/ClauseSharingControls.tsx, not invented): private (owner + explicit sharedWithOrgIds grants), federation (same federation ancestor via organizations.hierarchyPath/organizationType), congress (both orgs CLC-affiliated + active congress_memberships row, matching lib/auth/hierarchy-access-control.ts's validateSharingLevel congress check), public (any authenticated org). MULTI_PARTY_RLS_REQUIRED (not simple tenant-scoped) because a single row's READ authority legitimately spans multiple organizations depending on sharingLevel, while WRITE authority remains single-party (owner) \u2014 existing TENANT_RLS_REQUIRED/PARENT_OWNED_RLS_REQUIRED vocabulary cannot truthfully express cross-org read fan-out; matches the existing per_capita_remittances precedent for this classification. RLS_POLICY_HELPER_REQUIRED (deployment debt, not a classification-blocking defect): no PostgreSQL RLS policy shape yet exists for the federation-ancestor / congress-membership predicates used in the application-layer check \u2014 an eventual DB-level policy must express the identical OR-condition in lib/clause-library/sharing-authority.ts's buildClauseVisibilityCondition, not a simplified owner-only or all-tenant-read approximation. Django SharedClauseLibraryViewSet contained via DenyAllPermission this round (was IsAuthenticated-only, queryset=Model.objects.all(), no legitimate consumer \u2014 reproducing this sharing model in Django is out of scope).",
    supportingCapability: ["app/api/clause-library/route.ts","app/api/clause-library/[id]/route.ts","app/api/clause-library/[id]/share/route.ts","app/api/clause-library/[id]/tags/route.ts","app/api/clause-library/search/route.ts","app/api/clause-library/compare/route.ts","lib/clause-library/sharing-authority.ts"],
    requiredRuntimePrivileges: ["SELECT", "INSERT", "UPDATE", "DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "signature_verification",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence): no organization_id column; NOT NULL FKs to signatureWorkflows and signers (both still NEEDS_REVIEW, moot given reachability below). Sole writer lib/services/signature-workflow-service.ts's handleSignerCompleted() has ZERO real callers anywhere — the whole module is NOT re-exported from lib/services/index.ts (which explicitly comments 'import directly from file') and has no direct importer either (git-grep confirmed) — fully dead TS code. Django SignatureVerificationViewSet (backend/content/views.py) was IsAuthenticated-only with queryset=Model.objects.all() — contained via DenyAllPermission this round (same pattern as the sibling SignatureAuditLogViewSet in the same file, round 49). No legitimate consumer found on either side.",
    supportingCapability: ["lib/services/signature-workflow-service.ts","backend/content/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "signers",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 54 (final non-voting parent-owned authority convergence — supersedes the round-42 PARENT_OWNED ejection): re-derived from scratch, not from naming. This is a DIFFERENT table from documentSigners (which closed PARENT_OWNED_RLS_REQUIRED this round) — signers (db/schema/domains/documents/workflows.ts) belongs to the separate services/pki/** PKI/e-signature subsystem. Traced every function touching it: services/pki/signature-service.ts's createSignatureRequest/getUserSignatureRequests/completeSignatureRequestStep/cancelSignatureRequest/expireOverdueSignatureRequests/rejectSignature/getDocumentSignatures ALL have zero real callers — the ONE reachable route into that file, app/api/admin/pki/signatures/[id]/sign/route.ts, only calls signDocument() (writes the separate digitalSignatures table) and workflow-engine.ts's recordSignature() (mutates JSONB steps[].signers embedded in signatureWorkflows, NOT this table). lib/services/signature-workflow-service.ts's createSignatureWorkflow() (also touches this table) is likewise dead — not re-exported from lib/services/index.ts and no direct importer. The lib/api/signature-service-api.ts references found in earlier scans are a generated Django-HTTP-client fetch wrapper hitting a DIFFERENT Django custom-action endpoint, not calls into these TS functions. Django SignersViewSet (backend/content/views.py) was IsAuthenticated-only with queryset=Model.objects.all() — contained via DenyAllPermission this round. No legitimate consumer found on either side.",
    supportingCapability: ["services/pki/signature-service.ts","lib/services/signature-workflow-service.ts","backend/content/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "sla_policies",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) lib/services/support-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/core/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/core/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/support-service.ts","backend/core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "staff_certifications",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: services/certification-management-service.ts's CertificationManagementService class (the sole reader/writer of this table and its 2 siblings) has ZERO real instantiations anywhere in app/, actions/ (confirmed via `new CertificationManagementService(` search) — dead TS. The `continuing_education` string match in components/education/admin/ApprenticeshipManager.tsx is a UI category label, not a table reference — that component's real fetch calls target /api/education/programs, an unrelated table. SECURITY DEFECT FOUND AND FIXED THIS ROUND: compliance.StaffCertificationsViewSet was IsAuthenticated-only with no organization/user filter (no organization_id column exists at all — user_id only) — contained via DenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/certification-management-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "t106_filing_tracking",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: same dead TransferPricingService class as bank_of_canada_rates. SECURITY DEFECT FOUND AND FIXED THIS ROUND: billing.T106FilingTrackingViewSet was IsAuthenticated-only with no organization filter (no organization_id column exists at all) — contained via SharedDenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/transfer-pricing-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "tentative_agreements",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 42 (PARENT_OWNED:parent:HIGH), with a fix applied first: no direct organization_id column; authority derived through negotiation_id -> negotiations (closed round 41, TENANT_RLS_REQUIRED). DEFECT FOUND AND FIXED THIS ROUND: app/api/bargaining/tentative-agreements/route.ts used lib/api/crud-factory.ts with orgScoped:true, but crud-factory's org filter only applies to a table's OWN organizationId column — since tentativeAgreements has none, orgScoped:true was a silent no-op, exposing every organization's tentative collective-bargaining agreements (GET) and allowing creation against any negotiationId (POST) to any authenticated member. Replaced with an explicit implementation: GET inner-joins negotiations and filters eq(negotiations.organizationId, organizationId); POST verifies and(eq(negotiations.id, negotiationId), eq(negotiations.organizationId, organizationId)) before insert. No Django model exists for this table.",
    supportingCapability: ["app/[locale]/dashboard/bargaining/negotiations/[id]/page.tsx","app/api/bargaining/tentative-agreements/route.ts","lib/services/negotiations-service.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "testimonials",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "RESOLVED (round 11): app/api/testimonials/[id]/route.ts uses crudRoutes({ orgScoped: false }) for genuinely global, non-tenant marketing content (public proof-page testimonials). Same follow-up flagged as case_studies (db/rls-storage-authority/claims-continuity.ts): writeRole:'admin' resolves via withApi's minRole mechanism, checking the caller's role WITHIN THEIR OWN ORGANIZATION — any tenant's own org-admin can create/edit/delete this platform-wide content, not just platform marketing staff. Content-integrity risk, not a tenant-data confidentiality breach; tracked for follow-up.",
    supportingCapability: ["app/[locale]/(marketing)/proof/page.tsx","app/api/testimonials/[id]/route.ts","lib/integrations/marketing-notifications.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "ticket_comments",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 44 (PARENT_OWNED:parent:NORMAL candidate; ineligible for the fast-path since its parent support_tickets was just corrected to SYSTEM_ONLY, not a tenant-owned root — but closed on its OWN independent reachability evidence, not by inheriting the parent's status): lib/services/support-service.ts's addComment (the sole INSERT writer) and getTicketComments (the sole SELECT reader) have zero real callers anywhere in app/, actions/, lib/, services/, components/ outside their own test file (git-grep-confirmed round 44) — dead code, independent of support_tickets' own real platform-admin dashboard consumers (which never touch ticket_comments). DEFECT FOUND AND FIXED THIS ROUND: TicketCommentsViewSet (backend/core/views.py) was router-registered ('ticket-comments') with queryset=Model.objects.all() + IsAuthenticated-only and NO organization filter — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/support-service.ts","backend/core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "ticket_history",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 44 (PARENT_OWNED:parent:NORMAL candidate; ineligible for the fast-path since its parent support_tickets was just corrected to SYSTEM_ONLY, not a tenant-owned root — but closed on its OWN independent reachability evidence, not by inheriting the parent's status): lib/services/support-service.ts's ticketHistory INSERT calls (createTicket/updateTicket/assignTicket/resolveTicket/closeTicket) have zero real callers anywhere in app/, actions/, lib/, services/, components/ outside their own test file (git-grep-confirmed round 44) — dead code, independent of support_tickets' own real platform-admin dashboard consumers (which never touch ticket_history). DEFECT FOUND AND FIXED THIS ROUND: TicketHistoryViewSet (backend/core/views.py) was router-registered ('ticket-history') with queryset=Model.objects.all() + IsAuthenticated-only and NO organization filter — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/support-service.ts","backend/core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "traditional_knowledge_registry",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/indigenous-data-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/indigenous-data-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "transaction_currency_conversions",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57: BOTH backing files are dead — services/transfer-pricing-service.ts's TransferPricingService class (zero real instantiations) and lib/services/multi-currency-treasury-service.ts (zero real callers of any exported function). SECURITY DEFECT FOUND AND FIXED THIS ROUND: billing.TransactionCurrencyConversionsViewSet was IsAuthenticated-only with no organization filter — contained via SharedDenyAllPermission. No legitimate consumer exists. Satisfies CONTAINED_NO_AUTHORITY.",
    supportingCapability: ["services/transfer-pricing-service.ts","lib/services/multi-currency-treasury-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "union_density",
    classification: "SYSTEM_ONLY",
    reason: "CLOSED round 46 (SYSTEM_AND_MIXED_EXECUTION_AUTHORITY cohort, mechanically discovered via a broader cron/worker/webhook hint sweep, not the original known-4 list): no organization_id column in either declaration (db/schema/domains/data/benchmarks.ts — confirmed ORPHANED/dead, zero importers anywhere; db/schema/wage-benchmarks-schema.ts — the LIVE one, re-exported via db/schema/domains/data/index.ts -> db/schema/index.ts). Genuinely global Statistics Canada reference data. SYSTEM PATH: app/api/cron/external-data-sync/route.ts (withApi, auth.cron=true, a real registered weekly-scheduled cron root per its docstring) -> wageEnrichmentService.syncUnionDensity() — SELECT/INSERT/UPDATE, idempotency key (geographyCode, unionStatus, refDate). TENANT-INITIATED READ: app/[locale]/dashboard/data-source/page.tsx calls requireUser() (any authenticated user, no role/org restriction) then executes its query entirely inside withSystemContext() — the human caller can be a tenant user, but execution never touches the tenant runtime/RLS-gated connection, matching the icra_continuity_scores precedent's shape (tenant-initiated request, fully SYSTEM_RUNTIME execution). SECURITY DEFECT FOUND AND FIXED this round: syncUnionDensity() (and its shared createSyncLog/updateSyncLog helper calls) executed on the plain tenant `db` import despite being SYSTEM_SCHEDULE-invoked — a principal mismatch (doctrine section 22). Fixed by wrapping the whole method body in withSystemContext(); the sibling syncWageData/syncCOLAData/syncContributionRates methods in the same class (write to wage_benchmarks/cost_of_living_data/contribution_rates, none in this round's cohort) were deliberately NOT touched, staying in scope. lib/services/external-data/__tests__/wage-enrichment-service.test.ts updated to mock withSystemContext (14/14 tests passing across both affected test files). Django UnionDensityViewSet defaulted to full CRUD for any authenticated user (IsAuthenticated-only) — contained via a new DenyAllPermission (analytics app had none yet); write access there could have let any user corrupt reference data shared by every organization.",
    supportingCapability: ["app/api/cron/external-data-sync/route.ts","lib/services/external-data/wage-enrichment-service.ts","app/[locale]/dashboard/data-source/page.tsx"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: ["SELECT","INSERT","UPDATE"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "union_only_data_tags",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 51 (GLOBAL_REFERENCE_AND_SHARED_CONFIGURATION_SCOPE_AUTHORITY, no-org candidate universe): sole TS consumer(s) services/employer-non-interference-service.ts have zero production importers anywhere in app/, actions/, lib/, services/ outside their own test files (git-grep confirmed) — dead TS code, no legitimate consumer on the TypeScript side. The generated Django ViewSet(s) in backend/compliance/views.py exposed queryset=Model.objects.all() + IsAuthenticated-only with NO organization filter of any kind. Contained this round via DenyAllPermission in backend/compliance/views.py. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/employer-non-interference-service.ts","backend/compliance/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "user_uuid_mapping",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, CROSS_SERVICE_IDENTITY_MAPPING family): db/schema/domains/infrastructure/uuid-mapping.ts's userUuidMapping bridges an auth-provider user id (a legacy column storing both a prior provider's identifier and current Entra Object IDs) to an internal UUID for FK relationships. VERIFIED DATABASE BOUNDARY (round-52 doctrine section 47, not assumed from folder names): services/financial-service is a genuinely SEPARATE deployable microservice — its own package.json ('Microservice for union financial operations'), its own drizzle.config.ts, and its own DATABASE_URL pointing at a distinct Supabase Postgres project (see services/financial-service/.env.example) — so its same-named userUuidMapping/arbitratorProfiles declarations are a genuine SEPARATE_DATABASE_BOUNDARY, not evidence about THIS app's own table (round-48 precedent: duplicate schema declarations are not automatically the same DB). Scoped strictly to this app's own table: lib/utils/user-uuid-helpers.ts's getOrCreateUserUuid (the ONLY code in this app touching this table — a single 33-line file, one exported function, no update/delete/reassignment path at all) has ZERO production callers anywhere (git-grep confirmed) — dead TS code. Django: backend/auth_core/views.py UserUuidMappingViewSet allowed full CRUD (including UPDATE, an identity-reassignment/account-takeover-equivalent operation on this exact bridge table — round-52 doctrine section 49) to any authenticated user with no legitimate consumer of its own — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/utils/user-uuid-helpers.ts","backend/auth_core/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "voter_eligibility",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 44 (RESOLVED_PARENT_ROOT:HIGH-confidence, parents voting_sessions + organization_members already closed TENANT_RLS_REQUIRED): no organizationId column; authority derived through session_id -> voting_sessions and member_id -> organization_members. The only reachable path is lib/services/voting-service.ts's checkVoterEligibility(sessionId, memberId), called internally from castVote() (app/api/governance/elections/sessions/[id]/vote/route.ts's POST, already verified round 42 to check the session belongs to the caller's organization before this point) — a narrow point-lookup keyed on the exact (sessionId, memberId) pair, with no listing/enumeration surface exposed to the client. addVoterEligibility/bulkAddVoterEligibility/updateVoterEligibility (the write paths) have zero real callers anywhere (no admin route manages eligibility lists) — dead code, so requiredRuntimePrivileges is SELECT-only despite the schema supporting writes. DEFECT FOUND AND FIXED THIS ROUND: VoterEligibilityViewSet (backend/unions/views.py) was router-registered with full CRUD, queryset=Model.objects.all() + IsAuthenticated-only and NO organization filter — contained via DenyAllPermission.",
    supportingCapability: ["app/api/governance/elections/sessions/[id]/vote/route.ts","lib/services/voting-service.ts","backend/unions/views.py"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
    {
    table: "votes",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 57 (final voting-integrity resolution): no direct organization_id; scopes through votingSessions.organizationId (session_id FK, NOT NULL). Django unions.VotesViewSet already DenyAll-contained (round 56). CROSS-ELECTION OPTION INJECTION: already prevented — both app/api/voting/sessions/[id]/vote/route.ts (via castVote()) and app/api/governance/elections/sessions/[id]/vote/route.ts validate `eq(votingOptions.id, optionId) AND eq(votingOptions.sessionId, sessionId)` before inserting. BALLOT SECRECY: voterId is an anonymized HMAC derived from (memberId, sessionId) via lib/services/voting-service.ts's generateAnonymousVoterId() — the real voter identity is never stored directly when isAnonymous=true; voting_sessions.allowAnonymous governs the product-level policy. SECURITY DEFECTS FOUND AND FIXED THIS ROUND: (1) generateAnonymousVoterId() embedded Date.now() in the HMAC input, making voterId non-deterministic across calls — silently defeating the 'already voted' duplicate-vote check on EVERY vote (a member could vote unlimited times); fixed by deriving voterId deterministically from (memberId, sessionId) only. (2) A DB-level unique index votes_session_voter_unique on (session_id, voter_id) was added (db/schema/domains/governance/voting.ts) for defense-in-depth against a concurrent-request race that a SELECT-then-INSERT check alone cannot close. (3) app/api/governance/elections/sessions/[id]/vote/route.ts accepted a client-supplied voterId and NEVER checked voter_eligibility at all — a caller could impersonate/duplicate ballots by supplying an arbitrary voterId; fixed to delegate to the same castVote() (server-derived voterId + real eligibility check) used by the sibling route. VOTE LIFECYCLE: insert-only at the API surface — no PATCH/DELETE route exists for votes (immutable once cast, matching the append-only ballot model). Regression tests added (voting-service.test.ts, governance-elections-session-vote-idor-fix.route.test.ts).",
    supportingCapability: ["lib/services/voting-service.ts","app/api/voting/sessions/[id]/vote/route.ts","app/api/governance/elections/sessions/[id]/vote/route.ts","db/schema/domains/governance/voting.ts","backend/unions/views.py"],
    requiredRuntimePrivileges: ["SELECT","INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
    {
    table: "voting_options",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED round 57: no direct organization_id; scopes through votingSessions.organizationId (session_id FK, NOT NULL). Django unions.VotingOptionsViewSet already DenyAll-contained (round 56). MUTATION LIFECYCLE: confirmed no live API route exposes INSERT/UPDATE/DELETE for voting_options — only SELECT (app/api/voting/sessions/route.ts, app/api/governance/elections/sessions/[id]/{vote,results}/route.ts, app/api/voting/verify/route.ts). lib/services/voting-service.ts's addVotingOption/updateVotingOption/deleteVotingOption exist but have zero real route callers (latent capability, not a live mutation surface) — so there is currently no live-election option-tampering risk to remediate, though a future admin route must enforce a 'cannot mutate once the session is active' guard before exposing writes.",
    supportingCapability: ["app/api/voting/sessions/route.ts","app/api/governance/elections/sessions/[id]/results/route.ts","backend/unions/views.py"],
    requiredRuntimePrivileges: ["SELECT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "wage_benchmarks",
    classification: "GLOBAL_REFERENCE_DATA",
    reason: "CLOSED round 52 (NON_FINANCE_SCOPE_EXCEPTION_REMEDIATION, EXTERNAL_REFERENCE_AND_SYNC family): externally-sourced Statistics Canada wage reference data (keyed on nocCode+geographyCode+refDate+sex) with no organizationId column — same meaning for every organization. Sole writer is lib/services/external-data/wage-enrichment-service.ts's syncWageData, invoked exclusively via the real CRON_SECRET-gated app/api/cron/external-data-sync/route.ts (git-grep confirmed zero other callers). DEFECT FOUND AND FIXED THIS ROUND: syncWageData previously ran on the plain tenant `db` import despite being SYSTEM_SCHEDULE-invoked; now wrapped in withSystemContext (same round-46 pattern already applied to syncUnionDensity). Existing rows are period-keyed by refDate (each survey period gets its own natural-keyed row, updated in place only for the SAME period, not overwriting prior periods' history). Regression test added (lib/services/external-data/__tests__/wage-enrichment-service.test.ts) proving withSystemContext is invoked. Django: backend/analytics/views.py WageBenchmarksViewSet was a separate unscoped IsAuthenticated-only surface with no legitimate consumer — contained via DenyAllPermission.",
    supportingCapability: ["lib/services/external-data/wage-enrichment-service.ts","app/api/cron/external-data-sync/route.ts","backend/analytics/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: ["SELECT","INSERT","UPDATE"],
    invocationAuthority: "SYSTEM_SCHEDULE",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "wage_progressions",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 44 (RESOLVED_PARENT_ROOT candidate; parents collective_agreements + cba_clauses are both closed TENANT_RLS_REQUIRED, but the child itself has no real consumer): lib/services/clause-service.ts's getWageProgressions/createWageProgression have zero real callers anywhere in app/, actions/, lib/, services/ (git-grep-confirmed round 44). DEFECT FOUND AND FIXED THIS ROUND: WageProgressionsViewSet (backend/bargaining/views.py) was router-registered with queryset=Model.objects.all() + IsAuthenticated-only and NO organization filter — contained via DenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["lib/services/clause-service.ts","backend/bargaining/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "wcag_success_criteria",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'wcagSuccessCriteria' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
    {
    table: "weekly_threshold_tracking",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 57 (resolves round-56's REACHABILITY_VERIFICATION_INCOMPLETE exception): services/tax-slip-service.ts's TaxSlipService.generateT4ASlips()/generateRL1Slips() (the sole writers/readers of weeklyThresholdTracking) have ZERO real callers — locked by services/__tests__/round34-dead-writer-reachability-lock.test.ts. SECURITY DEFECT FOUND AND FIXED THIS ROUND: billing.WeeklyThresholdTrackingViewSet was router-registered with IsAuthenticated-only and no organization/user filter (no organization_id column exists at all — user_id only) — contained via SharedDenyAllPermission. Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/tax-slip-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "whiplash_violations",
    classification: "CONTAINED_NO_AUTHORITY",
    reason: "CLOSED round 44 (same whiplash cluster as round 42's account_balance_reconciliation/payment_routing_rules/separated_payment_transactions): services/whiplash-prevention-service.ts's WhiplashPreventionService class (the sole reader/writer of whiplashViolations) has ZERO real instantiations anywhere in app/, actions/, lib/, services/ outside its own test file (git-grep-confirmed round 44 for 'WhiplashPreventionService' and 'new WhiplashPrevention') — dead code. No raw-SQL reference to the physical table name exists outside db/schema/**, db/migrations/**, and this manifest. DEFECT FOUND AND FIXED THIS ROUND: WhiplashViolationsViewSet (backend/billing/views.py) was router-registered with queryset=Model.objects.all() + IsAuthenticated-only — contained via SharedDenyAllPermission (the same mixin used for the round-42 whiplash entries). Satisfies CONTAINED_NO_AUTHORITY: reachable-but-denied Django, dead TS, no legitimate consumer.",
    supportingCapability: ["services/whiplash-prevention-service.ts","backend/billing/views.py"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "workbook_continuity_breakpoints",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'workbookContinuityBreakpoints' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "workbook_memory_holders",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority — direct child of the now-closed workbooks root, USER_RLS_REQUIRED): no direct ownership column; authority derives entirely through workbookId -> workbooks. SECURITY DEFECT FOUND AND FIXED here (see the workbooks entry for the full write-up): the API routes and the page's own server component previously checked only workbook EXISTENCE, not the claimed-ownership rule required once a workbook is identity-linked. Fixed via the shared lib/workbook/access-control.ts helper (verifyClaimedWorkbookAccess), applied to GET/POST (route.ts), PATCH/DELETE ([holderId]/route.ts), and the memory-holders page server component; regression tests added for all three.",
    supportingCapability: ["app/api/workbook/[id]/memory-holders/route.ts","app/api/workbook/[id]/memory-holders/[holderId]/route.ts","app/[locale]/workbook/[id]/memory-holders/page.tsx","app/api/workbook/[id]/memory-holders/__tests__/route.test.ts","app/api/workbook/[id]/memory-holders/[holderId]/__tests__/route.test.ts","lib/workbook/access-control.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE","DELETE"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "workbook_modernization_alignment",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'workbookModernizationAlignment' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "workbook_modules",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority — direct child of the now-closed workbooks root): no direct ownership column; authority derives through workbookId -> workbooks. Sole write path is app/api/workbook/start/route.ts, seeding one row per WORKBOOK_MODULE_IDS immediately after creating the SAME freshly-generated workbookId in the same request (no client-suppliable workbookId, no cross-workbook injection possible). No separate read/update route exists for this table today (module status advancement is a documented product gap, not an authority defect) — the module hub page (app/[locale]/workbook/[id]/page.tsx) only reads workbooks.status/reportTierId, never workbook_modules directly.",
    supportingCapability: ["app/[locale]/workbook/start/page.tsx","app/api/workbook/start/route.ts"],
    requiredRuntimePrivileges: ["INSERT"],
    requiredSystemPrivileges: [],
    invocationAuthority: "TENANT_USER",
    dbExecutionPrincipal: "TENANT_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "workbook_purchases",
    classification: "PARENT_OWNED_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority — direct child of the now-closed workbooks root): no direct ownership column; authority derives through workbookId -> workbooks. Sole write path is app/api/payments/webhooks/stripe/route.ts (SYSTEM/WEBHOOK principal, not TENANT_USER) — verifies the Stripe signature (verifyStripeSignature) before processing, derives workbookId from the checkout session's own metadata (set server-side when WE created the session, never client input), and is idempotent via onConflictDoNothing on the unique stripePaymentRef. Classified PARENT_OWNED rather than ejected: the parent root (workbooks) is now proven, and the webhook-vs-tenant invocation distinction is exactly what dbExecutionPrincipal/invocationAuthority already exist to encode (SYSTEM_RUNTIME/WEBHOOK).",
    supportingCapability: ["app/api/payments/webhooks/stripe/route.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: ["INSERT"],
    invocationAuthority: "WEBHOOK",
    dbExecutionPrincipal: "SYSTEM_RUNTIME",
    reviewPriority: "NONE",
  },
  {
    table: "workbook_stewardship_signals",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'workbookStewardshipSignals' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "workbook_transformation_roadmap",
    classification: "LATENT_UNREACHABLE",
    reason: "Round-7 scope-expansion scan: zero references to the 'workbookTransformationRoadmap' Drizzle export found outside db/schema/**, __tests__/**, .test./.spec./.stories. files via a combined ripgrep pass across app/, actions/, lib/, services/. No known application code path queries this table today. NOT yet individually checked for raw-SQL (non-Drizzle-symbol) references to the physical table name — see the raw-SQL LATENT fixture test for why that check matters before this can be treated as final.",
    supportingCapability: [],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
  },
  {
    table: "workbooks",
    classification: "USER_RLS_REQUIRED",
    reason: "CLOSED (round 50, state-machine root and fan-out cascade authority): the Governance Entropy Workbook™ is a deliberately pseudonymous product (own doc comment: 'A workbook can exist with no user account'). No organization_id column. STATE MACHINE: status draft -> awaiting_claim (Stripe webhook, on purchase) -> active (claim) -> [no further transitions found]. OWNERSHIP MODEL (two phases, both proven safe): (1) UNCLAIMED — the workbookId itself is the bearer credential by design (app/api/workbook/start/route.ts creates with no auth, matching its own docstring 'No authentication required'); (2) CLAIMED — app/api/workbook/[id]/claim/route.ts requires authentication + a single-use claimToken verified against the exact row (eq(workbooks.id, workbookId) AND eq(workbooks.claimToken, claimToken)), rejects already-claimed/expired tokens, and nulls the token after use (replay-proof) — stamping claimedByUserId/claimedOrgId/claimedAt, matching this app's USER_RLS_REQUIRED archetype (member_location_consent, round 17) once claimed. SECURITY DEFECT FOUND AND FIXED: 3 surfaces that read/write IDENTITY-LINKED child data for a workbook only checked workbook EXISTENCE, not the claimed-ownership rule the product's own design already documented and already correctly implemented in app/api/workbook/[id]/export/route.ts — app/api/workbook/[id]/memory-holders/route.ts (GET+POST), its [holderId]/route.ts sibling (PATCH+DELETE), and app/[locale]/workbook/[id]/memory-holders/page.tsx (a server component querying workbookMemoryHolders, including free-text notes, directly with no auth at all). Any caller who ever held a workbookId (e.g. from the anonymous pre-claim phase, a shared link, or logs) could keep reading/editing/deleting institutional-succession data indefinitely after the workbook became identity-linked. Fixed by extracting the export route's proven ownership check into a shared lib/workbook/access-control.ts (verifyClaimedWorkbookAccess) and applying it to all 3 surfaces; 15 new tests across 3 test files.",
    supportingCapability: ["app/[locale]/workbook/[id]/memory-holders/page.tsx","app/[locale]/workbook/[id]/page.tsx","app/[locale]/workbook/start/page.tsx","app/api/payments/webhooks/stripe/route.ts","app/api/workbook/[id]/claim/route.ts","app/api/workbook/[id]/export/route.ts","app/api/workbook/[id]/memory-holders/route.ts","app/api/workbook/checkout/route.ts","app/api/workbook/start/route.ts","lib/workbook/access-control.ts","lib/workbook/__tests__/access-control.test.ts","lib/hubspot/syncWorkbookPurchase.ts","lib/oci/facilitation/facilitationGuide.ts","lib/workbook-pdf/generateWorkbookPdf.ts"],
    requiredRuntimePrivileges: ["SELECT","INSERT","UPDATE"],
    requiredSystemPrivileges: ["UPDATE"],
    invocationAuthority: "MIXED",
    dbExecutionPrincipal: "MIXED",
    reviewPriority: "NONE",
  },
]
