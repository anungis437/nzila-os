/**
 * db/rls-storage-authority/financial-service-latent.ts
 *
 * Round 58D — CANONICAL_DATABASE_CONVERGENCE_SUPABASE_PURGE_AND_FINAL_ENFORCEMENT_CUTOVER.
 *
 * Deterministic census (scripts/rls-enforcement/census-111-declarations.ts)
 * found 95 pgTable(...) declarations that exist ONLY under
 * services/financial-service/** (never duplicated in the canonical
 * db/schema/** tree - 0 duplicate-across-both-roots cases found) and had
 * no entry anywhere in the pre-58D manifest.
 *
 * Architecture finding (round 58D): services/financial-service has NO
 * current deployment path of any kind - no Dockerfile, no docker-compose
 * service entry, no Azure deploy workflow, no infrastructure/** Bicep or
 * Terraform resource. Its own CI ("financial-service-health" in
 * .github/workflows/cupe-pilot-readiness.yml) runs only typecheck/lint/
 * test, never a real migration apply or deployment. Its runtime DB
 * bootstrap (services/financial-service/src/db/index.ts) is a fully
 * generic `postgres` package connection against process.env.DATABASE_URL
 * - zero Supabase SDK usage despite the historical dependency (removed
 * this round, see reports/union-eyes-authority-enforcement-round58.md).
 *
 * 84 of these 95 names appear verbatim as `CREATE TABLE IF NOT EXISTS`
 * statements in services/financial-service/drizzle/0000_lucky_mole_man.sql
 * - a 175-table snapshot the manifest's own prior evidence (see the
 * "members" entry in analytics-ai.ts) already established was produced by
 * drizzle-kit INTROSPECTING the SAME production database the main
 * application uses, not an independent database. These 84 are therefore
 * treated as genuinely real physical relations in the canonical database
 * (classification: MIGRATED_SNAPSHOT below) - but with zero current live
 * consumer, since financial-service itself has no deployment path at all.
 *
 * The remaining 11 names do NOT appear in that migration snapshot at all
 * (classification: TS_ONLY_NEVER_MIGRATED below) - they read as a later,
 * separate SaaS-billing-analytics feature addition (accounts_payable,
 * budgets, budget_line_items, customer_acquisition, expense_approvals,
 * expense_requests, mrr_snapshots, revenue_cohorts, subscription_events,
 * vendor_invoices, vendors) with no evidence they were ever created in any
 * real database.
 *
 * Both groups receive the SAME classification and privileges here
 * (LATENT_UNREACHABLE, zero runtime/system privileges) because both share
 * the load-bearing fact that makes this classification correct: zero
 * current legitimate/reachable consumer. This is not a judgment that the
 * MIGRATED_SNAPSHOT group is unimportant - only that no live code path
 * reaches it today, exactly matching the existing, established
 * LATENT_UNREACHABLE pattern already used for ~199 other entries in this
 * manifest. If financial-service is ever actually deployed, EVERY entry in
 * this file must be re-opened and re-classified against its real,
 * deployed privilege requirements before that deployment ships.
 *
 * 19 of the 95 names use a "v_"-prefixed naming convention
 * (v_annual_remittance_summary, v_pending_remittances, etc.) that reads
 * like a SQL VIEW, but ALL 19 appear as literal `CREATE TABLE IF NOT
 * EXISTS` statements in the introspected snapshot - they are ordinary
 * base tables using a reporting-style naming convention, not actual
 * PostgreSQL view objects. No VIEW_AUTHORITY_REGISTRY entry is needed for
 * them.
 */
import type { StorageAuthorityEntry } from './types';

export const financialServiceLatentEntries: StorageAuthorityEntry[] = [
  {
    table: "accounts_payable",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"accounts_payable\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "ai_chunks",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"ai_chunks\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "ai_documents",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"ai_documents\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "ai_feedback",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"ai_feedback\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "ai_feedback_summary",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"ai_feedback_summary\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "ai_queries",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"ai_queries\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "ai_query_logs",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"ai_query_logs\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "ai_usage_by_tenant",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"ai_usage_by_tenant\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "arrears",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"arrears\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "arrears_cases",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"arrears_cases\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "attestation_templates",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"attestation_templates\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "blockchain_audit_anchors",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"blockchain_audit_anchors\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "budget_line_items",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"budget_line_items\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "budgets",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"budgets\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "case_summaries",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"case_summaries\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "certification_applications",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"certification_applications\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "compliance_validations",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"compliance_validations\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "cope_contributions",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"cope_contributions\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "cra_xml_batches",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"cra_xml_batches\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "customer_acquisition",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"customer_acquisition\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "digital_signatures",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"digital_signatures\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "dues_rules",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"dues_rules\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "elected_officials",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"elected_officials\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "encryption_keys",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"encryption_keys\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "equity_snapshots",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"equity_snapshots\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "expense_approvals",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"expense_approvals\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "expense_requests",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"expense_requests\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "fund_eligibility",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"fund_eligibility\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "hardship_applications",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"hardship_applications\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "hw_benefit_claims",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"hw_benefit_claims\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "hw_benefit_enrollments",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"hw_benefit_enrollments\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "hw_benefit_plans",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"hw_benefit_plans\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "jurisdiction_rules",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"jurisdiction_rules\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "jurisdiction_rules_summary",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"jurisdiction_rules_summary\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "jurisdiction_templates",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"jurisdiction_templates\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "legislation_tracking",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"legislation_tracking\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "member_demographics",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"member_demographics\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "member_dues_assignments",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"member_dues_assignments\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "member_political_participation",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"member_political_participation\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "members",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"members\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "members_with_pii",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"members_with_pii\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "mrr_snapshots",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"mrr_snapshots\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "organization_hierarchy_audit",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"organization_hierarchy_audit\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "organization_tree",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"organization_tree\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "organizing_activities",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"organizing_activities\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "organizing_volunteers",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"organizing_volunteers\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "pay_equity_complaints",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"pay_equity_complaints\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "pension_actuarial_valuations",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"pension_actuarial_valuations\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "pension_hours_banks",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"pension_hours_banks\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "pension_trustee_boards",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"pension_trustee_boards\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "picket_attendance",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"picket_attendance\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "picket_tracking",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"picket_tracking\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "pii_access_log",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"pii_access_log\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "political_activities",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"political_activities\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "political_campaigns",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"political_campaigns\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "public_donations",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"public_donations\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "revenue_cohorts",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"revenue_cohorts\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "statcan_submissions",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"statcan_submissions\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "statutory_holidays",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"statutory_holidays\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "stipend_disbursements",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"stipend_disbursements\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "strike_funds",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"strike_funds\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "subscription_events",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"subscription_events\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "tax_slips",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"tax_slips\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "tax_year_configurations",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"tax_year_configurations\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "tenant_management_view",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"tenant_management_view\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "tenants",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"tenants\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "transaction_clc_mappings",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"transaction_clc_mappings\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "trust_compliance_reports",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"trust_compliance_reports\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "trusted_certificate_authorities",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"trusted_certificate_authorities\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_annual_remittance_summary",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_annual_remittance_summary\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_certification_expiry_tracking",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_certification_expiry_tracking\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_cope_member_summary",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_cope_member_summary\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_course_session_dashboard",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_course_session_dashboard\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_critical_deadlines",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_critical_deadlines\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_elected_official_engagement",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_elected_official_engagement\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_equity_statistics_anonymized",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_equity_statistics_anonymized\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_hw_claims_aging",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_hw_claims_aging\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_legislative_priorities",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_legislative_priorities\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_member_benefit_eligibility",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_member_benefit_eligibility\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_member_training_transcript",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_member_training_transcript\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_organizing_campaign_dashboard",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_organizing_campaign_dashboard\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_pay_equity_pipeline",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_pay_equity_pipeline\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_pending_remittances",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_pending_remittances\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_pension_funding_summary",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_pension_funding_summary\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_political_campaign_dashboard",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_political_campaign_dashboard\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_tax_slip_summary",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_tax_slip_summary\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_training_program_progress",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_training_program_progress\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "v_workplace_contact_map",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"v_workplace_contact_map\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "vendor_invoices",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"vendor_invoices\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "vendors",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"vendors\", ...)). Does NOT appear in services/financial-service/drizzle/0000_lucky_mole_man.sql's 175-table introspected snapshot - no evidence this relation was ever created in any real database. financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test). Satisfies LATENT_UNREACHABLE: undeployed declaration, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "vote_merkle_tree",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"vote_merkle_tree\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "voting_auditors",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"voting_auditors\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "voting_key_access_log",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"voting_key_access_log\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "voting_session_auditors",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"voting_session_auditors\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
  {
    table: "voting_session_keys",
    classification: "LATENT_UNREACHABLE",
    reason: "CLOSED (round 58D, financial-service latent census): physically declared only in services/financial-service/{src/db,drizzle}/schema.ts (pgTable(\"voting_session_keys\", ...)). Appears verbatim as a CREATE TABLE IF NOT EXISTS statement in services/financial-service/drizzle/0000_lucky_mole_man.sql, a 175-table snapshot the manifest's own prior evidence (analytics-ai.ts, \"members\" entry) established was produced by drizzle-kit introspecting the SAME production database this application uses. However financial-service itself has zero current deployment path (no Dockerfile, no docker-compose entry, no Azure deploy workflow, no infrastructure/** resource - CI only runs typecheck/lint/test, never a real migration apply or deployment). No legitimate live consumer exists today. Satisfies LATENT_UNREACHABLE: real physical relation, zero current reachable authority.",
    supportingCapability: ["services/financial-service/src/db/schema.ts", "services/financial-service/drizzle/schema.ts"],
    requiredRuntimePrivileges: [],
    requiredSystemPrivileges: [],
    invocationAuthority: "NONE",
    dbExecutionPrincipal: "NONE",
    reviewPriority: "NONE",
    scopeDisposition: "DECLARATION_STALE_OR_NONCANONICAL",
  },
]
