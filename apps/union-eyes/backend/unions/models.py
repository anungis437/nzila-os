"""
Django models for unions app.
Auto-generated by Nzila Code Generator — 2026-02-17

DO NOT EDIT manually unless you know what you're doing.
Re-run the generator to overwrite.
"""

import uuid

from django.contrib.postgres.fields import ArrayField
from django.contrib.postgres.search import SearchVectorField
from django.db import models

try:
    from pgvector.django import VectorField
except ImportError:
    VectorField = None  # pgvector not installed


class BaseModel(models.Model):
    """Abstract base with standard audit fields."""

    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        abstract = True


class OrganizationModel(BaseModel):
    """Abstract base for multi-organization models."""

    organization = models.ForeignKey(
        "auth_core.Organizations", on_delete=models.CASCADE, related_name="%(class)ss"
    )

    class Meta:
        abstract = True


class AwardTemplates(BaseModel):
    """Migrated from drizzle: award-templates-schema.ts"""

    name = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "award_templates"
        verbose_name = "AwardTemplates"

    def __str__(self):
        return str(self.name)


class AwardHistory(BaseModel):
    """Migrated from drizzle: award-templates-schema.ts"""

    template = models.ForeignKey(
        "AwardTemplates",
        on_delete=models.CASCADE,
        related_name="award_history_template_set",
    )
    recipient_id = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "award_history"
        verbose_name = "AwardHistory"


class RewardWalletLedger(BaseModel):
    """Migrated from drizzle: award-templates-schema.ts"""

    user_id = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "reward_wallet_ledger"
        verbose_name = "RewardWalletLedger"


class BudgetPool(BaseModel):
    """Migrated from drizzle: award-templates-schema.ts"""

    name = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "budget_pool"
        verbose_name = "BudgetPool"

    def __str__(self):
        return str(self.name)


class BudgetReservations(BaseModel):
    """Migrated from drizzle: award-templates-schema.ts"""

    pool = models.ForeignKey(
        "BudgetPool",
        on_delete=models.CASCADE,
        related_name="budget_reservations_pool_set",
    )
    reserved_amount = models.IntegerField()
    status = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "budget_reservations"
        verbose_name = "BudgetReservations"


class Calendars(BaseModel):
    """Migrated from drizzle: calendar-schema.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "calendars"
        verbose_name = "Calendars"


class CalendarEvents(BaseModel):
    """Migrated from drizzle: calendar-schema.ts"""

    calendar = models.ForeignKey(
        "Calendars",
        on_delete=models.CASCADE,
        related_name="calendar_events_calendar_set",
    )

    class Meta:
        db_table = "calendar_events"
        verbose_name = "CalendarEvents"


class EventAttendees(BaseModel):
    """Migrated from drizzle: calendar-schema.ts"""

    event = models.ForeignKey(
        "CalendarEvents",
        on_delete=models.CASCADE,
        related_name="event_attendees_event_set",
    )

    class Meta:
        db_table = "event_attendees"
        verbose_name = "EventAttendees"


class MeetingRooms(BaseModel):
    """Migrated from drizzle: calendar-schema.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "meeting_rooms"
        verbose_name = "MeetingRooms"


class RoomBookings(BaseModel):
    """Migrated from drizzle: calendar-schema.ts"""

    room = models.ForeignKey(
        "MeetingRooms", on_delete=models.CASCADE, related_name="room_bookings_room_set"
    )

    class Meta:
        db_table = "room_bookings"
        verbose_name = "RoomBookings"


class CalendarSharing(BaseModel):
    """Migrated from drizzle: calendar-schema.ts"""

    calendar = models.ForeignKey(
        "Calendars",
        on_delete=models.CASCADE,
        related_name="calendar_sharing_calendar_set",
    )

    class Meta:
        db_table = "calendar_sharing"
        verbose_name = "CalendarSharing"


class ExternalCalendarConnections(BaseModel):
    """Migrated from drizzle: calendar-schema.ts"""

    user_id = models.TextField()
    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "external_calendar_connections"
        verbose_name = "ExternalCalendarConnections"


class EventReminders(BaseModel):
    """Migrated from drizzle: calendar-schema.ts"""

    event = models.ForeignKey(
        "CalendarEvents",
        on_delete=models.CASCADE,
        related_name="event_reminders_event_set",
    )

    class Meta:
        db_table = "event_reminders"
        verbose_name = "EventReminders"


class CongressMemberships(BaseModel):
    """Migrated from drizzle: congress-memberships-schema.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "congress_memberships"
        verbose_name = "CongressMemberships"


class Holidays(BaseModel):
    """Migrated from drizzle: deadlines-schema.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="holidays_organization_set",
        null=True,
        blank=True,
    )

    class Meta:
        db_table = "holidays"
        verbose_name = "Holidays"


class StewardAssignments(BaseModel):
    """Migrated from drizzle: organizer-workflows.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "steward_assignments"
        verbose_name = "StewardAssignments"


class OutreachSequences(BaseModel):
    """Migrated from drizzle: organizer-workflows.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "outreach_sequences"
        verbose_name = "OutreachSequences"


class OutreachEnrollments(BaseModel):
    """Migrated from drizzle: organizer-workflows.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "outreach_enrollments"
        verbose_name = "OutreachEnrollments"


class OutreachStepsLog(BaseModel):
    """Migrated from drizzle: organizer-workflows.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "outreach_steps_log"
        verbose_name = "OutreachStepsLog"


class FieldNotes(BaseModel):
    """Migrated from drizzle: organizer-workflows.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "field_notes"
        verbose_name = "FieldNotes"


class OrganizerTasks(BaseModel):
    """Migrated from drizzle: organizer-workflows.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "organizer_tasks"
        verbose_name = "OrganizerTasks"


class TaskComments(BaseModel):
    """Migrated from drizzle: organizer-workflows.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "task_comments"
        verbose_name = "TaskComments"


class MemberRelationshipScores(BaseModel):
    """Migrated from drizzle: organizer-workflows.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "member_relationship_scores"
        verbose_name = "MemberRelationshipScores"


class Surveys(BaseModel):
    """Migrated from drizzle: surveys.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "surveys"
        verbose_name = "Surveys"


class SurveyQuestions(BaseModel):
    """Migrated from drizzle: surveys.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "survey_questions"
        verbose_name = "SurveyQuestions"


class SurveyResponses(BaseModel):
    """Migrated from drizzle: surveys.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "survey_responses"
        verbose_name = "SurveyResponses"


class SurveyAnswers(BaseModel):
    """Migrated from drizzle: surveys.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "survey_answers"
        verbose_name = "SurveyAnswers"


class Polls(BaseModel):
    """Migrated from drizzle: surveys.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "polls"
        verbose_name = "Polls"


class PollVotes(BaseModel):
    """Migrated from drizzle: surveys.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "poll_votes"
        verbose_name = "PollVotes"


class MemberLocationConsent(BaseModel):
    """Migrated from drizzle: geofence.ts"""

    user_id = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "member_location_consent"
        verbose_name = "MemberLocationConsent"


class Federations(BaseModel):
    """Migrated from drizzle: federation-schema.ts"""

    organization_id = models.UUIDField()
    name = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "federations"
        verbose_name = "Federations"

    def __str__(self):
        return str(self.name)


class FederationMemberships(BaseModel):
    """Migrated from drizzle: federation-schema.ts"""

    federation_id = models.UUIDField()
    union_organization_id = models.UUIDField()
    status = models.TextField(default="active")
    membership_number = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "federation_memberships"
        verbose_name = "FederationMemberships"


class FederationExecutives(BaseModel):
    """Migrated from drizzle: federation-schema.ts"""

    federation_id = models.UUIDField()
    profile_user_id = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "federation_executives"
        verbose_name = "FederationExecutives"


class FederationMeetings(BaseModel):
    """Migrated from drizzle: federation-schema.ts"""

    federation_id = models.UUIDField()
    title = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "federation_meetings"
        verbose_name = "FederationMeetings"

    def __str__(self):
        return str(self.title)


class FederationRemittances(BaseModel):
    """Migrated from drizzle: federation-schema.ts"""

    federation_id = models.UUIDField()
    from_organization_id = models.UUIDField()
    to_organization_id = models.UUIDField()
    membership_id = models.UUIDField(null=True, blank=True)
    per_capita_remittance_id = models.UUIDField(null=True, blank=True)
    remittance_month = models.IntegerField()
    remittance_year = models.IntegerField()
    period_start = models.DateField(null=True, blank=True)
    period_end = models.DateField(null=True, blank=True)
    due_date = models.DateField()
    total_members = models.IntegerField()
    remittable_members = models.IntegerField()
    per_capita_rate = models.DecimalField(
        max_digits=19, decimal_places=2, null=True, blank=True
    )

    class Meta:
        db_table = "federation_remittances"
        verbose_name = "FederationRemittances"


class FederationCampaigns(BaseModel):
    """Migrated from drizzle: federation-schema.ts"""

    federation_id = models.UUIDField()
    name = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "federation_campaigns"
        verbose_name = "FederationCampaigns"

    def __str__(self):
        return str(self.name)


class FederationCommunications(BaseModel):
    """Migrated from drizzle: federation-schema.ts"""

    federation_id = models.UUIDField()
    title = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "federation_communications"
        verbose_name = "FederationCommunications"

    def __str__(self):
        return str(self.title)


class FederationResources(BaseModel):
    """Migrated from drizzle: federation-schema.ts"""

    federation_id = models.UUIDField()
    title = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "federation_resources"
        verbose_name = "FederationResources"

    def __str__(self):
        return str(self.title)


class VotingSessions(BaseModel):
    """Migrated from drizzle: voting.ts"""

    title = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "voting_sessions"
        verbose_name = "VotingSessions"

    def __str__(self):
        return str(self.title)


class VotingOptions(BaseModel):
    """Migrated from drizzle: voting.ts"""

    session = models.ForeignKey(
        "VotingSessions",
        on_delete=models.CASCADE,
        related_name="voting_options_session_set",
    )

    class Meta:
        db_table = "voting_options"
        verbose_name = "VotingOptions"


class VoterEligibility(BaseModel):
    """Migrated from drizzle: voting.ts"""

    session = models.ForeignKey(
        "VotingSessions",
        on_delete=models.CASCADE,
        related_name="voter_eligibility_session_set",
    )

    class Meta:
        db_table = "voter_eligibility"
        verbose_name = "VoterEligibility"


class Votes(BaseModel):
    """Migrated from drizzle: voting.ts"""

    session = models.ForeignKey(
        "VotingSessions", on_delete=models.CASCADE, related_name="votes_session_set"
    )

    class Meta:
        db_table = "ue_votes"
        verbose_name = "Votes"


class VotingNotifications(BaseModel):
    """Migrated from drizzle: voting.ts"""

    session = models.ForeignKey(
        "VotingSessions",
        on_delete=models.CASCADE,
        related_name="voting_notifications_session_set",
    )

    class Meta:
        db_table = "voting_notifications"
        verbose_name = "VotingNotifications"


class VotingAuditLog(BaseModel):
    """Migrated from drizzle: voting.ts"""

    session = models.ForeignKey(
        "VotingSessions",
        on_delete=models.CASCADE,
        related_name="voting_audit_log_session_set",
    )

    class Meta:
        db_table = "voting_audit_log"
        verbose_name = "VotingAuditLog"


class OrganizingCampaigns(BaseModel):
    """Migrated from drizzle: organizing.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="organizing_campaigns_organization_set",
    )

    class Meta:
        db_table = "organizing_campaigns"
        verbose_name = "OrganizingCampaigns"


class OrganizingContacts(BaseModel):
    """Migrated from drizzle: organizing.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="organizing_contacts_organization_set",
    )

    class Meta:
        db_table = "organizing_contacts"
        verbose_name = "OrganizingContacts"


class CardSigningEvents(BaseModel):
    """Migrated from drizzle: organizing.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="card_signing_events_organization_set",
    )

    class Meta:
        db_table = "card_signing_events"
        verbose_name = "CardSigningEvents"


class NlrbClrbFilings(BaseModel):
    """Migrated from drizzle: organizing.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="nlrb_clrb_filings_organization_set",
    )

    class Meta:
        db_table = "nlrb_clrb_filings"
        verbose_name = "NlrbClrbFilings"


class UnionRepresentationVotes(BaseModel):
    """Migrated from drizzle: organizing.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="union_representation_votes_organization_set",
    )

    class Meta:
        db_table = "union_representation_votes"
        verbose_name = "UnionRepresentationVotes"


class FieldOrganizerActivities(BaseModel):
    """Migrated from drizzle: organizing.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="field_organizer_activities_organization_set",
    )

    class Meta:
        db_table = "field_organizer_activities"
        verbose_name = "FieldOrganizerActivities"


class EmployerResponses(BaseModel):
    """Migrated from drizzle: organizing.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="employer_responses_organization_set",
    )

    class Meta:
        db_table = "employer_responses"
        verbose_name = "EmployerResponses"


class OrganizingCampaignMilestones(BaseModel):
    """Migrated from drizzle: organizing.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="organizing_campaign_milestones_organization_set",
    )

    class Meta:
        db_table = "organizing_campaign_milestones"
        verbose_name = "OrganizingCampaignMilestones"


class RecognitionPrograms(BaseModel):
    """Migrated from drizzle: rewards.ts"""

    org_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "recognition_programs"
        verbose_name = "RecognitionPrograms"


class RecognitionAwardTypes(BaseModel):
    """Migrated from drizzle: rewards.ts"""

    org_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "recognition_award_types"
        verbose_name = "RecognitionAwardTypes"


class RecognitionAwards(BaseModel):
    """Migrated from drizzle: rewards.ts"""

    org_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "recognition_awards"
        verbose_name = "RecognitionAwards"


class RewardBudgetEnvelopes(BaseModel):
    """Migrated from drizzle: rewards.ts"""

    org_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "reward_budget_envelopes"
        verbose_name = "RewardBudgetEnvelopes"


class RewardRedemptions(BaseModel):
    """Migrated from drizzle: rewards.ts"""

    org_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "reward_redemptions"
        verbose_name = "RewardRedemptions"


class MemberAddresses(BaseModel):
    """Migrated from drizzle: addresses.ts"""

    user_id = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "member_addresses"
        verbose_name = "MemberAddresses"


class MemberEmployment(BaseModel):
    """Migrated from drizzle: member-employment.ts"""

    EMPLOYMENT_STATUS_CHOICES = [
        ("active", "Active"),
        ("on_leave", "On Leave"),
        ("layoff", "Layoff"),
        ("suspended", "Suspended"),
        ("terminated", "Terminated"),
        ("retired", "Retired"),
        ("deceased", "Deceased"),
    ]
    EMPLOYMENT_TYPE_CHOICES = [
        ("full_time", "Full Time"),
        ("part_time", "Part Time"),
        ("casual", "Casual"),
        ("seasonal", "Seasonal"),
        ("temporary", "Temporary"),
        ("contract", "Contract"),
        ("probationary", "Probationary"),
    ]

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="member_employment_organization_set",
    )
    member = models.ForeignKey(
        "auth_core.OrganizationMembers",
        on_delete=models.CASCADE,
        related_name="member_employment_member_set",
    )
    employer = models.ForeignKey(
        "Employers",
        on_delete=models.CASCADE,
        related_name="member_employment_employer_set",
        null=True,
        blank=True,
    )
    worksite = models.ForeignKey(
        "Worksites",
        on_delete=models.CASCADE,
        related_name="member_employment_worksite_set",
        null=True,
        blank=True,
    )
    bargaining_unit = models.ForeignKey(
        "BargainingUnits",
        on_delete=models.CASCADE,
        related_name="member_employment_bargaining_unit_set",
        null=True,
        blank=True,
    )
    employment_status = models.CharField(
        choices=EMPLOYMENT_STATUS_CHOICES, max_length=20, default="active"
    )
    employment_type = models.CharField(
        choices=EMPLOYMENT_TYPE_CHOICES, max_length=22, default="full_time"
    )
    hire_date = models.DateField()
    seniority_date = models.DateField()
    termination_date = models.DateField(null=True, blank=True)
    expected_return_date = models.DateField(null=True, blank=True)
    seniority_years = models.DecimalField(
        max_digits=19, decimal_places=2, null=True, blank=True
    )

    class Meta:
        db_table = "member_employment"
        verbose_name = "MemberEmployment"


class EmploymentHistory(BaseModel):
    """Migrated from drizzle: member-employment.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="employment_history_organization_set",
    )
    member = models.ForeignKey(
        "auth_core.OrganizationMembers",
        on_delete=models.CASCADE,
        related_name="employment_history_member_set",
    )
    member_employment = models.ForeignKey(
        "MemberEmployment",
        on_delete=models.CASCADE,
        related_name="employment_history_member_employment_set",
        null=True,
        blank=True,
    )
    change_type = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "employment_history"
        verbose_name = "EmploymentHistory"


class MemberLeaves(BaseModel):
    """Migrated from drizzle: member-employment.ts"""

    LEAVE_TYPE_CHOICES = [
        ("vacation", "Vacation"),
        ("sick", "Sick"),
        ("maternity", "Maternity"),
        ("paternity", "Paternity"),
        ("parental", "Parental"),
        ("bereavement", "Bereavement"),
        ("medical", "Medical"),
        ("disability", "Disability"),
        ("union_business", "Union Business"),
        ("unpaid", "Unpaid"),
        ("lwop", "Lwop"),
        ("other", "Other"),
    ]

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="member_leaves_organization_set",
    )
    member = models.ForeignKey(
        "auth_core.OrganizationMembers",
        on_delete=models.CASCADE,
        related_name="member_leaves_member_set",
    )
    member_employment = models.ForeignKey(
        "MemberEmployment",
        on_delete=models.CASCADE,
        related_name="member_leaves_member_employment_set",
        null=True,
        blank=True,
    )
    leave_type = models.CharField(choices=LEAVE_TYPE_CHOICES, max_length=24)
    start_date = models.DateField()
    end_date = models.DateField(null=True, blank=True)
    expected_return_date = models.DateField(null=True, blank=True)
    actual_return_date = models.DateField(null=True, blank=True)
    is_approved = models.BooleanField(default=False)
    approved_by = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "member_leaves"
        verbose_name = "MemberLeaves"


class JobClassifications(BaseModel):
    """Migrated from drizzle: member-employment.ts"""

    organization = models.ForeignKey(
        "auth_core.Organizations",
        on_delete=models.CASCADE,
        related_name="job_classifications_organization_set",
    )
    bargaining_unit = models.ForeignKey(
        "BargainingUnits",
        on_delete=models.CASCADE,
        related_name="job_classifications_bargaining_unit_set",
        null=True,
        blank=True,
    )
    job_code = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "job_classifications"
        verbose_name = "JobClassifications"


class MemberSegments(BaseModel):
    """Migrated from drizzle: member-segments.ts"""

    organization_id = models.UUIDField()
    name = models.TextField()
    description = models.TextField(null=True, blank=True)
    filters = models.JSONField(default=dict)
    created_by = models.TextField()
    last_executed_at = models.DateTimeField(null=True, blank=True)
    execution_count = models.IntegerField(default=0)
    is_active = models.BooleanField(default=False)
    is_public = models.BooleanField(default=False)

    class Meta:
        db_table = "member_segments"
        verbose_name = "MemberSegments"
        ordering = ["-created_at"]

    def __str__(self):
        return str(self.name)


class SegmentExecutions(BaseModel):
    """Migrated from drizzle: member-segments.ts"""

    segment = models.ForeignKey(
        "MemberSegments",
        on_delete=models.CASCADE,
        related_name="segment_executions_segment_set",
    )

    class Meta:
        db_table = "segment_executions"
        verbose_name = "SegmentExecutions"


class SegmentExports(BaseModel):
    """Migrated from drizzle: member-segments.ts"""

    organization_id = models.UUIDField()
    segment = models.ForeignKey(
        "MemberSegments",
        on_delete=models.CASCADE,
        related_name="segment_exports_segment_set",
        null=True,
        blank=True,
    )

    class Meta:
        db_table = "segment_exports"
        verbose_name = "SegmentExports"


class TrainingCourses(BaseModel):
    """Migrated from drizzle: training.ts"""

    organization_id = models.UUIDField()
    course_code = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "training_courses"
        verbose_name = "TrainingCourses"


class CourseSessions(BaseModel):
    """Migrated from drizzle: training.ts"""

    organization_id = models.UUIDField()
    course_id = models.UUIDField()
    session_code = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "course_sessions"
        verbose_name = "CourseSessions"


class CourseRegistrations(BaseModel):
    """Migrated from drizzle: training.ts"""

    organization_id = models.UUIDField()
    member_id = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "course_registrations"
        verbose_name = "CourseRegistrations"


class MemberCertifications(BaseModel):
    """Migrated from drizzle: training.ts"""

    organization_id = models.UUIDField()
    member_id = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "member_certifications"
        verbose_name = "MemberCertifications"


class TrainingPrograms(BaseModel):
    """Migrated from drizzle: training.ts"""

    organization_id = models.UUIDField()
    program_name = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "training_programs"
        verbose_name = "TrainingPrograms"


class ProgramEnrollments(BaseModel):
    """Migrated from drizzle: training.ts"""

    organization_id = models.UUIDField()
    member_id = models.CharField(max_length=255, null=True, blank=True)

    class Meta:
        db_table = "program_enrollments"
        verbose_name = "ProgramEnrollments"


class Employers(BaseModel):
    """Migrated from drizzle: union-structure-schema.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "employers"
        verbose_name = "Employers"


class Worksites(BaseModel):
    """Migrated from drizzle: union-structure-schema.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "worksites"
        verbose_name = "Worksites"


class BargainingUnits(BaseModel):
    """Migrated from drizzle: union-structure-schema.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "bargaining_units"
        verbose_name = "BargainingUnits"


class Committees(BaseModel):
    """Migrated from drizzle: union-structure-schema.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "committees"
        verbose_name = "Committees"


class CommitteeMemberships(BaseModel):
    """Migrated from drizzle: union-structure-schema.ts"""

    committee_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "committee_memberships"
        verbose_name = "CommitteeMemberships"


class RoleTenureHistory(BaseModel):
    """Migrated from drizzle: union-structure-schema.ts"""

    organization_id = models.UUIDField(null=True, blank=True)

    class Meta:
        db_table = "role_tenure_history"
        verbose_name = "RoleTenureHistory"
