# Django
Django>=5.2.1,<5.3.0
djangorestframework>=3.15.2
django-cors-headers>=4.3.0
django-filter>=23.5
psycopg2-binary>=2.9.9

# Server
gunicorn>=21.2.0

# Celery (for background tasks)
celery[redis]>=5.3.4
redis>=5.0.1
django-redis>=5.4.0
django-celery-beat>=2.6.0
django-celery-results>=2.5.1

# Upstash Redis REST client (parallel path to native rediss://; lets us
# verify the REST token end-to-end without coupling Celery's RESP broker
# to the same credential surface). See docs/nzila-residual-closure/
# r1-pilot-django-sidecar-binding-log.md (2026-05-10T02:05Z entry).
upstash-redis>=1.1.0

# Azure integrations
azure-identity>=1.16.1
azure-storage-blob>=12.19.0
azure-keyvault-secrets>=4.7.0

# SMS
twilio>=8.0.0

# Clerk authentication
pyjwt>=2.8.0
cryptography>=48.0.1

# Utilities
python-dotenv>=1.0.0
requests>=2.32.4

# Development
pytest>=7.4.3
pytest-django>=4.7.0
black>=23.12.1
flake8>=7.0.0

# UnionEyes specific
scikit-learn>=1.5.0
pandas>=2.2.0
numpy>=1.26.3
