# Union Eyes E2E deterministic test environment
# Copy to .env.test (optional) and adjust only for local non-default ports.

AUTH_SECRET=test-auth-secret
VOTING_SECRET=test-voting-secret-0123456789abcdef
QA_TEST_ENV=true
NODE_ENV=test
DATABASE_URL=postgresql://nzila:nzila_dev@localhost:5433/nzila_automation
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3002
