/**
 * Platform adapter barrel — union-eyes
 *
 * Each adapter implements a platform contract interface,
 * connecting the app's internal domain logic to the
 * canonical platform contract shape.
 *
 * See @nzila/platform-contracts for contract definitions.
 *
 * Adapters will be added here as platform contracts are implemented:
 *   - health-adapter
 *   - metrics-adapter
 *   - governance-adapter
 *   - evidence-adapter
 */
export {}
