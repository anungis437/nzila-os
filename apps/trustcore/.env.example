# Auth — Nzila platform-auth (email/password + optional Entra SSO)
AUTH_SECRET=
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=

# Database
DATABASE_URL=postgresql://nzila:nzila_dev@localhost:5433/nzila_automation

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3010

# Dev-only role override — never set in production
# Valid values: platform_admin | org_admin | staff | auditor
TRUSTCORE_DEV_ROLE=
