/**
 * Console — Database Client
 *
 * Re-exports the shared db client for console app code.
 * Import from here (`@/lib/db`) rather than directly from `@nzila/db/client`.
 */
export { db } from '@nzila/db/client'
