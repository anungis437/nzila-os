# Analytics Data

This directory contains analytics data stores.

## Structure

- `cache/` - Cached analytics results for faster dashboard loading
- `models/` - Data models and schemas for analytics modules
- `seeds/` - Sample/seed data for development and testing
