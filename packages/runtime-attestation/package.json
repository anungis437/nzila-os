{
  "name": "@nzila/runtime-attestation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runtime attestation envelopes, content-addressable evidence ledger records, and attestation manifests.",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./attestation": "./src/attestation.ts",
    "./ledger": "./src/ledger.ts",
    "./content-hash": "./src/content-hash.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "zod": "^3.25.0" },
  "devDependencies": { "vitest": "^4.1.7", "@types/node": "^20.0.0" }
}
