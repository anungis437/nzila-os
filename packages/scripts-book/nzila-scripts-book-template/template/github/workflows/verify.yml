# AUTO-GENERATED by nzila-scripts-book-template — do not edit directly
name: Verify

on:
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        working-directory: "{{PRIMARY_APP_PATH}}"
        run: pnpm install --frozen-lockfile

      - name: Lint
        working-directory: "{{PRIMARY_APP_PATH}}"
        run: pnpm lint

      - name: Type-check
        working-directory: "{{PRIMARY_APP_PATH}}"
        run: pnpm type-check

      - name: Test
        working-directory: "{{PRIMARY_APP_PATH}}"
        run: pnpm test

      - name: Build
        working-directory: "{{PRIMARY_APP_PATH}}"
        run: pnpm build
