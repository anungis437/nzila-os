{
  "name": "@nzila/governance-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runtime governance primitives: release identity, environment identity, deployment legitimacy validation, governance assertions.",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./release-identity": "./src/release-identity.ts",
    "./deployment-legitimacy": "./src/deployment-legitimacy.ts",
    "./assertions": "./src/assertions.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "zod": "^3.25.0" },
  "devDependencies": { "vitest": "^4.1.7" }
}
