{
  "name": "@nzila/platform-org-resolver",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 0B.2 §11 — Governed hybrid organization resolver. Returns the canonical platform tenant id (public.orgs.id == union_eyes.organizations.id) for foundational request paths. Fail-closed by default.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./foundational-paths": "./src/foundational-paths.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
