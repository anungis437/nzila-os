# Gitleaks fingerprint allowlist for known-safe findings.
#
# Format (gitleaks 8.x): {filepath}:{rule-id}:{line}
#
# Each entry MUST be reviewed and documented. Do NOT add fingerprints without
# a security review comment explaining why the finding is a false positive.

# ─────────────────────────────────────────────────────────────────────────────
# apps/abr/e2e/courtlens-review-packet.spec.mjs:33
# Rule: nzila-database-url-with-password
# Match: <REDACTED — see referenced file line 33 for the local-dev fallback URL>
# Justification: The rule description explicitly states "not localhost"; the
#   regex is broader than the description and flags the local-dev fallback used
#   only when DATABASE_URL is unset. No real secret is exposed — the literal
#   is a well-known development password published in dev.ps1 / docker-compose.
#   Comment intentionally redacted to prevent Gitleaks from self-matching this
#   file (Phase 0 CI incident 2026-07-19).
# Reviewed: Phase 0 CourtLens refactor gate (2026-07-19).
apps/abr/e2e/courtlens-review-packet.spec.mjs:nzila-database-url-with-password:33

# ─────────────────────────────────────────────────────────────────────────────
# .gitleaksignore:11 (historical, commit d94ef09b1)
# Rule: nzila-database-url-with-password
# Justification: The prior justification comment for the entry above (line 11)
#   previously quoted the exact literal it was allowlisting, which caused
#   Gitleaks to re-flag the .gitleaksignore file itself. That comment was
#   redacted in commit 4abe95f54, but the historical scan range still hits
#   the introducing commit d94ef09b1. This fingerprint allowlists the
#   historical occurrence on that specific path+rule+line. No secret is
#   exposed — see the entry above for the underlying dev-only fallback.
# Reviewed: Phase 0 CourtLens refactor gate (2026-07-19).
.gitleaksignore:nzila-database-url-with-password:11

# ─────────────────────────────────────────────────────────────────────────────
# Phase 0C.1 governed E2E lifecycle files — 6 findings, all the same well-known
# local-dev PostgreSQL admin URL used as a fallback default when
# E2E_DB_ADMIN_URL is unset. The credential pattern (nzila:nzila_dev on
# localhost:5433) is the standard local dev credential documented in dev.ps1,
# docker-compose.yml, and AGENTS.md/CLAUDE.md. The rule regex is intentionally
# broader than its own description ("not localhost") and matches this fallback.
# No secret is exposed — production and staging environments require
# E2E_DB_ADMIN_URL to be provided via environment (see hardening in the
# Phase 0C.2 commit series that removes the fallback going forward). The
# historical commit range still shows the literal.
# Rule: nzila-database-url-with-password
# Reviewed: Phase 0C.2 gate (2026-07-23).
reports/audits/cupe-national-phase-0/phase-0c/phase-0c1-final-report.md:nzila-database-url-with-password:131
apps/union-eyes/scripts/lifecycle/allocate-db.ts:nzila-database-url-with-password:197
apps/union-eyes/scripts/lifecycle/prove-db-allocator.ts:nzila-database-url-with-password:26
reports/audits/cupe-national-phase-0/phase-0c/phase-0c-database-fixture-proof.md:nzila-database-url-with-password:20
apps/union-eyes/scripts/lifecycle/env.ts:nzila-database-url-with-password:75
apps/union-eyes/scripts/lifecycle/env.ts:nzila-database-url-with-password:203

# ─────────────────────────────────────────────────────────────────────────────
# Phase 0C.2 §3 — one historical commit-scoped fingerprint.
# Rule: nzila-database-url-with-password
# Introduced by: commit edfdd1f02 (Phase 0C.2 §8 clean-DB proof harness) at
#   apps/union-eyes/scripts/lifecycle/prove-phase-0c2-clean-migration.ts:195.
# The literal was the standard local-dev fallback (nzila:nzila_dev on
# localhost:5433) used prior to Phase 0C.2 §3 hardening. The current
# lifecycle source no longer contains this fallback — see the resolveAdminUrl()
# helper in allocate-db.ts and the loadGovernedE2EEnv() call at the top of
# prove-phase-0c2-clean-migration.ts (both introduced in the §3 hardening
# commit). This fingerprint is scoped to the introducing commit + exact line
# so it will NOT silently allowlist any future occurrence.
# Reviewed: Phase 0C.2 §3 gate (2026-07-23).
edfdd1f02c178ed83dbeb70efe8b15813d73bf9e:apps/union-eyes/scripts/lifecycle/prove-phase-0c2-clean-migration.ts:nzila-database-url-with-password:195