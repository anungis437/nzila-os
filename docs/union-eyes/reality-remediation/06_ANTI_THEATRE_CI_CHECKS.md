# 06 — Anti-Theatre CI Checks

**Wave:** 1 (PENDING).

Placeholder — will define grep-based CI checks for:

1. 2xx responses containing the string `not_implemented`.
2. Hardcoded readiness flags in pilot/health aggregators.
3. Fabricated provenance labels (e.g. calling a cached FX rate a
   fresh Bank of Canada observation).
4. Capability registry integrity (`REAL` must not depend on
   `NOT_IMPLEMENTED` / `DEMO_ONLY`).
