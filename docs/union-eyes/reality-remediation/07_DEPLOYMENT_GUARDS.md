# 07 — Deployment Guards

**Wave:** 1 (PENDING).

Placeholder — will define the deployment-time guard that rejects
`UE_FEATURE_PROFILE=cupe4373` and `NEXT_PUBLIC_UE_DEMO_PROFILE=cupe4373`
outside `NODE_ENV=development`, and how the guard is wired into
`gitops-deploy.yml`.
