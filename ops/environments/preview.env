# Nzila OS — Preview Environment (ephemeral, per-branch)
# This file is loaded by @nzila/platform-environment for PREVIEW config.
# Secrets must NOT be stored here.

ENVIRONMENT=PREVIEW
ALLOW_DEBUG_LOGGING=true
ALLOW_AI_EXPERIMENTAL=true
DEPLOYMENT_REGION=canadacentral
