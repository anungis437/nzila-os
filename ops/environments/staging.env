# Nzila OS — Staging Environment
# Persistent integration environment for pre-production validation.
# Secrets must NOT be stored here — use CI secrets manager.

ENVIRONMENT=STAGING
ALLOW_DEBUG_LOGGING=true
ALLOW_AI_EXPERIMENTAL=true
DEPLOYMENT_REGION=canadacentral
