-- Fix arbitration_decisions stub table
ALTER TABLE arbitration_decisions
  ADD COLUMN IF NOT EXISTS case_title VARCHAR(500) NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS tribunal tribunal_type NOT NULL DEFAULT 'other',
  ADD COLUMN IF NOT EXISTS decision_type decision_type NOT NULL DEFAULT 'other',
  ADD COLUMN IF NOT EXISTS decision_date TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  ADD COLUMN IF NOT EXISTS filing_date TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS hearing_date TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS arbitrator VARCHAR(200) NOT NULL DEFAULT 'Unknown',
  ADD COLUMN IF NOT EXISTS panel_members JSONB,
  ADD COLUMN IF NOT EXISTS grievor VARCHAR(300),
  ADD COLUMN IF NOT EXISTS "union" VARCHAR(300) NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS employer VARCHAR(300) NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS outcome outcome NOT NULL DEFAULT 'dismissed',
  ADD COLUMN IF NOT EXISTS remedy JSONB,
  ADD COLUMN IF NOT EXISTS key_findings JSONB,
  ADD COLUMN IF NOT EXISTS issue_types JSONB,
  ADD COLUMN IF NOT EXISTS precedent_value precedent_value NOT NULL DEFAULT 'low',
  ADD COLUMN IF NOT EXISTS legal_citations JSONB,
  ADD COLUMN IF NOT EXISTS related_decisions JSONB,
  ADD COLUMN IF NOT EXISTS cba_references JSONB,
  ADD COLUMN IF NOT EXISTS full_text TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS summary TEXT,
  ADD COLUMN IF NOT EXISTS headnote TEXT,
  ADD COLUMN IF NOT EXISTS precedent_summary TEXT,
  ADD COLUMN IF NOT EXISTS reasoning TEXT,
  ADD COLUMN IF NOT EXISTS key_facts TEXT,
  ADD COLUMN IF NOT EXISTS sector VARCHAR(100),
  ADD COLUMN IF NOT EXISTS jurisdiction VARCHAR(50),
  ADD COLUMN IF NOT EXISTS language VARCHAR(10) NOT NULL DEFAULT 'en',
  ADD COLUMN IF NOT EXISTS citation_count INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS view_count INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS embedding TEXT,
  ADD COLUMN IF NOT EXISTS is_public BOOLEAN DEFAULT true,
  ADD COLUMN IF NOT EXISTS access_restrictions TEXT,
  ADD COLUMN IF NOT EXISTS imported_from VARCHAR(200);

-- Fix deadline_rules stub table
ALTER TABLE deadline_rules
  ADD COLUMN IF NOT EXISTS rule_name TEXT,
  ADD COLUMN IF NOT EXISTS rule_code TEXT,
  ADD COLUMN IF NOT EXISTS description TEXT,
  ADD COLUMN IF NOT EXISTS claim_type TEXT,
  ADD COLUMN IF NOT EXISTS priority_level TEXT,
  ADD COLUMN IF NOT EXISTS step_number INTEGER,
  ADD COLUMN IF NOT EXISTS days_from_event INTEGER,
  ADD COLUMN IF NOT EXISTS event_type TEXT,
  ADD COLUMN IF NOT EXISTS business_days_only BOOLEAN DEFAULT false,
  ADD COLUMN IF NOT EXISTS allows_extension BOOLEAN DEFAULT true,
  ADD COLUMN IF NOT EXISTS max_extension_days INTEGER DEFAULT 30,
  ADD COLUMN IF NOT EXISTS requires_approval BOOLEAN DEFAULT false,
  ADD COLUMN IF NOT EXISTS escalate_to_role TEXT,
  ADD COLUMN IF NOT EXISTS escalation_delay_days INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS is_active BOOLEAN DEFAULT true,
  ADD COLUMN IF NOT EXISTS is_system_rule BOOLEAN DEFAULT false,
  ADD COLUMN IF NOT EXISTS created_by TEXT;

-- Fix deadline_extensions stub table
ALTER TABLE deadline_extensions
  ADD COLUMN IF NOT EXISTS requested_by TEXT,
  ADD COLUMN IF NOT EXISTS requested_at TIMESTAMP DEFAULT NOW(),
  ADD COLUMN IF NOT EXISTS requested_days INTEGER,
  ADD COLUMN IF NOT EXISTS request_reason TEXT,
  ADD COLUMN IF NOT EXISTS status TEXT DEFAULT 'pending',
  ADD COLUMN IF NOT EXISTS requires_approval BOOLEAN DEFAULT true,
  ADD COLUMN IF NOT EXISTS approved_by TEXT,
  ADD COLUMN IF NOT EXISTS approval_decision_at TIMESTAMP,
  ADD COLUMN IF NOT EXISTS approval_notes TEXT,
  ADD COLUMN IF NOT EXISTS new_deadline TIMESTAMP,
  ADD COLUMN IF NOT EXISTS days_granted INTEGER;

-- Fix deadline_alerts stub table
ALTER TABLE deadline_alerts
  ADD COLUMN IF NOT EXISTS alert_type TEXT,
  ADD COLUMN IF NOT EXISTS alert_severity TEXT DEFAULT 'info',
  ADD COLUMN IF NOT EXISTS alert_trigger TEXT,
  ADD COLUMN IF NOT EXISTS recipient_id TEXT,
  ADD COLUMN IF NOT EXISTS recipient_role TEXT,
  ADD COLUMN IF NOT EXISTS delivery_method TEXT DEFAULT 'in_app',
  ADD COLUMN IF NOT EXISTS sent_at TIMESTAMP DEFAULT NOW(),
  ADD COLUMN IF NOT EXISTS delivered_at TIMESTAMP,
  ADD COLUMN IF NOT EXISTS delivery_status TEXT,
  ADD COLUMN IF NOT EXISTS delivery_error TEXT,
  ADD COLUMN IF NOT EXISTS viewed_at TIMESTAMP,
  ADD COLUMN IF NOT EXISTS acknowledged_at TIMESTAMP,
  ADD COLUMN IF NOT EXISTS action_taken TEXT,
  ADD COLUMN IF NOT EXISTS action_taken_at TIMESTAMP,
  ADD COLUMN IF NOT EXISTS subject TEXT,
  ADD COLUMN IF NOT EXISTS message TEXT,
  ADD COLUMN IF NOT EXISTS action_url TEXT;
