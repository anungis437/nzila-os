# Archived workflows

These workflows were superseded but kept for git history / rollback.

- `agri-gov-ingestion-check.yml` (2026-04-21) - Superseded by `agri-core-check.yml` which now runs the same scripts AND broader package tests under an expanded path filter that includes `fixtures/agri/**`.
